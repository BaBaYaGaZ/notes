# 第 1 课：浏览器如何找到你的程序

## 一、先建立最重要的一张图

以后你学习 Web、Spring Boot，脑子里要一直有这张最基本的图：

```text
客户端
  │
  │ 发出请求
  ↓
服务器
  │
  │ 处理请求
  ↓
返回结果
```

比如：

```text
Chrome 浏览器
       │
       │ “我要查询用户 1”
       ↓
Spring Boot 后端
       │
       │ 查到用户信息
       ↓
返回用户数据
```

这就是 Web 开发最核心的事情之一。

你以后看到再复杂的代码，本质上很多事情都围绕：

> **谁在发请求？请求发给谁？服务器收到以后做什么？最后返回什么？**

展开。

------

# 二、什么是客户端 Client？

先不要背定义。

你现在打开 Chrome，在地址栏输入一个网址：

```text
https://www.example.com
```

Chrome 会主动去找某台服务器：

> “我要这个网页。”

这里 Chrome 是**主动发起请求的一方**。

那么在这次通信里，Chrome 就叫：

> **客户端 Client**

所以你暂时可以把客户端理解成：

> **主动向服务器提出要求的程序。**

常见客户端有：

```text
Chrome
Edge
Firefox
手机 App
Postman
前端网页中的 JavaScript
```

例如以后你会经常使用 Postman。

Postman 本质上就是一个专门帮程序员：

> “向后端发送 HTTP 请求”

的客户端。

所以：

```text
Chrome → 客户端
Postman → 客户端
Vue 前端 → 也可以向后端发请求
```

------

# 三、什么是服务器 Server？

客户端既然在：

> “向别人要东西”

那么总要有一个程序负责接待它。

这个程序就是服务器。

例如：

```text
浏览器：
给我用户 1 的信息。

↓

服务器：
好的，我找一下。

↓

服务器：
找到了。
名字 Tom，年龄 20。

↓

浏览器：
收到。
```

这里负责：

- 等待客户端请求
- 接收请求
- 处理请求
- 返回结果

的一方就是：

> **服务器 Server**

------

## 一个很重要的纠正

初学者很容易把“服务器”理解成：

> 一台放在机房里的巨大电脑。

实际上 **Server 既可以指机器，也可以指提供服务的软件**。

我们学习 Spring Boot 时，更关注的是后者。

比如你自己的 Windows 电脑完全可以：

```text
既运行 Chrome
又运行 Spring Boot
```

于是同一台电脑上：

```text
Chrome
客户端

↓

Spring Boot 程序
服务器端
```

也就是说：

> **客户端和服务器完全可以运行在同一台电脑上。**

这点非常重要。

因为你学习 Spring Boot 时，大多数时候就是这么干的。

------

# 四、Spring Boot 在这里是什么角色？

现在我们只提前认识一下，不学习 Spring Boot 本身。

以后你启动一个 Spring Boot Web 项目，很可能看到类似：

```text
Tomcat started on port 8080
```

这时你的电脑里有一个程序正在：

> **等待 HTTP 请求。**

对于现阶段，你可以先简单理解为：

```text
Spring Boot 后端
=
服务器端程序
```

更准确地说，典型 Spring Boot Web 项目里面通常还会有一个 Web 服务器，例如 Tomcat。

以后我们会再拆：

```text
Spring Boot
Tomcat
Controller
```

现在完全不用管。

这一课你只需要知道：

> **Spring Boot 项目运行起来以后，可以等待浏览器、Postman、前端程序来访问它。**

------

# 五、问题来了：客户端怎么找到服务器？

假设世界上有无数台电脑。

你的 Chrome 想访问一个服务器，总不能喊：

> “喂！服务器！”

因为它不知道服务器在哪。

所以首先需要解决：

> **我要找哪台电脑？**

这就涉及：

# IP 地址

你可以暂时把 IP 地址理解成：

> **网络中用来确定一台设备位置的地址。**

比如：

```text
192.168.1.10
```

或者：

```text
8.8.8.8
```

都是 IPv4 地址的形式。

这里不需要学习二进制、子网掩码、路由。

你只需要抓住一个核心作用：

```text
IP 地址
↓
帮助确定我要访问哪台设备
```

可以粗略类比成：

```text
IP地址 ≈ 楼的地址
```

但注意，这只是帮助理解。

------

# 六、那 localhost 是什么？

这就是你以后开发 Spring Boot 会天天遇见的东西：

```text
localhost
```

它表达的意思非常简单：

> **我自己这台电脑。**

比如你在自己的电脑上运行 Spring Boot，然后浏览器输入：

```text
http://localhost:8080
```

这里的意思就是：

> “我要访问**我自己这台电脑**上的某个服务。”

所以：

```text
localhost
```

并不是某个互联网上的远程服务器。

它是在指：

```text
当前这台电脑
```

------

# 七、127.0.0.1 又是什么？

你可能还见过：

```text
127.0.0.1
```

现阶段可以这样理解：

```text
localhost
≈
127.0.0.1
≈
我自己这台电脑
```

所以很多时候：

```text
http://localhost:8080
```

和：

```text
http://127.0.0.1:8080
```

访问的是同一个本机服务。

严格来说：

- `localhost` 是一个名字
- `127.0.0.1` 是 IPv4 的回环地址

但你现在不需要研究“回环网络”原理。

知道：

> **开发时看到 localhost 或 127.0.0.1，通常就是在访问自己电脑上的程序。**

就足够了。

------

# 八、但是只有 IP 还不够

这里是这一课非常关键的一步。

假设你的电脑地址已经确定了。

但你的电脑同时运行：

```text
Chrome
微信
MySQL
Redis
Spring Boot
Steam
QQ
```

如果有人只是说：

> “我要访问 Zhang Hao 的电脑。”

电脑会问：

> “你到底找哪个程序？”

这时候就需要：

# 端口 Port

端口就是用来进一步区分：

> **这台电脑上的哪个网络服务。**

所以你可以先形成一个非常实用的模型：

```text
IP
↓
找到哪台电脑

端口
↓
找到这台电脑上的哪个服务
```

这是今天最重要的结论之一。

------

# 九、为什么会有 8080、3306、6379？

你以后会经常看到这些数字：

```text
8080
3306
6379
```

它们就是端口号。

开发中常见的情况：

```text
Spring Boot Web 项目
常见：8080

MySQL
默认：3306

Redis
默认：6379
```

所以假设你电脑上同时运行：

```text
Spring Boot → 8080

MySQL → 3306
```

那么：

```text
localhost:8080
```

大致意思是：

> 找我自己电脑上，监听 8080 端口的那个服务。

而：

```text
localhost:3306
```

则是在找：

> 我自己电脑上，监听 3306 端口的服务。

这两个访问目标完全不同。

------

# 十、一个很重要的类比

现在我们可以把 IP 和端口组合起来理解。

假设：

```text
IP = 小区地址
端口 = 房间号
```

那么：

```text
192.168.1.10:8080
```

就像：

> 去 `192.168.1.10` 这栋楼，找 `8080` 号房间。

虽然真实计算机网络并不是“房间”，但这个类比非常适合现阶段建立直觉。

所以：

```text
localhost:8080
```

就是：

> 找我自己这台电脑，再找其中使用 8080 端口的服务。

------

# 十一、为什么 Spring Boot 经常是 localhost:8080？

假设以后你启动 Spring Boot 项目。

它启动了一个 Web 服务，并监听：

```text
8080
```

那么你打开 Chrome：

```text
http://localhost:8080
```

浏览器其实是在做：

```text
localhost
↓
找到自己的电脑

8080
↓
找到 Spring Boot Web 服务

然后发送 HTTP 请求
```

因此以后你看到：

```text
localhost:8080
```

不要把它当成一个需要死记的固定字符串。

你应该能读出它的意思：

> **访问本机 8080 端口上的 Web 服务。**

------

# 十二、现在终于可以看 URL 了

例如教学计划中的：

```text
http://localhost:8080/users/1
```

这整个东西通常叫：

> **URL**

URL 可以理解成：

> **告诉客户端“去哪里访问什么资源”的地址。**

我们拆开来看：

```text
http://localhost:8080/users/1
│       │         │      │
│       │         │      └─ 路径
│       │         └──────── 端口
│       └────────────────── 主机
└────────────────────────── 协议
```

一个一个来。

------

# 十三、第一部分：http

```text
http://
```

这里表示：

> **使用 HTTP 这种规则进行通信。**

你下一课会专门学习 HTTP。

现在只需要知道：

```text
http
```

表示：

> 浏览器和服务器使用 HTTP 进行交流。

暂时不要追 TCP，也不要研究 HTTP 协议细节。

下一课解决。

------

# 十四、第二部分：localhost

```text
http://localhost:8080/users/1
       ↑
```

这是：

> **主机 Host**

也就是：

> 我要访问哪台机器。

这里是：

```text
localhost
```

因此：

> 我要访问我自己这台电脑。

如果以后变成：

```text
http://192.168.1.100:8080
```

那就是访问：

```text
192.168.1.100
```

这台机器。

------

# 十五、第三部分：8080

```text
http://localhost:8080/users/1
                 ↑
```

这是端口。

表示：

> 找这台电脑上使用 8080 端口的服务。

到现在：

```text
http://localhost:8080
```

你应该已经可以读成：

> 使用 HTTP，访问自己电脑上的 8080 端口服务。

------

# 十六、第四部分：/users/1

```text
http://localhost:8080/users/1
                     ↑
```

这是：

> **路径 Path**

可以暂时理解成：

> 我具体想访问服务器提供的哪个资源或功能。

例如服务器里可能有：

```text
/users
/orders
/articles
/login
```

它们代表不同的东西。

比如：

```text
/users/1
```

以后很可能表达：

> 用户资源中的 1 号用户。

但这就开始涉及 REST，我们会在第 7 课正式学。

目前你只需要知道：

```text
/users/1
```

属于 URL 中的：

> 路径。

------

# 十七、URL 还可以带参数

现在看教学计划要求你最终能解释的这个：

```text
http://localhost:8080/users?id=1
```

注意后面出现了：

```text
?id=1
```

这里是：

> **Query 参数**

也叫查询参数。

可以理解成：

> 在访问某个路径时，额外带给服务器的一些信息。

比如：

```text
/users?id=1
```

可以表达：

> 访问 `/users`，同时告诉服务器 `id=1`。

再比如：

```text
/users?page=2
```

表示：

> 访问 `/users`，同时告诉服务器我要第 2 页。

如果有多个参数，常见形式是：

```text
/users?page=2&size=10
```

意思就是：

```text
page = 2
size = 10
```

参数到底怎么传，我们第 4 课会专门讲。

今天只需要先认识它。

------

# 十八、把整个 URL 从头到尾翻译成人话

现在我们来看今天最终目标：

```text
http://localhost:8080/users?id=1
```

把它拆开：

```text
http
```

→ 使用 HTTP 通信。

```text
localhost
```

→ 访问我自己的电脑。

```text
8080
```

→ 找这台电脑上使用 8080 端口的服务。

```text
/users
```

→ 访问服务器中的 `/users` 这个路径。

```text
?id=1
```

→ 额外携带一个参数：

```text
id = 1
```

所以把整句话翻译成人话：

> **浏览器使用 HTTP，访问自己电脑上 8080 端口运行的 Web 服务，请求 `/users` 这个路径，并额外告诉服务器 `id=1`。**

如果你真正理解了这句话，那么今天最核心的目标就已经达到了。

------

# 十九、把它放回 Spring Boot 场景

以后你可能会：

```text
1. 在 IDEA 里启动 Spring Boot
```

然后程序提示：

```text
Tomcat started on port 8080
```

你再打开：

```text
http://localhost:8080/users?id=1
```

现在你应该知道整个过程大概是：

```text
Chrome
  │
  │ HTTP 请求
  ↓
localhost
也就是你自己的电脑
  │
  ↓
8080 端口
  │
  ↓
Spring Boot Web 服务
  │
  ↓
寻找 /users 对应的处理逻辑
  │
  ↓
同时拿到 id=1
```

至于：

> Spring Boot 到底怎么知道 `/users` 应该交给哪段 Java 代码？

**现在不要管。**

那就是后面 Controller 的工作。

我们现在只是把 Controller 前面的道路修好。

------

# 二十、今天真正需要记住的只有 6 件事

### 1. Client

主动发送请求的一方。

例如：

```text
Chrome
Postman
```

------

### 2. Server

等待请求、处理请求、返回结果的一方。

以后你的 Spring Boot 后端就是服务器端。

------

### 3. IP

主要解决：

> **哪台设备？**

------

### 4. localhost / 127.0.0.1

开发时通常表示：

> **我自己这台电脑。**

------

### 5. Port

主要解决：

> **这台电脑上的哪个服务？**

所以：

```text
localhost:8080
```

就是：

> 本机的 8080 端口服务。

------

### 6. URL

例如：

```text
http://localhost:8080/users?id=1
```

包含：

```text
http          协议
localhost     主机
8080          端口
/users        路径
id=1          Query 参数
```

这正是本课教学计划要求最终掌握的能力。

------

# 第一课小测

先不要往下学习 HTTP。你可以直接回复答案，我会根据你的回答判断哪些地方需要补。

**1.** 你在自己的电脑上启动了 Spring Boot，它监听 `8080` 端口。你打开 Chrome 访问：

```text
http://localhost:8080
```

这里谁是客户端，谁是服务器端？

**2.** 为什么：

```text
localhost:8080
```

和：

```text
localhost:3306
```

可能访问的是完全不同的程序？

**3.** 把下面这个 URL 拆开解释：

```text
http://localhost:8080/users?page=2
```

分别说明：

```text
http
localhost
8080
/users
page=2
```

各是什么意思。

**4. 思考题：**

假设 Spring Boot 程序明明运行在：

```text
8080
```

你却访问：

```text
http://localhost:8081
```

你觉得为什么通常访问不到你的 Spring Boot 程序？

你不用追求标准答案，直接按照自己的理解说即可。下一轮我先批这四题，再决定第一课是否需要补充，然后才进入第 2 课 HTTP。