# 第 1 课：浏览器如何找到你的程序

## 一、先建立最重要的一张图

```text
客户端
  │
  │ 发出请求
  ↓
服务器
  │
  │ 处理请求
  ↓
返回结果
```

比如：

```text
Chrome 浏览器
       │
       │ “我要查询用户 1”
       ↓
Spring Boot 后端
       │
       │ 查到用户信息
       ↓
返回用户数据
```

这就是 Web 开发最核心的事情之一。

再复杂的代码，本质上很多事情都是：

**谁在发请求？请求发给谁？服务器收到以后做什么？最后返回什么？**

------



## 二、什么是客户端 Client？

可以把客户端理解成：主动向服务器提出要求的**程序**

常见客户端有：

```text
Chrome
Edge
Firefox
手机 App
Postman
前端网页中的 JavaScript
```

**Postman** 本质上就是一个专门帮程序员：**“向后端发送 HTTP 请求” 的客户端**

------



## 三、什么是服务器 Server？

服务器 Server 负责：

- 等待客户端请求
- 接收请求
- 处理请求
- 返回结果

 Server 既可以**指机器**，也可以**指提供服务的软件**

客户端和服务器完全可以运行在同一台电脑上

------



## 四、127.0.0.1 和 localhost

`127.0.0.1` 是 IPv4 的回环地址，`localhost` 是它的名字

现阶段可以这样理解：

```text
localhost
≈
127.0.0.1
≈
我自己这台电脑
```

所以很多时候：

```text
http://localhost:8080
```

和：

```text
http://127.0.0.1:8080
```

访问的是同一个本机服务

------



## 五、端口 Port

端口就是用来进一步区分：**这台电脑上的哪个网络服务**

```text
IP
↓
找到哪台电脑

端口
↓
找到这台电脑上的哪个服务
```

------

### 端口号

开发中常见的情况：

```text
Spring Boot Web 项目
常见：8080

MySQL
默认：3306

Redis
默认：6379
```

所以假设电脑上同时运行：

```text
Spring Boot → 8080
MySQL → 3306
```

```text
localhost:8080
```

大致意思是：找我自己电脑上，监听 8080 端口的那个服务

```text
localhost:3306
```

大致意思是：我自己电脑上，监听 3306 端口的服务

------



## 六、URL

```text
http://localhost:8080/login/users?id=1
```

这整个东西通常叫：**URL**

URL 可以理解成：**告诉客户端 “去哪里访问什么资源” 的地址**

我们拆开来看：

```text
http://localhost:8080/login/users?id=1
│       │         │      │         │
└────────────────────────────────────── 协议：使用 HTTP 这种规则进行通信
        └────────────────────────────── 主机：访问我自己这台电脑
                  └──────────────────── 端口：找这台电脑上使用 8080 端口的服务
                         └───────────── 路径：具体要访问服务器提供的某个资源或功能
                                   └─── 查询参数：在访问某个路径时，额外带给服务器的一些信息
       

```

------



# 第2课：HTTP 到底是什么

## 一、一次完整的 HTTP 通信是什么样？

先看最重要的一张图：

```
客户端                         服务器

Chrome
  |
  |
  |  HTTP请求 Request
  | -------------------->
  |
  |
  | <--------------------
  |  HTTP响应 Response
  |
Spring Boot
```

也就是说：

一次访问网页，本质上就是：

1. 浏览器发送请求；
2. 服务器处理请求；
3. 服务器返回响应。

这就是 Web 开发最核心的循环。

------

举个生活例子：

你去餐厅：

```
你：
老板，我要一份牛肉面。

老板：
好的。

老板：
你的牛肉面好了。
```

对应到 HTTP：

```
浏览器：
我要用户1的信息。

服务器：
好的。

服务器：
用户1的信息如下。
```

------

# 三、HTTP 请求 Request

我们先看请求。

请求就是：

> **客户端告诉服务器：我想做什么。**

例如：

浏览器访问：

```
http://localhost:8080/users/1
```

它可能发送：

```
GET /users/1
```

这句话包含两个重要信息：

------

## 1. GET

表示：

> 我要进行什么操作。

这里：

```
GET
```

意思：

> 获取数据。

例如：

```
我要查询用户信息。
```

后面的课程我们会专门学习：

- GET
- POST
- PUT
- DELETE

现在只需要知道：

HTTP 请求首先要告诉服务器：

> “我要干什么。”

------

## 2. /users/1

表示：

> 我要访问服务器的哪个资源。

也就是：

路径。

上一课我们学习 URL：

```
http://localhost:8080/users/1
```

其中：

```
/users/1
```

就是路径。

所以：

```
GET /users/1
```

可以理解为：

> 使用 GET 方法，请求服务器里的 `/users/1`。

------

# 四、HTTP 请求里面还有什么？

刚才：

```
GET /users/1
```

只是最简单的请求。

真实请求通常更完整：

例如：

```
GET /users/1 HTTP/1.1

Host: localhost:8080

User-Agent: Chrome
```

不用害怕。

我们慢慢拆。

------

## 1. 请求头 Header

请求头可以理解为：

> 客户端附带的一些说明信息。

比如：

```
User-Agent: Chrome
```

意思：

> “我是 Chrome 浏览器。”

再比如：

```
Content-Type: application/json
```

意思：

> “我发送的数据格式是 JSON。”

以后你学习 Spring Boot，经常会看到：

```
Authorization: Bearer xxxxx
```

这表示：

> “这是我的身份认证信息。”

------

现在阶段你不用记具体有哪些 Header。

你只需要形成概念：

> Header 是请求的附加说明。

类似寄快递：

快递里面有：

```
包裹
```

还有：

```
备注：
易碎
到付
电话
```

Header 就像这些备注。

------

## 2. 请求体 Body

Body 是：

> 真正携带的数据。

例如：

你注册账号：

```
{
    "username":"zhanghao",
    "password":"123456"
}
```

这就是请求体。

也就是说：

请求可以包含：

```
请求
 |
 |-- 方法
 |
 |-- 地址
 |
 |-- Header
 |
 |-- Body
```

------

但是注意：

GET 请求通常没有 Body。

例如：

查询用户：

```
GET /users/1
```

你只需要告诉服务器：

> 我要哪个用户。

服务器已经知道。

但是创建用户：

```
POST /users
```

服务器需要知道：

> 新用户叫什么？

所以需要：

```
{
    "name":"Tom",
    "age":20
}
```

这个以后讲 POST 时会重点理解。

------

# 五、服务器返回什么？Response

请求发送出去以后：

服务器处理。

然后返回：

> HTTP 响应 Response。

对应：

```
Request
请求

↓

Response
响应
```

------

例如：

浏览器：

```
GET /users/1
```

服务器：

```
{
    "id":1,
    "name":"Tom"
}
```

这就是响应。

------

# 六、响应里面有什么？

和请求类似：

响应也有：

```
响应
 |
 |-- 状态码
 |
 |-- Header
 |
 |-- Body
```

------

## 1. 状态码 Status Code

这是 HTTP 非常重要的概念。

它告诉客户端：

> “你的请求结果怎么样。”

例如：

### 200

表示：

成功。

比如：

你请求：

```
查询用户1
```

服务器：

```
找到了
```

返回：

```
200 OK
```

------

### 404

表示：

找不到。

比如：

你访问：

```
/users/999999
```

服务器：

> 没有这个用户。

返回：

```
404 Not Found
```

------

### 500

表示：

服务器自己出问题。

比如：

代码报错：

```
NullPointerException
```

服务器：

> 我处理不了。

返回：

```
500 Internal Server Error
```

------

第六课我们会专门学习状态码。

现在先形成一个感觉：

```
200
成功

404
找不到

500
服务器错误
```

------

## 2. 响应 Body

Body 是服务器真正返回的数据。

例如：

```
{
    "id":1,
    "name":"Tom"
}
```

浏览器拿到以后：

可以显示网页；

前端程序可以读取数据；

App 可以展示内容。

------

# 七、完整走一遍 Spring Boot 场景

现在把今天所有知识串起来。

假设：

你打开浏览器：

```
http://localhost:8080/users/1
```

发生什么？

------

## 第一步：找到服务器

浏览器看到：

```
localhost:8080
```

知道：

```
localhost
=
我的电脑

8080
=
找8080端口程序
```

找到：

Spring Boot。

------

## 第二步：发送 HTTP 请求

浏览器发送：

```
GET /users/1
```

意思：

> 我要查询用户1。

------

## 第三步：Spring Boot 处理

这里先不要管内部细节。

你只需要知道：

服务器收到请求。

------

## 第四步：返回 HTTP 响应

服务器返回：

状态：

```
200 OK
```

数据：

```
{
    "id":1,
    "name":"Tom"
}
```

------

整个过程：

```
Chrome

发送：

GET /users/1

↓

Spring Boot

处理

↓

返回：

200 OK

{
"id":1,
"name":"Tom"
}

↓

Chrome显示结果
```

这就是一个最基本的 Web 请求。

------

# 八、现在和未来 Spring Boot 的关系

这一课最重要的目的，是让你以后看到 Controller 不迷糊。

例如以后你看到：

```
@GetMapping("/users/{id}")
public User getUser(Long id){

}
```

现在你还不知道代码细节。

没关系。

但是你应该知道：

这个东西大概是在说：

> “当有人发送 HTTP GET 请求访问 `/users/{id}` 时，让这个 Java 方法处理它。”

也就是说：

```
HTTP请求

↓

Controller

↓

Java方法
```

这条线以后会成为 Spring Boot 学习的核心。

------

# 九、本节最重要的知识

今天不要记几十个 HTTP 细节，只记下面这些。

## 1. HTTP 是什么？

> 浏览器和服务器通信的规则。

------

## 2. HTTP 通信有两个方向

客户端：

```
Request 请求
```

服务器：

```
Response 响应
```

------

## 3. 请求包含什么？

简单理解：

```
请求

方法
+
地址
+
Header
+
Body
```

------

## 4. 响应包含什么？

简单理解：

```
响应

状态码
+
Header
+
Body
```

------

## 5. Spring Boot 在哪里？

目前只需要知道：

```
浏览器

↓

HTTP请求

↓

Spring Boot

↓

HTTP响应
```

------

# 课后思考题（不需要反馈给我，可以交给其他 AI）

你可以让其他 AI 批改。

### 1.

用户访问：

```
GET /users/1
```

这里：

- GET 表示什么？
- `/users/1` 表示什么？

------

### 2.

下面哪个属于请求，哪个属于响应？

A：

```
{
 "name":"Tom"
}
```

B：

```
404 Not Found
```

------

### 3.

为什么说 HTTP 是浏览器和服务器之间的“交流规则”？

------

### 4.

假设：

浏览器发送：

```
GET /users/1
```

服务器返回：

```
{
"id":1,
"name":"Tom"
}
```

请你用自己的话描述整个过程。

------

下一课进入第二模块：

**第3课：HTTP 请求方式——为什么查询用 GET，创建用 POST？**

这一课会开始把 HTTP 和真实后端开发联系起来。第二阶段：Web基础教学计划.mdMD
