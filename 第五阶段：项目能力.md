# 第1课：Git（基础）

这一课我们不把 Git 当成“需要背命令的工具”来学，而是站在**真实项目开发**的角度理解它。

你前面已经学过 Git 的安装和一些基本操作，所以这次重点不是重新讲“Git 是什么”，而是建立一个更重要的认知：

> **为什么一个真正的软件项目，几乎离不开 Git。**

按照你的教学计划，这一课主要覆盖：为什么需要 Git、工作区、暂存区、仓库、第一次 Commit、查看历史、恢复版本，以及完整的 `写代码 → git add → git commit` 流程。

------

## 一、先理解 Git 到底解决了什么问题

### 1. 没有 Git 时，项目开发会变成什么样

假设你现在正在做一个 Spring Boot 用户管理项目。

项目原本可以正常运行：

```
UserController
UserService
UserMapper
```

今天你决定修改登录功能。

你改了十几个地方，结果项目突然启动失败。

这时候你可能会想：

> “我刚才到底改了哪些地方？”

如果没有 Git，你最原始的解决办法可能是：

```
user-system
user-system-备份
user-system-最终版
user-system-最终版2
user-system-真的最终版
user-system-修改前备份
```

这种方法的问题非常明显。

你很难回答：

```
哪个版本能运行？

这一版和上一版改了什么？

昨天正常的代码在哪里？

到底是谁改坏的？
```

Git 本质上就是在解决这类问题。

它让一个项目拥有了：

```
项目当前状态
+
历史版本
+
每次修改记录
```

所以可以把 Git 暂时理解成：

> **项目的版本历史管理系统。**

你不是单纯在“保存文件”，而是在不断给整个项目留下**可追溯的历史节点**。

------

### 2. Git 最核心的能力：留下“存档点”

例如你开发一个项目：

```
第一次：

项目初始化完成
```

然后：

```
第二次：

完成用户查询接口
```

再然后：

```
第三次：

完成用户新增接口
```

Git 可以把它们保存成：

```
Commit A
项目初始化完成

↓

Commit B
完成用户查询接口

↓

Commit C
完成用户新增接口
```

这里的每一个 **Commit**，都可以粗略理解成：

> **一次项目存档。**

这个概念非常重要。

玩游戏时你不会只保存“当前画面”，而是留下一个存档点：

```
打Boss前
```

失败以后还能回来。

Git 的 Commit 很像这个思想。

当然 Git 的实现远比“复制整个项目文件夹”高效，但现阶段不用管底层实现。

你只需要先形成：

```
开发一小段功能

↓

确认现在代码状态不错

↓

Commit

↓

继续开发
```

这样的习惯。

------

### 3. Git 和普通“Ctrl+S”不是一回事

这是初学 Git 时很容易混淆的地方。

你在 IDEA 中按：

```
Ctrl + S
```

只是：

> 把当前代码保存到硬盘。

例如原来：

```
return "hello";
```

你改成：

```
return "hello world";
```

保存以后，原内容通常就被覆盖了。

而 Git 的 Commit 是：

> 把这个阶段的项目状态记录进版本历史。

所以：

```
保存文件
```

和：

```
保存版本
```

是完全不同的两件事。

以后你看到：

```
git commit
```

脑子里不要理解成：

> 保存代码。

更准确地应该理解成：

> **创建一个版本记录。**

------

## 二、Git 最重要的三个区域

这一课最需要真正理解的是：

```
工作区
暂存区
仓库
```

这三个东西如果理解了，`git add`、`git commit` 就会非常自然。

------

### 1. 工作区：你正在修改的项目

例如：

```
D:\GithubProjects\user-system
```

里面有：

```
src
pom.xml
application.yml
```

你在 IDEA 中打开这个项目，然后修改：

```
UserController.java
```

这个你正在实际操作的项目目录，就是：

> **工作区（Working Directory）**

简单理解：

```
你看得见、正在编辑的文件
```

就在工作区。

例如今天你修改了：

```
UserController.java
UserService.java
application.yml
```

这些修改首先都只是存在于：

```
工作区
```

Git 能发现：

> 这些文件和上一次 Commit 相比发生变化了。

但它们还没有自动进入下一个版本。

------

### 2. 暂存区：决定“这一次准备提交什么”

这是 Git 初学阶段最容易觉得多余的东西。

很多人第一次学都会问：

> 为什么不能修改完以后直接 Commit？

因为有时候你一天改了很多东西，但你并不想一次全部提交。

例如你现在同时改了：

```
UserController.java
UserService.java
README.md
application.yml
```

其中：

```
UserController.java
UserService.java
```

属于：

```
新增用户查询功能
```

而：

```
README.md
```

只是你顺便改了项目说明。

`application.yml`：

```
则是你为了本地调试临时改了一些配置
```

这时候你可以只选择：

```
UserController.java
UserService.java
```

执行：

```
git add
```

它们就进入：

> **暂存区（Staging Area）**

所以暂存区可以理解成：

> **“下一次 Commit 的候选清单”。**

这是非常准确的理解。

------

### 3. `git add` 并不是“上传”

这一点必须纠正。

很多 Git 初学者看到：

```
git add
```

容易理解成：

> 把代码添加进 Git。

甚至理解成：

> 上传代码。

都不准确。

它真正的意思更接近：

> **把当前这个修改放进下一次提交的准备区。**

例如：

```
工作区：

UserController 修改了
UserService 修改了
README 修改了
```

执行：

```
git add UserController.java
git add UserService.java
```

就变成：

```
工作区
├── UserController 修改
├── UserService 修改
└── README 修改

暂存区
├── UserController
└── UserService
```

此时 README 没有进入暂存区。

那么下一次 Commit 时：

```
UserController
UserService
```

会进入版本记录。

README 不会。

------

### 4. 仓库：已经真正形成历史的地方

第三个区域叫：

> **仓库（Repository）**

你执行：

```
git commit
```

以后，暂存区中的内容会形成一个新的 Commit。

例如：

```
暂存区：

UserController.java
UserService.java
```

执行：

```
git commit -m "完成用户查询功能"
```

Git 就会创建：

```
Commit：

完成用户查询功能
```

这时候这个版本才真正进入：

```
Git 仓库
```

所以完整关系是：

```
工作区
   │
   │ git add
   ↓
暂存区
   │
   │ git commit
   ↓
本地仓库
```

这张图建议你记住。

因为以后绝大多数 Git 基础操作，本质上都围绕这三个区域转。

------

## 三、一次真正的 Git 开发流程

### 1. 从修改代码开始

假设你已经有一个 Git 项目。

先查看当前状态：

```
git status
```

比如 Git 告诉你：

```
nothing to commit, working tree clean
```

意思就是：

> 当前工作区和最新 Commit 一致，没有新的修改。

这时你修改：

```
@GetMapping("/users")
public List<User> list() {
    return userService.list();
}
```

再次执行：

```
git status
```

Git 可能告诉你：

```
modified: UserController.java
```

意思是：

> Git 发现这个文件被你修改了。

但是这时候：

```
修改只在工作区。
```

------

### 2. 使用 `git add`

你准备把这个修改加入下一次版本：

```
git add UserController.java
```

也可以一次暂存当前目录下的所有修改：

```
git add .
```

这里：

```
.
```

表示当前目录。

所以：

```
git add .
```

基本可以理解成：

> 把当前项目中这些修改都加入暂存区。

实际开发中这个命令非常常见。

但是需要注意：

> `git add .` 很方便，但最好先看一眼 `git status`。

否则可能把一些你本来不想提交的东西也一起暂存了。

------

### 3. 再看一次 `git status`

执行：

```
git status
```

这一次可能看到：

```
Changes to be committed:
    modified: UserController.java
```

关键词是：

```
to be committed
```

也就是：

> 已经准备进入下一次 Commit。

所以 `git status` 其实是 Git 中非常重要的命令。

你可以把它理解成：

> **“我现在到底处于什么状态？”**

以后如果你搞不清 Git 现在到底发生了什么，第一反应通常就应该是：

```
git status
```

------

### 4. 创建 Commit

确认没有问题以后：

```
git commit -m "完成用户查询接口"
```

这里：

```
-m
```

后面跟的是：

> Commit message，提交说明。

例如：

```
git commit -m "完成用户查询接口"
```

就是告诉 Git：

> 这个版本主要做了什么。

最终你的历史可能变成：

```
Commit 1
初始化项目

↓

Commit 2
完成用户查询接口
```

到这里，一次标准的本地 Git 开发流程就完成了。

也就是教学计划里要求你掌握的：

```
写代码
↓
git add
↓
git commit
```

第五阶段：项目能力教学计划.mdMD

------

## 四、Commit 以后，我们得到了什么

### 1. 查看历史：`git log`

执行：

```
git log
```

可能看到类似：

```
commit a13cd8...
Author: Zhang Hao
Date: ...

    完成用户查询接口

commit 91ac42...
Author: Zhang Hao
Date: ...

    初始化项目
```

这就是项目历史。

每个 Commit 都有一个类似：

```
a13cd8...
```

的编号。

它叫：

```
Commit Hash
```

你暂时不需要研究 Hash 算法。

只需要知道：

> Git 使用它唯一标识某一次 Commit。

以后你可能会说：

```
我要看看 a13cd8 这个版本
```

或者：

```
我要恢复到 a13cd8 之前
```

Git 就知道你指的是哪个历史节点。

------

### 2. 更常用的简洁历史

相比直接：

```
git log
```

实际学习和日常使用中你可能更喜欢：

```
git log --oneline
```

例如：

```
a13cd8f 完成用户查询接口
91ac42e 初始化项目
```

这样非常清楚。

以后项目历史很多时也更方便看。

------

### 3. Commit 最重要的价值并不是“记录”

真正重要的是：

> **你有了回头路。**

例如：

```
Commit A
项目可以正常运行

↓

Commit B
增加用户查询功能

↓

你继续修改了一大堆代码

↓

项目彻底坏了
```

至少你知道：

```
Commit B
```

是一个可靠状态。

所以 Git 带来的开发安全感其实来自：

```
我可以大胆修改

因为我知道之前的正常版本还在
```

这也是为什么 Git 对程序员如此重要。

------

## 五、恢复版本到底是什么意思

恢复是 Git 中比较容易误操作的部分，所以这一课我们先把**思想**理解清楚，不急着背很多命令。

### 1. 第一种情况：代码改坏了，但还没 Commit

例如原来：

```
public User getUser() {
    ...
}
```

你乱改了几十行。

发现：

```
越改越坏。
```

但幸运的是：

```
还没有 Commit。
```

这时候你的需求可能只是：

> 我不要这些修改了，恢复到最近一次 Commit 的状态。

现代 Git 可以使用：

```
git restore 文件名
```

例如：

```
git restore UserController.java
```

意思大致就是：

> 丢掉这个文件当前尚未提交的修改，让它回到版本库中记录的状态。

注意：

**这个操作会丢失当前修改。**

所以不要看到 `restore` 就随便执行。

------

### 2. 第二种情况：已经 Commit，但后来发现版本有问题

情况变成：

```
Commit A
正常

↓

Commit B
正常

↓

Commit C
有 Bug
```

那么你的问题已经不是：

```
丢掉工作区修改
```

而是：

> **如何处理已经进入历史的 Commit。**

Git 有不同办法，例如：

```
reset
revert
```

但它们适用的场景不同，而且涉及是否改写历史。

这一课不用急着深入。

你现在只需要建立一个区别：

```
未提交的修改
```

和：

```
已经 Commit 的历史
```

不是一回事。

以后恢复时必须先问：

> 我到底想恢复哪一层？

------

## 六、把这一课放回 Spring Boot 项目里

你以后使用 Codex 协助开发 Spring Boot 项目时，我尤其建议你养成一个习惯。

例如你准备让 Codex：

```
重构用户登录模块
```

在此之前先保证当前代码是一个干净、正常的 Commit：

```
git status
```

确认没问题以后：

```
git add .
git commit -m "登录模块重构前"
```

然后再让 Codex 修改。

如果修改结果很好：

```
git add .
git commit -m "重构登录模块"
```

于是历史变成：

```
登录模块重构前
↓
重构登录模块
```

如果 Codex 一口气改了十几个文件，最后项目完全跑不起来，你至少知道：

> 修改之前的可靠版本在哪里。

这个能力对你的学习目标尤其重要。

因为你的目标本来就不是以后所有代码都亲手逐行编写，而是：

> **能够看懂、判断、调试和维护 AI 协助完成的项目。**

在这种开发模式下，Git 反而更加重要。

AI 可以很快修改大量代码，而 Git 负责保证：

```
这些修改可追踪
可以比较
可以撤销
不会把原来能工作的项目彻底弄丢
```

------

## 七、这一课你真正应该掌握的内容

不要求你现在背很多 Git 命令。

这一课最重要的是脑子里形成下面这张图：

```
          修改代码
             ↓
          工作区
             ↓
          git add
             ↓
          暂存区
             ↓
         git commit
             ↓
          本地仓库
             ↓
          历史版本
```

以及理解三个核心动作：

```
git status
```

看：

> 我现在有哪些修改、哪些已经暂存。

```
git add
```

表示：

> 选择哪些修改进入下一次 Commit。

```
git commit
```

表示：

> 创建一个正式的历史版本节点。

再加两个辅助能力：

```
git log --oneline
```

查看提交历史。

```
git restore 文件名
```

在尚未 Commit 的情况下，放弃某个文件当前的修改。

如果这套逻辑已经理解，第1课的核心目标就已经完成了。下一课才会在这个基础上进一步进入 **GitHub、远程仓库、push、pull、branch 和多人协作**。





# 第2课：Git（项目协作）

上一课我们解决的是 Git 的本地版本管理：工作区、暂存区、本地仓库，以及 `git add`、`git commit` 的基本流程。

这一课开始进入“项目协作”的视角：**GitHub、远程仓库、push、pull、branch、merge，以及为什么开发时不能所有人都直接改 main。** 这正是你的第五阶段教学计划中第2课的内容。

------

## 一、本地 Git 和 GitHub 到底是什么关系

### 1. Git 不是 GitHub

这是最先要分清的一点。

Git 是：

> 一个版本控制工具。

它可以完全在你的电脑本地使用。

比如你在本地执行：

```
git init
git add .
git commit -m "完成用户查询接口"
```

哪怕你没有联网，没有 GitHub 账号，这些操作依然能正常完成。

因为这些 Commit 都保存在：

```
本地仓库
```

而 GitHub 是：

> 一个托管 Git 仓库的平台。

它的作用可以粗略理解为：

```
把你的 Git 仓库放到互联网上
```

所以可以这样区分：

```
Git
=
版本控制工具

GitHub
=
托管 Git 仓库的网站 / 平台
```

类似地，国内常见的 Gitee 也是同一类东西。

------

### 2. 为什么还需要远程仓库

如果只有本地仓库：

```
你的电脑
└── user-system
    └── Git 仓库
```

你当然也可以正常开发。

但问题是：

第一，别人拿不到你的代码。

第二，如果电脑硬盘损坏，本地 Git 历史也可能一起丢失。

第三，多人协作非常麻烦。

于是我们会增加一个：

> 远程仓库（Remote Repository）

例如：

```
你的电脑
本地仓库
    │
    │ 网络
    ↓
GitHub
远程仓库
```

此时项目就出现了两个仓库：

```
本地仓库
+
远程仓库
```

注意，这两个仓库不是同一个东西。

你的代码不会因为 Commit 了，就自动跑到 GitHub。

------

## 二、push 和 pull：本地与远程之间的数据流动

### 1. `git push`：把本地提交推送到远程

假设你的本地历史现在是：

```
Commit A
初始化项目

↓

Commit B
完成用户查询接口
```

而 GitHub 上只有：

```
Commit A
初始化项目
```

也就是说：

```
本地比远程多一个 Commit B
```

这时候执行：

```
git push
```

本质上就是：

> 把本地新增的 Commit 推送到远程仓库。

然后两边就一致了：

```
本地：
A → B

远程：
A → B
```

所以：

```
commit
```

和：

```
push
```

千万不要混为一谈。

完整流程应该是：

```
修改代码
↓
git add
↓
git commit
↓
本地仓库产生新版本
↓
git push
↓
GitHub 上的远程仓库得到这个版本
```

------

### 2. Commit 不等于上传

这是一个很常见的误区。

执行：

```
git commit -m "完成登录接口"
```

以后，代码只是进入：

```
本地仓库
```

GitHub 上不会自动出现。

必须再：

```
git push
```

才会同步出去。

所以以后你看到：

```
我已经 commit 了，为什么 GitHub 没变化？
```

答案通常就是：

> 还没有 push。

------

### 3. `git pull`：把远程的新内容拉到本地

多人开发时，另一个方向同样重要。

假设你和同学一起开发。

你的本地：

```
A → B
```

但你的同学完成了一个新功能，并 push 到 GitHub：

```
远程：
A → B → C
```

这时你本地还不知道 C 的存在。

你执行：

```
git pull
```

就会把远程的新内容拿下来。

于是变成：

```
本地：
A → B → C
```

所以最简单地记：

```
push

本地 → 远程
pull

远程 → 本地
```

------

### 4. 一个非常典型的开发流程

假设早上你开始工作。

第一件事：

```
git pull
```

为什么？

因为昨天晚上可能有队友提交了新代码。

先把自己的本地项目更新到最新。

然后你开发功能：

```
写代码
↓
git add .
↓
git commit
```

完成以后：

```
git push
```

所以团队开发中很常见的节奏就是：

```
开始工作
↓
pull

开发
↓
add
↓
commit

完成工作
↓
push
```

当然，真实项目会因为分支、代码审查等机制更复杂，但现在先掌握这个基本逻辑。

------

## 三、branch：为什么不能所有人都直接改 main

这是这一课最重要的新概念。

### 1. main 是什么

一个 Git 项目通常会有一个主要分支：

```
main
```

以前很多项目也叫：

```
master
```

现在新项目一般更多使用 `main`。

你可以先把：

```
main
```

理解成：

> 项目的主线版本。

例如：

```
Commit A
↓
Commit B
↓
Commit C
```

main 指向当前最新的 C：

```
A → B → C
        ↑
       main
```

这个“指向”背后的机制现在不用深入。

你只需要知道：

> main 表示项目目前的主开发主线。

------

### 2. 为什么不能直接一直在 main 上开发

假设现在项目的 main 是稳定的：

```
main

用户查询   √
用户新增   √
用户删除   √
```

现在你准备开发：

```
登录功能
```

但登录功能可能要改：

```
UserController
UserService
JWT工具类
拦截器
配置类
```

你一开发就是两天。

开发到一半时：

```
项目暂时跑不起来
```

如果你直接在 main 上开发，那么 main 就会一直处于：

```
半成品
不稳定
甚至无法启动
```

这就很危险。

尤其多人协作时更明显。

假设：

```
张三：开发登录功能
李四：开发订单功能
王五：修复用户模块 Bug
```

三个人全在 main 上修改：

```
main
```

整个项目会非常混乱。

------

### 3. 分支的思想

Git 的解决办法就是：

> 分支。

例如当前：

```
A → B → C
        ↑
       main
```

你从 C 创建一个新分支：

```
feature-login
```

于是：

```
             feature-login
             ↓
A → B → C
        ↑
       main
```

刚创建的时候：

```
main
```

和：

```
feature-login
```

指向同一个版本。

然后你切换到：

```
feature-login
```

开始开发：

```
A → B → C
        ↑
       main
             \
              D → E
                  ↑
          feature-login
```

其中：

```
D
E
```

都是登录功能相关 Commit。

但是 main 依然停留在：

```
C
```

也就是说：

> 你的开发不会影响主线。

这就是 branch 最大的价值。

------

## 四、如何创建和切换分支

### 1. 查看当前分支

执行：

```
git branch
```

可能看到：

```
* main
```

其中：

```
*
```

表示：

> 你当前正在 main 分支。

------

### 2. 创建新分支

比如：

```
git branch feature-login
```

这表示创建：

```
feature-login
```

分支。

但是注意：

> 创建以后，你还没有切过去。

如果再次：

```
git branch
```

可能看到：

```
  feature-login
* main
```

当前仍然是 main。

------

### 3. 切换分支

可以使用：

```
git switch feature-login
```

之后：

```
git branch
```

变成：

```
* feature-login
  main
```

说明你已经进入：

```
feature-login
```

以后 Commit 就会提交到这个分支。

------

### 4. 更常用的一步完成

实际开发时，通常会直接：

```
git switch -c feature-login
```

含义是：

```
创建 feature-login
+
立即切换过去
```

于是：

```
main
↓
创建新分支
↓
feature-login
↓
开始开发
```

这非常常用。

------

## 五、分支开发到底是什么感觉

假设：

```
main
```

现在是：

```
项目 V1.0
```

你执行：

```
git switch -c feature-login
```

然后开发。

修改：

```
LoginController
UserService
JwtUtils
```

接着：

```
git add .
git commit -m "完成登录接口"
```

这时候：

```
main
```

仍然是原来的稳定状态。

而：

```
feature-login
```

已经多出了你的代码。

你甚至可以：

```
git switch main
```

切回 main。

这时候你刚写的登录代码可能“消失”了。

不要慌。

它并没有真的消失。

只是：

```
登录代码属于 feature-login
```

你切回 main 以后，Git 会把工作区恢复成 main 对应的状态。

再次：

```
git switch feature-login
```

登录代码又回来了。

这就是 Git 分支一个很关键的体验：

> **不同分支可以代表项目的不同开发状态。**

------

## 六、merge：把开发成果合回主线

### 1. 功能开发完成以后怎么办

假设：

```
main
```

是：

```
A → B → C
```

你的登录分支已经完成：

```
A → B → C
         \
          D → E
```

而且已经测试通过。

现在希望：

> 把登录功能正式加入 main。

这就叫：

```
merge
```

也就是：

> 合并分支。

------

### 2. 基本思想

你先切回 main：

```
git switch main
```

然后：

```
git merge feature-login
```

意思就是：

> 把 feature-login 的开发成果合并进当前 main。

最终可能变成：

```
A → B → C → D → E
                  ↑
                 main
```

现在 main 就包含登录功能了。

这就是一次最基本的分支开发闭环：

```
main
↓
创建功能分支
↓
feature-login
↓
开发
↓
commit
↓
测试
↓
回到 main
↓
merge
```

------

### 3. 为什么项目开发更安全了

对比一下。

不用分支：

```
main
↓
直接疯狂修改
↓
项目坏了
```

使用分支：

```
main
稳定
│
├── feature-login
│   开发登录
│
├── feature-order
│   开发订单
│
└── bugfix-user
    修复用户Bug
```

每个人都有自己的开发空间。

main 可以尽量保持稳定。

这就是为什么你的教学计划里特别写了：

> 不能一直在 main 开发。第五阶段：项目能力教学计划.mdMD

------

## 七、多人协作时实际发生什么

我们模拟一个很简单的场景。

你和同学一起开发 Spring Boot 后台。

当前远程仓库：

```
main
```

是：

```
用户管理系统基础版本
```

你负责：

```
登录
```

同学负责：

```
注册
```

于是你：

```
git switch -c feature-login
```

同学：

```
git switch -c feature-register
```

你们各自开发。

你：

```
feature-login
↓
完成登录
↓
commit
↓
push
```

同学：

```
feature-register
↓
完成注册
↓
commit
↓
push
```

GitHub 上可能就会有：

```
main
feature-login
feature-register
```

这三个分支。

最后再逐步把两个功能：

```
feature-login
feature-register
```

合并进：

```
main
```

这就是多人协作最基本的结构。

------

## 八、冲突是什么

这里先认识，不深入处理。

假设你和同学同时修改：

```
UserService.java
```

你把：

```
return user;
```

改成：

```
return userDTO;
```

而你的同学在同一位置改成：

```
return result;
```

Git 在合并时就会遇到问题：

> 这两份修改不能自动判断到底应该保留哪一个。

这就叫：

```
Merge Conflict
```

也就是：

> 合并冲突。

Git 并不是坏了。

它是在告诉你：

> “这个地方我不知道该选谁，你自己决定。”

真实开发中出现冲突非常正常。

IDEA 通常也会提供图形化界面，让你选择：

```
保留你的版本
保留对方版本
两边都保留
手动修改
```

现在只需要知道冲突是什么，不需要专门学习复杂冲突解决。

------

## 九、把 GitHub 加进上一课的完整流程

上一课我们只有：

```
工作区
↓
git add
↓
暂存区
↓
git commit
↓
本地仓库
```

现在可以继续补全：

```
工作区
    ↓
git add
    ↓
暂存区
    ↓
git commit
    ↓
本地仓库
    ↓
git push
    ↓
远程仓库 GitHub
```

反过来：

```
GitHub
远程仓库
    ↓
git pull
    ↓
本地项目更新
```

再加入分支：

```
main
↓
创建 feature 分支
↓
开发
↓
add
↓
commit
↓
push
↓
测试通过
↓
merge 回 main
```

这已经非常接近真实项目的基本 Git 工作流了。

------

## 十、你使用 Codex 时尤其推荐的 Git 习惯

你的情况和传统“纯手写代码学习”还有一点区别。

以后你很可能经常做：

```
我：
让 Codex 帮我修改登录模块

Codex：
一次修改 12 个文件
```

这种情况下，分支非常有价值。

比如：

```
git switch -c codex-login-refactor
```

然后再让 Codex 重构。

如果结果很好：

```
git add .
git commit -m "重构登录模块"
```

最后合并回 main。

如果结果完全不可用：

```
main 完全没受影响
```

你甚至可以直接放弃这个分支。

所以对于 AI 辅助开发，可以形成一个很实用的习惯：

```
小修改
↓
直接 commit

大修改 / 风险修改
↓
新建 branch
↓
让 AI 修改
↓
测试
↓
满意再 merge
```

这样比“让 Codex 直接在主项目里随便改”安全得多。

------

## 十一、这一课真正需要记住什么

这一课不用背很多命令，但下面几个概念必须分清。

```
本地仓库
```

在你电脑上的 Git 仓库。

```
远程仓库
```

GitHub 上的仓库。

```
push
本地 → 远程
pull
远程 → 本地
branch
```

创建独立开发路线，不直接污染 main。

```
merge
```

把某个分支的成果合回另一个分支。

最后形成一个完整直觉：

```
pull 最新代码
↓
创建功能分支
↓
写代码
↓
git add
↓
git commit
↓
git push
↓
功能完成并测试
↓
merge
```

这就是第2课真正想让你建立的“Git 项目协作”思维。

下一课会进入这个模块的最后一课：**Postman**。到那时重点会从“管理代码”转向“如何独立测试 Spring Boot 的 GET、POST、PUT、DELETE 接口”。



# 第3课：Postman

前两课解决的是“代码怎么管理”。这一课开始解决另一个非常实际的问题：

> **后端接口写完以后，怎么验证它到底能不能正常工作？**

按照你的教学计划，这一课主要学习 Postman 中的 `GET`、`POST`、`PUT`、`DELETE`，以及 Header、Body、JSON 和环境变量的基本认识。目标是做到：**不依赖浏览器，也能独立测试 Spring Boot 接口。** 

------

## 一、为什么后端开发离不开 Postman

### 1. 浏览器只能很好地测试一部分接口

你前面学 Spring Boot 时，应该已经见过这种接口：

```
@GetMapping("/users")
public List<User> list() {
    return userService.list();
}
```

你可以直接在浏览器访问：

```
http://localhost:8080/users
```

浏览器会向服务器发送：

```
GET /users
```

所以测试 GET 接口非常方便。

问题是，真正的后端接口远远不只有 GET。

比如：

```
@PostMapping("/users")
```

表示新增用户。

```
@PutMapping("/users/{id}")
```

表示修改用户。

```
@DeleteMapping("/users/{id}")
```

表示删除用户。

这些请求，普通浏览器地址栏就不方便直接发送了。

更麻烦的是，我们经常还需要发送：

```
请求参数
JSON
Header
Token
```

于是需要一个专门的：

> **HTTP 接口测试工具。**

Postman 就是最常见的一类工具。

------

### 2. Postman 本质上是在“假装前端”

假设未来你的项目结构是：

```
Vue 前端
   ↓ HTTP
Spring Boot 后端
   ↓
MySQL
```

前端可能发送：

```
POST /users
```

并携带：

```
{
  "username": "zhanghao",
  "age": 23
}
```

后端接收以后：

```
Controller
↓
Service
↓
Mapper
↓
数据库
```

但是现在有一个问题：

> 后端已经写好了，前端还没写好怎么办？

难道必须等 Vue 页面做完才能测试？

当然不需要。

我们可以让 Postman：

```
Postman
   ↓ HTTP
Spring Boot 后端
```

也就是说，Postman 临时扮演了前端。

它可以帮你人为构造 HTTP 请求：

```
请求方法
URL
参数
Header
Body
```

然后把请求发给 Spring Boot。

所以以后你写完一个接口，第一件事情通常不是：

> “去做一个页面看看能不能用。”

而是：

> “先用 Postman 把接口测通。”

------

## 二、先认识 Postman 的核心界面逻辑

虽然 Postman 界面版本会变化，但你真正需要关注的东西基本不会变。

一条请求最重要的结构可以看成：

```
请求方法 + URL + 请求数据
```

例如：

```
POST
http://localhost:8080/users
```

请求数据：

```
{
  "username": "zhanghao",
  "age": 23
}
```

在 Postman 里，你主要会操作：

```
Method
URL
Params
Headers
Body
Send
```

然后查看下面返回的：

```
Status Code
Response Body
Response Headers
响应时间
```

你现阶段甚至不用记 Postman 有多少高级功能。

只要会把一条 HTTP 请求完整构造出来即可。

------

## 三、GET：测试查询接口

### 1. 最简单的 GET

假设 Spring Boot 有：

```
@GetMapping("/users")
public List<User> list() {
    return userService.list();
}
```

在 Postman 中：

```
Method：GET
URL：http://localhost:8080/users
```

然后点击：

```
Send
```

服务器可能返回：

```
[
  {
    "id": 1,
    "username": "zhanghao"
  },
  {
    "id": 2,
    "username": "tom"
  }
]
```

这时候你至少应该观察两个东西：

```
状态码
响应数据
```

如果：

```
200 OK
```

通常说明请求成功。

------

### 2. GET 携带 `@RequestParam`

比如：

```
@GetMapping("/users")
public User getUser(@RequestParam Long id) {
    ...
}
```

对应请求：

```
GET /users?id=1
```

你可以直接写 URL：

```
http://localhost:8080/users?id=1
```

也可以使用 Postman 的：

```
Params
```

填写：

```
KEY     VALUE
id      1
```

Postman 最后其实仍然会拼成：

```
/users?id=1
```

所以这里没有新的 Web 知识。

Postman 只是用图形化方式帮你构造之前学过的请求。

------

### 3. GET 携带 `@PathVariable`

比如：

```
@GetMapping("/users/{id}")
public User getUser(@PathVariable Long id) {
    ...
}
```

那请求就是：

```
GET /users/1
```

Postman 中直接：

```
GET
http://localhost:8080/users/1
```

即可。

所以你前面学过的：

```
@RequestParam
@PathVariable
```

现在终于进入真正的接口测试场景。

------

## 四、POST 与 JSON Body

POST 是这一课最需要熟练的，因为实际项目中非常常见。

### 1. 一个新增用户接口

例如：

```
@PostMapping("/users")
public void add(@RequestBody User user) {
    userService.add(user);
}
```

这里的：

```
@RequestBody
```

意味着 Spring Boot 希望从：

> HTTP 请求体

中读取数据。

假设 User 有：

```
username
password
age
```

那么前端可能发送：

```
{
  "username": "zhanghao",
  "password": "123456",
  "age": 23
}
```

Postman 也可以完全模拟这条请求。

------

### 2. 在 Postman 中发送 JSON

配置：

```
Method：

POST
```

URL：

```
http://localhost:8080/users
```

然后进入：

```
Body
```

选择：

```
raw
```

再选择：

```
JSON
```

输入：

```
{
  "username": "zhanghao",
  "password": "123456",
  "age": 23
}
```

最后：

```
Send
```

这就是一个非常标准的 Spring Boot 接口测试。

完整过程实际上是：

```
Postman
↓
发送 HTTP POST
↓
Body 中携带 JSON
↓
Spring Boot
↓
@RequestBody
↓
Java User 对象
```

这应该和你前面 Spring Boot 阶段学过的内容联系起来。

------

## 五、Header 到底是干什么的

### 1. Header 是请求的附加信息

HTTP 请求并不只有 URL 和 Body。

它还可以包含：

```
Headers
```

例如：

```
Content-Type: application/json
```

它是在告诉服务器：

> 我这次 Body 里面发送的是 JSON。

所以：

```
{
  "username": "zhanghao"
}
```

本身只是一些文本。

服务器怎么知道你想把它当成 JSON？

一个重要依据就是：

```
Content-Type: application/json
```

------

### 2. Postman 通常会自动帮你设置

如果你在：

```
Body
→ raw
→ JSON
```

中填写数据，Postman 通常会自动加入：

```
Content-Type: application/json
```

所以你可能以前没主动设置过它，接口依然能工作。

但需要知道：

> Postman 不是在“施魔法”，它只是帮你把正确的 HTTP Header 自动补上了。

------

### 3. 以后 Token 也经常放在 Header

比如以后学习登录认证之后，你可能会看到：

```
Authorization: Bearer xxxxx
```

也就是说：

```
Header
```

除了描述请求内容，还经常承载：

```
身份信息
Token
客户端信息
```

第9课学习 JWT 时，你会再次遇到它。

这一课只需要先知道 Header 在哪里、是什么即可。

------

## 六、PUT 和 DELETE

这两个和 POST 并没有本质上的“Postman 操作难度”。

### 1. PUT：修改数据

例如：

```
@PutMapping("/users/{id}")
public void update(
        @PathVariable Long id,
        @RequestBody User user) {
    ...
}
```

那么：

```
Method：

PUT
```

URL：

```
http://localhost:8080/users/1
```

Body：

```
{
  "username": "newName",
  "age": 24
}
```

发送以后表达的意思就是：

> 修改 ID 为 1 的用户。

这里实际上同时使用了：

```
PathVariable
+
JSON Body
```

------

### 2. DELETE：删除数据

例如：

```
@DeleteMapping("/users/{id}")
public void delete(@PathVariable Long id) {
    ...
}
```

Postman：

```
DELETE
http://localhost:8080/users/1
```

然后：

```
Send
```

就可以模拟删除操作。

于是一个很典型的 CRUD 接口可以对应：

```
查询用户
GET /users/1

新增用户
POST /users

修改用户
PUT /users/1

删除用户
DELETE /users/1
```

Postman 最重要的意义之一，就是让你可以非常方便地测试这整套 CRUD。

------

## 七、不要只看“有没有返回数据”

这是使用 Postman 时需要尽早养成的习惯。

### 1. 首先看 HTTP 状态码

请求之后可能出现：

```
200 OK
```

通常表示成功。

也可能：

```
404 Not Found
```

通常说明：

> 请求路径没有找到对应资源。

例如 Controller 写的是：

```
@GetMapping("/user")
```

你请求：

```
/users
```

就可能出现 404。

还可能出现：

```
500 Internal Server Error
```

这意味着：

> 请求确实进入服务器了，但服务器执行过程中发生异常。

例如：

```
NullPointerException
SQL异常
业务代码异常
```

以后第6课专门训练 Bug 排查时：

```
404
500
SQL执行失败
```

都会成为重点案例。

现在只需要养成：

> **Postman 请求失败时，先看状态码。**

------

### 2. 再看 Response Body

例如后端返回：

```
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "zhanghao"
  }
}
```

你就要检查：

```
code 对不对
message 对不对
data 对不对
```

而不是看到：

```
200
```

就认为功能一定正确。

比如接口返回 200，但：

```
{
  "data": null
}
```

那业务逻辑可能仍然有问题。

所以接口验证一般要同时看：

```
HTTP 状态
+
返回数据
+
数据库是否真的发生预期变化
```

例如 DELETE 返回成功之后，你最好再查询一次数据库或发送 GET：

> 用户真的被删除了吗？

这才叫“验证功能”。

------

## 八、一个完整的 CRUD 测试过程

现在我们把它串起来。

假设你刚写完一个用户管理模块。

### 1. 先查询

```
GET
/users
```

确认当前数据。

比如：

```
[]
```

------

### 2. 新增

```
POST
/users
```

Body：

```
{
  "username": "zhanghao",
  "age": 23
}
```

成功。

------

### 3. 再查询

```
GET
/users
```

结果变成：

```
[
  {
    "id": 1,
    "username": "zhanghao",
    "age": 23
  }
]
```

于是你能确认：

> 新增真的成功了。

------

### 4. 修改

```
PUT
/users/1
```

Body：

```
{
  "username": "zhanghao",
  "age": 24
}
```

然后再次 GET。

确认：

```
age
```

真的从：

```
23
```

变成：

```
24
```

------

### 5. 删除

```
DELETE
/users/1
```

最后再次 GET：

```
[]
```

这才形成一个完整测试闭环：

```
新增
↓
查询验证

修改
↓
查询验证

删除
↓
查询验证
```

不要养成：

```
点一下 Send
↓
看见 200
↓
测试结束
```

这样的习惯。

------

## 九、环境变量：先认识即可

教学计划对这一部分的要求只是“认识”，所以我们不展开太深。第五阶段：项目能力教学计划.mdMD

假设你有很多接口：

```
http://localhost:8080/users
http://localhost:8080/orders
http://localhost:8080/products
http://localhost:8080/login
```

每个都写：

```
http://localhost:8080
```

很重复。

而且以后部署服务器后，地址可能变成：

```
https://api.example.com
```

难道几十个请求一个个改？

Postman 可以定义一个变量，例如：

```
baseUrl
=
http://localhost:8080
```

然后请求写成：

```
{{baseUrl}}/users
{{baseUrl}}/orders
{{baseUrl}}/login
```

以后只修改：

```
baseUrl
```

即可。

所以环境变量本质上就是：

> **把经常变化、重复使用的值抽出来统一管理。**

你现在知道它为什么存在就够了。

------

## 十、Postman 和 Swagger / 前端测试有什么区别

以后你可能还会遇到 Swagger、Knife4j 之类的工具，所以现在顺便建立边界。

Postman 是：

> 独立的 HTTP 客户端。

只要知道：

```
请求方法
URL
参数格式
```

它就能测试接口。

而前端页面是：

```
用户点按钮
↓
Vue / JavaScript
↓
发 HTTP 请求
↓
Spring Boot
```

因此，如果页面点击“注册”失败，我们需要判断：

```
到底是前端写错了？
还是后端接口坏了？
```

很实用的方法就是：

```
用 Postman 单独请求后端
```

如果 Postman 可以成功：

```
后端接口大概率正常
```

那么问题可能在：

```
前端请求
参数
URL
跨域
```

如果 Postman 本身都失败：

```
先查后端
```

这就是为什么 Postman 不只是“一个发请求的软件”，它还是非常重要的：

> **问题隔离工具。**

------

## 十一、这一课真正要形成的能力

你不需要记住 Postman 的所有按钮。

你应该能够看到一个 Controller：

```
@PostMapping("/users")
public void add(@RequestBody User user)
```

然后自己立刻知道：

```
Postman：

POST
/users

Body
raw
JSON
```

看到：

```
@GetMapping("/users/{id}")
public User get(@PathVariable Long id)
```

立刻知道：

```
GET
/users/1
```

看到：

```
@GetMapping("/users")
public User get(@RequestParam Long id)
```

知道：

```
GET
/users?id=1
```

也就是说，你已经可以完成：

```
阅读 Controller
↓
判断 HTTP 请求格式
↓
在 Postman 构造请求
↓
Send
↓
查看状态码和响应
↓
验证数据库 / 后续查询结果
```

到了这里，第3课的核心目标就完成了，也意味着第五阶段的第一个模块——**开发工具**——已经结束。

下一课开始进入这个阶段非常关键的第二模块：**调试能力**。第4课会正式学习 IDEA 的 **Debug：断点、Step Over、Step Into、Resume、变量观察和调用栈**，开始从“看到报错”进阶到“亲自追踪程序到底在哪里出了问题”。



# 第4课：Debug

从这一课开始进入第五阶段的第二模块——**调试能力**。按照你的教学计划，这一模块是整个第五阶段最重要的部分，而第4课首先学习 IDEA 的 Debug：断点、Step Over、Step Into、Resume、变量观察和调用栈。目标是逐步摆脱只靠 `System.out.println()` 找 Bug 的方式。

这一课真正需要建立的能力是：

> **程序出问题时，不再只是盯着报错猜，而是让程序停下来，亲眼观察它到底执行到了哪里、变量现在是什么值。**

------

## 一、Debug 到底是在做什么

### 1. 普通运行的问题：程序跑得太快

假设有这样一段代码：

```
public int calculatePrice(int price, int count) {
    int total = price * count;
    int discount = 100;
    int result = total - discount;
    return result;
}
```

你直接运行程序时，计算机会迅速执行：

```
price * count
↓
total - discount
↓
return
```

整个过程可能连一毫秒都不到。

如果最终结果不对，比如：

```
你预期：200
实际：100
```

你只知道：

> 最后错了。

但你并不知道中间哪一步开始出问题。

Debug 做的事情就是：

```
让程序执行到某个位置
↓
暂停
↓
查看此刻变量
↓
再执行一行
↓
再查看变量
```

所以 Debug 可以理解成：

> **把原本高速运行的程序变成“逐步播放”。**

有点像视频：

```
正常 Run
=
直接播放
```

而：

```
Debug
=
暂停 + 单帧播放 + 查看当前状态
```

------

### 2. Debug 不等于“自动帮你找 Bug”

这点很重要。

IDEA 不会因为你点了：

```
Debug
```

就告诉你：

> “第37行就是 Bug。”

它真正提供的是：

> **观察程序实际运行过程的能力。**

最终仍然由你判断：

```
这个变量为什么是 null？

为什么这里进入了 if？

为什么 SQL 参数变成了 0？

为什么代码根本没进入这个方法？
```

所以 Debug 的核心价值不是：

```
IDEA 帮你修代码
```

而是：

```
减少猜测
↓
获得证据
↓
定位问题
```

------

## 二、断点：让程序停在哪里

### 1. 什么是断点

假设 Controller：

```
@GetMapping("/users/{id}")
public User getUser(@PathVariable Long id) {
    User user = userService.getById(id);
    return user;
}
```

你怀疑：

```
userService.getById(id)
```

有问题。

可以在这一行左边的 IDEA 行号区域点击一下。

通常会出现一个红点：

```
●
```

这就是：

> **Breakpoint，断点。**

断点的意思不是：

> “这行有 Bug。”

而是告诉 Debugger：

> **程序执行到这里的时候先别往下跑，停下来让我看看。**

------

### 2. Run 和 Debug 的区别

平时启动 Spring Boot，你可能点击：

```
▶ Run
```

如果想让断点生效，需要使用：

```
🐞 Debug
```

假设程序启动以后，你在 Postman 请求：

```
GET /users/1
```

程序开始执行：

```
请求
↓
DispatcherServlet
↓
Controller
↓
getUser()
```

执行到断点：

```
User user = userService.getById(id);
```

程序暂停。

此时请求在 Postman 那边可能一直显示：

```
Sending...
```

这是正常的。

因为服务器并没有崩溃。

它只是被你人为暂停了。

------

### 3. 断点应该打在哪里

初学者很容易：

> 整个项目到处打断点。

其实没必要。

Debug 的基本思想是：

> **根据问题缩小范围。**

例如：

```
GET /users/1 返回错误数据
```

那么第一步可以在：

```
@GetMapping("/users/{id}")
```

对应的 Controller 方法中打断点。

如果发现：

```
id = 1
```

说明请求参数没问题。

继续进入 Service。

如果 Service 中数据也正常：

```
user = 正确用户
```

那再去检查：

```
返回值转换
JSON 序列化
其他逻辑
```

所以 Debug 和以后第6课 Bug 排查的核心思想其实是一致的：

```
先确定问题范围
↓
再逐渐缩小
```

------

## 三、程序停下来以后怎么看

假设代码：

```
public User getById(Long id) {
    User user = userMapper.selectById(id);

    if (user.getStatus() == 0) {
        user.setUsername("disabled");
    }

    return user;
}
```

你在：

```
User user = userMapper.selectById(id);
```

下面打断点。

程序暂停后，IDEA 会进入 Debug 界面。

这一课重点看三个东西：

```
当前代码位置
Variables
Call Stack
```

------

### 1. 当前执行位置

IDEA 通常会把当前即将执行的代码高亮。

例如停在：

```
if (user.getStatus() == 0) {
```

意味着：

> 上面的代码已经执行完了，但当前这一行还正在等待继续执行。

这点以后非常重要。

如果你看到程序停在：

```
User user = userMapper.selectById(id);
```

通常说明：

> 这一行此刻还没有完全执行结束。

所以如果你想查看：

```
user
```

到底是什么，就要先让这行执行完。

这就需要接下来讲的 Step Over。

------

### 2. Variables：变量观察

Debug 最有价值的区域之一就是：

```
Variables
```

例如当前方法：

```
public User getById(Long id)
```

IDEA 可能显示：

```
id = 1

user =
    id = 1
    username = "zhanghao"
    status = 0
```

这非常重要。

平时你只能通过代码想象：

```
user 应该是什么？
```

而 Debug 可以直接告诉你：

> **程序实际运行时它到底是什么。**

“我觉得它应该是 1”和“Debugger 告诉我它确实是 1”是完全不同的。

这就是调试时最重要的思维转变：

> 不猜变量值，看变量值。

------

## 四、Step Over：一行一行往下走

### 1. Step Over 是最常用的操作

IDEA 中通常叫：

```
Step Over
```

常见快捷键：

```
F8
```

它的作用可以理解为：

> **执行当前这一行，然后停在下一行。**

例如：

```
int a = 10;
int b = 20;
int c = a + b;
return c;
```

程序停在：

```
int b = 20;
```

按一次 Step Over：

```
int b = 20;
```

执行完成。

程序停到：

```
int c = a + b;
```

此时 Variables：

```
a = 10
b = 20
```

再按一次：

```
c = 30
```

然后停在：

```
return c;
```

所以 Step Over 就像：

```
执行一行
↓
停
↓
看看变量
↓
再执行一行
```

这是 Debug 时最常使用的操作。

------

### 2. 为什么叫“Over”

假设当前行是：

```
User user = userService.getById(id);
```

这里调用了：

```
getById()
```

如果使用 Step Over：

```
不会进去观察 getById 内部
```

而是：

```
把整个 getById() 当成一整步执行
↓
得到结果
↓
停在下一行
```

例如：

```
User user = userService.getById(id);
System.out.println(user);
```

Step Over 后直接停到：

```
System.out.println(user);
```

这就是：

> **跨过这个方法调用。**

------

## 五、Step Into：进入一个方法里面看

### 1. 什么时候 Step Over 不够

还是：

```
User user = userService.getById(id);
```

你 Step Over 后发现：

```
user = null
```

现在你已经得到一个重要线索：

```
Controller 本身没出问题

真正的问题可能出在：
userService.getById(id)
```

于是你不想直接跨过去了。

你想知道：

> `getById()` 里面到底发生了什么？

这时候使用：

```
Step Into
```

常见快捷键：

```
F7
```

------

### 2. Step Into 会进入方法内部

例如：

```
User user = userService.getById(id);
```

使用 Step Into。

程序可能跳到：

```
public User getById(Long id) {
    return userMapper.selectById(id);
}
```

然后你继续观察：

```
id 是多少？
```

如果：

```
id = 1
```

正常。

再 Step Into：

```
userMapper.selectById(id)
```

继续向内部追踪。

于是一个请求可能被你一路追进去：

```
Controller
↓
Service
↓
Mapper
```

这就是 Debug 特别适合 Spring Boot 项目的地方。

因为 Spring Boot 本身就是分层结构。

------

### 3. 不要什么方法都 Step Into

这一点非常重要。

假设：

```
String name = user.getUsername();
```

如果你疯狂 Step Into，你可能进入：

```
Java 标准库
Spring 内部
代理对象
框架源码
```

然后出现几十层你完全没必要看的代码。

所以正确思路是：

```
我怀疑这个方法内部有问题
↓
Step Into
```

而不是：

```
看到方法调用
↓
无脑 Step Into
```

你目前学习 Spring Boot 的目标也不是研究 Spring 源码。

所以大量框架内部代码：

> **可以直接略过。**

------

## 六、Resume：我已经不想一行行走了

假设有循环：

```
for (int i = 0; i < 1000; i++) {
    process(i);
}
```

如果你一直 Step Over：

```
i = 0
i = 1
i = 2
i = 3
……
```

显然不现实。

这时候有一个重要操作：

```
Resume Program
```

常见快捷键：

```
F9
```

它表示：

> **不用逐行执行了，继续正常运行，直到遇到下一个断点。**

例如有两个断点：

```
Controller        ●

↓

Service

↓

Mapper            ●
```

程序在 Controller 停住。

你检查以后觉得：

```
Controller 没问题
```

直接 Resume。

程序就继续高速执行，直到：

```
Mapper 的断点
```

再次暂停。

所以三个操作最简单的区别：

```
Step Over
执行一行，不进入方法
Step Into
进入当前调用的方法
Resume
不逐行看了，继续运行到下一个断点
```

这三个基本就够支撑你大部分初级 Debug。

------

## 七、调用栈：程序是怎么走到这里的

这是 Debug 中稍微抽象一点，但非常重要的概念。

### 1. 什么叫调用栈

假设请求经过：

```
UserController.getUser()
↓
UserService.getById()
↓
UserMapper.selectById()
```

现在程序停在：

```
UserMapper.selectById()
```

你可能会问：

> 我是怎么跑到这里来的？

Debugger 会提供：

```
Call Stack
```

也就是调用栈。

你可能看到大致这样的结构：

```
UserMapper.selectById()
UserService.getById()
UserController.getUser()
...
```

它表示：

```
Controller 调用了 Service

Service 又调用了 Mapper

现在程序正在 Mapper
```

------

### 2. 为什么调用栈非常有价值

假设某个方法：

```
saveUser()
```

在很多地方都会调用：

```
注册用户
修改用户
管理员创建用户
导入用户
```

现在程序在：

```
saveUser()
```

里报错。

你只看当前代码，可能不知道：

> 这一次到底是谁调用我的？

看 Call Stack 就可以知道：

```
AdminController
↓
UserService
↓
saveUser
```

那么你就知道：

> 原来这次是管理员创建用户流程触发的。

所以调用栈回答的是：

> **程序为什么会执行到当前这里？**

变量窗口回答：

> **程序现在的数据是什么？**

两者结合起来，就是 Debug 最重要的信息。

------

## 八、用一个 Spring Boot Bug 完整走一遍

现在模拟一个真正值得 Debug 的问题。

接口：

```
@GetMapping("/users/{id}")
public String getUsername(@PathVariable Long id) {
    return userService.getUsername(id);
}
```

Service：

```
public String getUsername(Long id) {
    User user = userMapper.selectById(id);
    return user.getUsername();
}
```

Postman 请求：

```
GET /users/10
```

结果：

```
500 Internal Server Error
```

控制台：

```
NullPointerException
```

你现在当然可以直接猜：

> `user` 可能是 null。

但我们使用 Debug 来确认。

------

### 1. Controller 打断点

在：

```
return userService.getUsername(id);
```

打断点。

Debug 启动。

Postman：

```
GET /users/10
```

程序暂停。

看 Variables：

```
id = 10
```

说明：

```
@PathVariable
```

正常。

------

### 2. Step Into Service

对：

```
userService.getUsername(id)
```

使用 Step Into。

进入：

```
public String getUsername(Long id) {
```

Variables：

```
id = 10
```

还是正常。

------

### 3. Step Over 查询

程序来到：

```
User user = userMapper.selectById(id);
```

按 Step Over。

执行完成后看：

```
user = null
```

现在 Bug 已经非常明确了。

下一行：

```
return user.getUsername();
```

相当于：

```
null.getUsername();
```

当然就会：

```
NullPointerException
```

于是我们真正定位到了：

```
请求 id = 10
↓
数据库没有对应用户
↓
Mapper 返回 null
↓
代码没有判断 null
↓
调用 user.getUsername()
↓
NullPointerException
```

这和仅仅看到：

```
NullPointerException
```

是两个完全不同的层次。

你已经知道：

> **错误是怎么一步步产生的。**

------

## 九、Debug 和 `System.out.println()` 的区别

你的教学计划特意强调了：

```
System.out.println()
```

因为初学者特别喜欢这样找 Bug。第五阶段：项目能力教学计划.mdMD

比如：

```
System.out.println("进入getUser");

System.out.println("id = " + id);

User user = userMapper.selectById(id);

System.out.println("user = " + user);
```

然后运行：

```
进入getUser
id = 10
user = null
```

这种办法并不是“错误”的。

实际上有时确实有用。

问题是，如果项目复杂起来，你可能会变成：

```
System.out.println("111");

System.out.println("222");

System.out.println("到这里了吗");

System.out.println(user);

System.out.println(result);
```

然后控制台全是：

```
111
222
333
test
???
```

查完 Bug 还得把这些代码删掉。

Debug 则可以直接：

```
暂停
↓
看 id
↓
看 user
↓
看 result
```

完全不需要修改业务代码。

所以对于本地开发阶段：

> **能使用 Debug 时，通常优先 Debug，而不是到处塞 println。**

不过下一课你会学日志，到时候会发现：

```
Debug
```

和：

```
Logger
```

并不是谁取代谁。

它们解决的场景不同。

------

## 十、你实际调 Bug 时应该怎么用

不要把 Debug 理解成：

```
出 Bug
↓
从第一行开始 F8
↓
一直按到最后
```

这效率很低。

更合理的流程是：

```
发现问题
↓
先判断问题大概在哪一层
↓
在可疑位置打断点
↓
复现问题
↓
检查变量
↓
Step Over / Step Into
↓
进一步缩小范围
```

例如：

```
接口返回 500
```

可以先判断：

```
请求有没有进入 Controller？
```

Controller 打断点。

如果没有停住：

```
问题可能在请求路径、映射等更前面
```

如果停住：

```
Controller 已经成功进入
```

继续检查：

```
参数正常吗？
```

正常：

```
进入 Service
```

再正常：

```
检查 Mapper
```

于是 Debug 的本质就是不断问：

> **到这里为止正常吗？**

如果：

```
Controller 正常
Service 前半段正常
Mapper 返回异常
```

那么范围就从：

```
整个项目
```

缩小到了：

```
一次数据库查询
```

这就是调试能力真正有价值的地方。

------

## 十一、这一课需要真正掌握的内容

现在你至少要能区分这四个动作：

```
Breakpoint
=
程序到这里暂停
Step Over
=
执行当前行，不进入方法内部
Step Into
=
进入当前调用的方法内部
Resume
=
继续正常执行到下一个断点
```

以及知道 Debug 时重点观察：

```
Variables
=
当前变量到底是什么值
Call Stack
=
程序是通过什么调用路径来到这里的
```

最终形成这样的调试思路：

```
出现 Bug
↓
找到可疑区域
↓
设置断点
↓
Debug 启动
↓
复现问题
↓
观察变量
↓
逐步执行
↓
确认从哪一步开始出现异常
↓
定位真正原因
```

如果你已经理解到这个程度，第4课的目标就达到了。

下一课会学习 **日志 Logger**。届时会解决一个 Debug 无法解决的现实问题：

> **你自己的电脑可以打断点，但项目部署到服务器以后出了问题，你不可能随时暂停服务器程序逐行调试。**

这时候就必须依靠日志来还原程序发生了什么。



# 第5课：日志

上一课我们学了 Debug：让程序暂停下来，查看变量、调用路径，一步步定位问题。

但真实项目里存在一个很现实的问题：

> **线上服务器不能随便打断点。**

用户正在使用系统时，你不可能把整个程序暂停下来慢慢 `Step Over`。因此，部署后的项目出了问题，最重要的排查依据之一就是——**日志（Log）**。

按照你的教学计划，这一课主要学习 `Logger`、`INFO`、`WARN`、`ERROR`、`DEBUG`，并理解为什么线上项目主要依靠日志排查问题。第五阶段：项目能力教学计划.mdMD

------

## 一、日志到底是什么

### 1. 日志就是“程序运行时留下的记录”

假设用户调用登录接口：

```
POST /login
```

程序内部经历：

```
收到登录请求
↓
查询用户
↓
校验密码
↓
生成 Token
↓
返回结果
```

如果我们在关键位置留下记录：

```
收到登录请求：username=zhanghao

查询到用户：id=10

密码校验成功

Token生成成功
```

那么以后即使程序已经执行结束，我们依然能够根据这些记录知道：

> 当时到底发生了什么。

这就是日志最核心的价值。

你可以把它理解成：

> **程序自己的“运行日记”。**

------

### 2. 日志和报错不是一回事

很多初学者会觉得：

> 日志是不是就是程序报错以后控制台出现的一堆红字？

不是。

日志可以记录任何你认为重要的信息：

```
程序启动成功
用户登录成功
订单创建完成
库存不足
数据库连接失败
```

其中只有一部分属于错误。

所以：

```
日志
```

是一个大概念。

而：

```
错误日志
```

只是其中的一部分。

------

## 二、为什么不能一直用 `System.out.println()`

### 1. `println` 确实也能打印信息

比如：

```
System.out.println("开始查询用户");
```

运行时你也能看到：

```
开始查询用户
```

所以在非常简单的学习代码中，`println` 并不是完全不能用。

问题是，真正项目需要的信息管理远比这复杂。

------

### 2. `println` 最大的问题：它不知道“信息的重要程度”

假设项目运行时输出：

```
程序启动成功
用户进入首页
用户名为空
数据库连接失败
缓存更新完成
订单金额异常
```

如果全部都是普通打印：

```
System.out.println(...)
```

你很难快速知道：

```
哪些只是正常运行信息？

哪些值得注意？

哪些已经是严重错误？
```

而日志系统可以给信息分等级。

例如：

```
INFO   用户登录成功
WARN   用户连续输错密码5次
ERROR  数据库连接失败
```

一眼就能区分重要程度。

------

### 3. 日志还可以统一管理

真正的日志系统通常还可以做到：

```
输出到控制台
```

或者：

```
写入日志文件
```

甚至：

```
只记录 ERROR
开发环境记录 DEBUG
生产环境关闭 DEBUG
```

这些都不是 `System.out.println()` 擅长做的事情。

因此可以把两者简单区分为：

```
System.out.println
=
临时打印
Logger
=
正式的项目运行记录
```

------

## 三、Spring Boot 中怎么写日志

### 1. 最基本的 Logger

在 Spring Boot 中，常见写法类似：

```
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class UserService {

    private static final Logger log =
            LoggerFactory.getLogger(UserService.class);
}
```

然后：

```
log.info("开始查询用户");
```

就可以输出日志。

你现在不需要深入研究：

```
SLF4J
Logback
日志门面
日志实现
```

这些底层关系。

对于你当前目标，只需要知道：

> Spring Boot 项目通常通过 Logger 来记录日志。

------

### 2. Lombok 中常见的 `@Slf4j`

以后你看 Codex 生成的 Spring Boot 项目，很可能看到：

```
@Slf4j
@Service
public class UserService {

}
```

然后代码里直接：

```
log.info("查询用户");
```

你可能会疑惑：

> `log` 从哪来的？代码里明明没定义。

因为：

```
@Slf4j
```

是 Lombok 提供的注解。

它帮你自动生成了一个日志对象，大致相当于：

```
private static final Logger log =
        LoggerFactory.getLogger(UserService.class);
```

所以以后看到：

```
@Slf4j
```

你可以直接理解成：

> **这个类准备使用 `log.xxx()` 记录日志。**

------

## 四、四个最重要的日志等级

这一课真正需要掌握的是：

```
DEBUG
INFO
WARN
ERROR
```

它们表达的是信息严重程度。

可以从轻到重大致理解成：

```
DEBUG
↓
INFO
↓
WARN
↓
ERROR
```

------

### 1. DEBUG：给开发者看的细节

例如：

```
log.debug("当前查询参数 id={}", id);
```

可能输出：

```
当前查询参数 id=10
```

这类信息通常用于：

> 调试程序内部执行过程。

比如：

```
查询参数是什么
计算中间结果是什么
进入了哪个分支
```

这些信息对于开发者很有价值，但普通情况下又太琐碎。

所以：

```
DEBUG
```

经常用于：

> **开发阶段的详细排查信息。**

线上环境通常不会无脑把所有 DEBUG 全开，否则日志量可能非常大。

------

### 2. INFO：正常的重要运行信息

`INFO` 是项目中非常常见的级别。

例如：

```
log.info("用户登录成功，userId={}", userId);
```

或者：

```
log.info("订单创建成功，orderId={}", orderId);
```

这些都不是异常。

它们只是告诉你：

> 系统发生了一件值得记录的正常事件。

所以可以理解为：

```
INFO
=
正常，但值得知道
```

例如：

```
应用启动完成
用户登录
任务执行完成
订单创建
```

------

### 3. WARN：有问题，但程序还能继续

例如：

```
log.warn("用户不存在，id={}", id);
```

或者：

```
log.warn("库存不足，productId={}", productId);
```

它表示：

> 出现了值得关注的异常情况，但不一定导致程序崩溃。

比如用户请求：

```
GET /users/999999
```

数据库没有这个用户。

程序可以正常返回：

```
用户不存在
```

这不一定是系统错误。

但可能值得记录：

```
WARN
```

所以可以理解成：

```
WARN
=
不太正常，需要注意，但系统还能处理
```

------

### 4. ERROR：真正的错误

例如：

```
log.error("查询用户失败", e);
```

这通常意味着：

> 程序执行出现了明显异常。

比如：

```
数据库连接失败
SQL执行异常
程序抛出未预期异常
```

这类情况通常需要开发者关注。

所以：

```
ERROR
=
程序发生严重异常
```

------

### 5. 用一个登录场景区分四个等级

假设登录流程：

```
用户提交登录请求
↓
查询数据库
↓
验证密码
↓
生成 Token
```

可能这样记录：

```
log.debug("收到登录请求，username={}", username);
```

表示：

> 开发调试细节。

如果登录成功：

```
log.info("用户登录成功，userId={}", userId);
```

属于正常重要事件。

如果用户名不存在：

```
log.warn("登录失败，用户不存在，username={}", username);
```

属于值得注意，但程序仍然正常处理。

如果数据库突然断了：

```
log.error("登录查询数据库失败", e);
```

这才属于系统错误。

于是：

```
DEBUG
开发细节

INFO
正常事件

WARN
异常情况，但可以处理

ERROR
真正的系统错误
```

这个区分掌握就够了。

------

## 五、日志参数为什么经常写成 `{}`

你以后非常常见到：

```
log.info("查询用户，id={}", id);
```

而不是：

```
log.info("查询用户，id=" + id);
```

这里：

```
{}
```

相当于占位符。

例如：

```
Long id = 10L;

log.info("查询用户，id={}", id);
```

最终输出：

```
查询用户，id=10
```

多个变量也可以：

```
log.info("用户登录，username={}, userId={}", username, userId);
```

输出：

```
用户登录，username=zhanghao, userId=10
```

以后你看项目时，只需要知道：

> `{}` 会被后面的参数依次替换。

------

## 六、日志应该写在哪里

### 1. 不是每一行代码都要写日志

这种代码显然没有必要：

```
log.info("进入第一行");

int a = 10;

log.info("执行第二行");

int b = 20;

log.info("执行第三行");
```

日志不是为了：

> 把整个程序每一行都打印一遍。

否则日志本身就会变成噪声。

------

### 2. 更适合记录“关键节点”

例如登录：

```
收到登录请求
↓
查询用户
↓
认证成功
↓
Token生成
```

没有必要每一步都写。

可以只记录：

```
log.info("用户登录成功，userId={}", userId);
```

失败的重要情况：

```
log.warn("用户登录失败，username={}", username);
```

异常：

```
log.error("登录过程异常", e);
```

核心原则是：

> **以后出了问题，这条日志是否能帮助你还原现场？**

如果答案是完全不能，那它很可能没有必要。

------

## 七、为什么线上项目离不开日志

这正是这一课最重要的现实场景。

### 1. 本地开发可以 Debug

假设你在 IDEA 本地运行：

```
localhost:8080
```

接口出现 500。

你可以：

```
打断点
↓
重新请求
↓
Step Into
↓
看 Variables
```

非常方便。

------

### 2. 线上环境通常做不到

假设项目部署：

```
服务器
↓
正在同时服务100个用户
```

晚上 22:31，有用户报告：

> “我刚才提交订单失败了。”

你第二天早上看到这个问题。

这时候你不可能：

```
回到昨天22:31
```

重新给那一次请求打断点。

那次程序早就执行完了。

怎么办？

看日志。

例如：

```
22:31:04 INFO  开始创建订单 userId=10
22:31:04 INFO  商品查询成功 productId=100
22:31:04 ERROR 创建订单失败
java.sql.SQLIntegrityConstraintViolationException ...
```

你就可以还原：

```
请求确实进入服务器
↓
商品查询成功
↓
创建订单数据库操作失败
```

于是问题范围立即缩小。

这就是：

> **日志是程序运行后的“历史证据”。**

而 Debug 主要观察：

> **程序现在正在发生什么。**

------

## 八、怎么看一段真实日志

假设控制台出现：

```
2026-09-02 15:30:21 INFO  UserController
查询用户，id=10

2026-09-02 15:30:21 DEBUG UserService
开始调用数据库查询

2026-09-02 15:30:21 WARN  UserService
用户不存在，id=10
```

你应该能读出：

```
15:30:21
```

是发生时间。

```
INFO / DEBUG / WARN
```

是日志等级。

```
UserController
UserService
```

告诉你日志来自哪个类。

最后：

```
查询用户，id=10
用户不存在，id=10
```

是开发者自己记录的信息。

于是你可以还原：

```
Controller 收到 id=10
↓
Service 开始查数据库
↓
最终没查到用户
```

这就是最基础的日志分析。

------

## 九、遇到 ERROR 时重点看什么

以后你看到类似：

```
ERROR ...
```

下面通常跟着很长一串内容：

```
java.lang.NullPointerException
    at com.example.service.UserService.getUser(UserService.java:32)
    at com.example.controller.UserController.getUser(UserController.java:25)
    ...
```

这部分叫：

> **异常堆栈（Stack Trace）**

你现在不用学习每一行怎么读。

先掌握一个非常实用的方法。

例如：

```
java.lang.NullPointerException
```

先告诉你：

> 什么异常。

然后：

```
UserService.java:32
```

告诉你：

> 异常发生在 UserService 第32行附近。

接下来可能看到：

```
UserController.java:25
```

说明：

> Controller 调用了这个 Service。

你应该能发现它和上一课 Debug 的：

```
Call Stack
```

其实非常像。

因为它们都在告诉你：

> 程序经过什么调用路径走到了错误位置。

区别是：

```
Debug Call Stack
=
程序暂停时现场查看
异常 Stack Trace
=
程序出错以后留下来的调用记录
```

这个联系很重要。

------

## 十、日志里不要随便记录敏感数据

这一点虽然教学计划没有专门展开，但实际项目必须形成基本意识。

比如登录接口绝对不应该：

```
log.info("登录 username={}, password={}", username, password);
```

因为这可能导致密码直接出现在日志文件：

```
username=zhanghao password=123456
```

日志通常会被长期保存，也可能有多人能够查看。

因此类似：

```
密码
Token完整值
身份证号码
银行卡信息
```

不应该随便完整打印。

例如可以只记录：

```
log.info("用户登录失败，username={}", username);
```

而不打印密码。

日志的目标是：

> 足够排查问题。

不是：

> 把所有数据全部写进去。

------

## 十一、Debug 和日志应该怎么配合

现在你已经学了两个排查工具。

### 1. Debug 更适合本地复现

例如：

```
我点击这个接口必然报错
```

那么：

```
断点
↓
复现
↓
看变量
↓
定位
```

效率很高。

------

### 2. 日志更适合还原已经发生的问题

例如：

```
昨天晚上服务器偶发500
```

你现在不一定能重新复现。

那就：

```
按照时间找日志
↓
找到 ERROR
↓
查看前后 INFO / WARN
↓
查看异常堆栈
↓
还原请求过程
```

------

### 3. 两者经常联合使用

真实开发中很常见：

```
用户报告问题
↓
先查日志
↓
判断问题可能出在 UserService
↓
尝试在本地复现
↓
对 UserService 打断点
↓
Debug进一步定位
```

所以不是：

```
学了日志
↓
Debug没用了
```

而是：

```
日志
帮助定位大概范围

+

Debug
帮助深入观察具体过程
```

------

## 十二、这一课真正需要掌握什么

看到：

```
@Slf4j
```

你应该知道：

> 这个类使用了日志功能，可以直接调用 `log.xxx()`。

看到：

```
log.debug(...)
```

理解成：

> 开发调试细节。

看到：

```
log.info(...)
```

理解成：

> 正常的重要运行信息。

看到：

```
log.warn(...)
```

理解成：

> 值得关注的异常情况，但系统可能仍能正常处理。

看到：

```
log.error(...)
```

理解成：

> 出现了真正需要关注的错误。

更重要的是建立这个排查思路：

```
线上出现问题
↓
确定发生时间
↓
查看附近日志
↓
优先找 WARN / ERROR
↓
阅读异常信息
↓
看调用栈
↓
结合前后日志还原发生过程
↓
定位问题范围
```

如果这一套思路已经理解，第5课就达到了目标。

下一课是这个模块的核心收尾——**第6课：排查 Bug**。它不会再主要介绍新工具，而是把前面学过的 Controller、数据库、Debug、日志结合起来，集中处理 `404`、`500`、空指针、SQL 执行失败、端口占用、Bean 找不到等典型故障。



# 第6课：排查 Bug

前两课分别学了 **Debug** 和 **日志**。这一课不再引入太多新工具，而是把前面的能力真正串起来：当 Spring Boot 项目出现问题时，应该怎么判断、怎么缩小范围、怎么定位。

你的教学计划中，这一课明确要求以案例为主，重点处理：

- 数据库连接失败
- 404
- 500
- 空指针
- SQL 执行失败
- 端口占用
- Bean 找不到

并且目标不是“老师告诉你答案”，而是培养你自己定位问题的能力。第五阶段：项目能力教学计划.mdMD

------

## 一、先建立一个最重要的排错框架

### 1. 不要一看到报错就乱改

初学阶段最常见的问题不是“不会修 Bug”，而是：

> 看到错误以后，没有判断问题属于哪一层，就开始到处改代码。

例如接口访问失败，你可能同时去检查：

```
Controller
Service
Mapper
application.yml
数据库
端口
依赖
```

这样效率很低。

更合理的方法是先问：

> **问题发生在哪一个阶段？**

对于一个典型 Spring Boot 请求：

```
Postman
↓
HTTP 请求
↓
Spring Boot 接收
↓
Controller
↓
Service
↓
Mapper
↓
数据库
↓
返回结果
```

排 Bug 的本质就是：

> 找到这条链路在哪一步断了。

------

### 2. 一个通用排查顺序

以后遇到接口问题，可以先按下面的思路走：

```
第一步：项目启动了吗？
↓
第二步：请求地址和端口对吗？
↓
第三步：请求进入 Controller 了吗？
↓
第四步：Controller 参数正常吗？
↓
第五步：Service 执行正常吗？
↓
第六步：Mapper / SQL 正常吗？
↓
第七步：数据库数据符合预期吗？
↓
第八步：返回结果正常吗？
```

不要上来就研究最深层的 SQL。

比如：

```
项目根本没启动
```

那你研究 Mapper 一小时也没有意义。

------

## 二、404：先判断“有没有找到接口”

### 1. 典型场景

你在 Postman 请求：

```
GET http://localhost:8080/users/1
```

结果：

```
404 Not Found
```

404 最核心的含义是：

> **请求没有找到对应的资源或映射。**

对于你现在的 Spring Boot 项目，优先想到的就是：

> Controller 路径没匹配上。

------

### 2. 先看 Controller

假设：

```
@RestController
@RequestMapping("/user")
public class UserController {

    @GetMapping("/{id}")
    public User getUser(@PathVariable Long id) {
        ...
    }
}
```

真正请求路径应该是：

```
/user/1
```

但你发送：

```
/users/1
```

当然就匹配不到。

所以 404 时第一反应应该是检查：

```
类上的 @RequestMapping
+
方法上的 @GetMapping / @PostMapping
```

拼起来到底是什么。

------

### 3. 再检查请求方法

比如：

```
@PostMapping("/users")
public void add(...) {
}
```

你却发送：

```
GET /users
```

请求方法不匹配，也会导致请求无法正常进入你预期的接口。

所以检查：

```
URL 对不对？
GET / POST / PUT / DELETE 对不对？
```

------

### 4. 怎么用 Debug 判断

最简单的方法：

在 Controller 方法第一行打断点。

例如：

```
@GetMapping("/{id}")
public User getUser(@PathVariable Long id) {
    // ● 断点
}
```

然后重新请求。

如果断点：

```
完全没有停
```

就可以判断：

> 问题发生在进入 Controller 之前。

这时候就不要继续检查 Service、Mapper。

优先看：

```
路径
请求方法
端口
Controller 是否被扫描
```

这就是“缩小范围”。

------

## 三、500：说明请求进来了，但程序内部炸了

### 1. 500 和 404 的区别

如果：

```
404
```

更像：

> “我不知道你要找哪个接口。”

而：

```
500 Internal Server Error
```

更像：

> “我知道你找哪个接口，但我执行的时候出问题了。”

所以 500 出现以后，你通常应该马上去看：

```
控制台日志 / ERROR / 异常堆栈
```

------

### 2. 示例

Postman：

```
GET /users/10
```

返回：

```
500
```

控制台：

```
java.lang.NullPointerException
    at com.example.service.UserService.getUsername(UserService.java:35)
```

这里已经给了两个极重要的信息：

```
NullPointerException
```

说明异常类型。

```
UserService.java:35
```

说明异常位置。

你甚至不用先猜 Controller。

直接去看：

```
UserService 第35行
```

------

### 3. 500 时不要只看 Postman

Postman 往往只告诉你：

```
500 Internal Server Error
```

这个信息不够。

真正详细的原因通常在：

```
Spring Boot 控制台
日志
异常堆栈
```

所以以后形成习惯：

```
Postman 看到 500
↓
马上切 IDEA 控制台
↓
找第一段关键 ERROR
↓
看异常类型和自己项目的代码行
```

------

## 四、空指针：最经典的 Java Bug

### 1. 为什么会空指针

例如：

```
User user = userMapper.selectById(id);

return user.getUsername();
```

如果数据库没有查到数据：

```
user = null
```

那么：

```
user.getUsername()
```

其实就是：

```
null.getUsername()
```

于是：

```
NullPointerException
```

------

### 2. 不要只想着“加一个非空判断”

很多人看到空指针以后马上改成：

```
if (user != null) {
    return user.getUsername();
}
```

这可能能避免报错，但排 Bug 时应该先问：

> **为什么 user 会是 null？**

可能是：

```
数据库确实没有这条记录
```

也可能是：

```
传进来的 id 错了
```

也可能是：

```
SQL 条件写错
```

或者：

```
Mapper 逻辑不对
```

所以正确思路不是：

```
看到 null
↓
立刻加 if
```

而是：

```
确认哪个变量是 null
↓
追踪这个变量从哪里来的
↓
判断它为什么会变成 null
```

------

### 3. Debug 怎么排

例如：

```
User user = userMapper.selectById(id);
return user.getUsername();
```

断点打在：

```
User user = ...
```

观察：

```
id = ?
```

如果：

```
id = 10
```

正常。

Step Over 后：

```
user = null
```

现在进一步查数据库：

```
SELECT * FROM user WHERE id = 10;
```

如果数据库本来就没有：

> 原因已经找到。

如果数据库明明有：

> 继续检查 Mapper / SQL。

------

## 五、SQL 执行失败：不要看到 SQL 报错就害怕

数据库错误看起来通常很长，但其实也可以分类。

### 1. SQL 语法错误

例如你写：

```
SELEC * FROM user;
```

少了一个 `T`。

日志可能出现：

```
SQLSyntaxErrorException
```

这种情况优先检查：

```
SQL 本身有没有拼错
字段名对不对
表名对不对
```

------

### 2. 字段不存在

例如数据库表：

```
user_name
```

你 SQL 写：

```
SELECT username FROM user;
```

日志可能告诉你：

```
Unknown column 'username'
```

这时候不用怀疑 Spring Boot。

因为异常已经非常明确：

> SQL 访问了数据库不存在的字段。

------

### 3. 数据约束错误

例如数据库：

```
username NOT NULL
```

你插入：

```
INSERT INTO user(username)
VALUES (NULL);
```

可能出现：

```
Column 'username' cannot be null
```

这说明：

> SQL 语法可能完全正确，但数据违反了数据库规则。

所以：

```
SQL执行失败
```

不等于：

```
SQL语法一定错了
```

可能是：

```
语法
字段
类型
约束
数据
```

中的任何一个。

------

### 4. 怎么排查 SQL

推荐顺序：

```
先看异常信息
↓
找到真正执行的 SQL
↓
看参数
↓
把 SQL 放到数据库工具里单独执行
```

这是非常实用的方法。

比如日志里：

```
SELECT * FROM user WHERE id = ?
```

参数：

```
id = 10
```

你可以直接在数据库客户端执行：

```
SELECT * FROM user WHERE id = 10;
```

如果数据库工具里都报错：

> 说明问题基本不在 Java。

如果数据库工具执行成功：

> 再回来检查 Java / Mapper 的映射与参数。

这就是“隔离问题”。

------

## 六、数据库连接失败：先看配置和数据库本身

### 1. 常见现象

Spring Boot 启动时直接报错：

```
Communications link failure
```

或者：

```
Access denied for user
```

或者：

```
Could not obtain JDBC Connection
```

这类问题通常不是：

```
Controller 写错了
```

而是：

> Spring Boot 根本没连接上数据库。

------

### 2. 第一类：数据库没启动

比如 MySQL 服务没有运行。

你的配置：

```
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo
```

Spring Boot 去连接：

```
localhost:3306
```

但那里根本没有 MySQL 在监听。

结果自然失败。

所以先确认：

> 数据库服务到底启动了吗？

------

### 3. 第二类：用户名密码错误

例如：

```
username: root
password: 123456
```

但数据库实际密码：

```
654321
```

日志可能：

```
Access denied for user 'root'
```

这种日志其实已经非常直白：

> 身份认证失败。

------

### 4. 第三类：库名或端口错误

例如：

```
url: jdbc:mysql://localhost:3306/user_db
```

但真实数据库叫：

```
users_db
```

或者 MySQL 实际跑在：

```
3307
```

你却连接：

```
3306
```

也会失败。

所以数据库连接失败可以先按这个顺序：

```
数据库服务启动了吗？
↓
IP / 主机名对吗？
↓
端口对吗？
↓
数据库名对吗？
↓
用户名密码对吗？
```

比一上来研究 Java 代码有效得多。

------

## 七、端口占用：项目甚至还没真正启动

### 1. 典型报错

你启动 Spring Boot：

```
Web server failed to start.
Port 8080 was already in use.
```

这句话已经基本把答案告诉你：

> 8080 被其他程序占用了。

最常见情况是：

```
你已经启动了一个 Spring Boot
↓
忘了停
↓
又启动第二个
```

于是：

```
第一个项目占用 8080
第二个项目也想要 8080
```

当然冲突。

------

### 2. 怎么处理

最简单的办法：

> 把之前运行的程序停止。

或者临时修改：

```
server:
  port: 8081
```

然后项目改用：

```
http://localhost:8081
```

这里最重要的排错意识是：

```
Port 8080 was already in use
```

已经是非常明确的信息。

不要看到大片红字以后就觉得：

> Spring Boot 出大问题了。

先抓关键句。

------

## 八、Bean 找不到：Spring 容器没有你要的对象

### 1. 典型场景

例如：

```
@Service
public class UserService {
}
```

Controller：

```
@RestController
public class UserController {

    private final UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }
}
```

正常情况下，Spring 会创建：

```
UserService Bean
```

然后注入。

但如果 Spring 找不到，启动时可能出现类似：

```
required a bean of type 'UserService' that could not be found
```

这个错误的核心就是：

> 你需要一个 UserService 对象，但 Spring 容器里没有。

------

### 2. 为什么可能没有 Bean

第一个常见原因：

忘了：

```
@Service
```

写成：

```
public class UserService {
}
```

那么 Spring 不知道：

> 这个类应该由我管理。

------

第二个常见原因：

包扫描不到。

比如启动类：

```
com.example.Application
```

而 Service 放在完全不相关的：

```
org.test.service
```

Spring Boot 默认扫描不到它。

------

第三个常见原因：

接口有多个实现或者配置存在歧义。

这个现在不用深入。

你只需要掌握基础判断：

```
Bean 找不到
↓
先确认这个类有没有被 Spring 管理
↓
@Service / @Component / @Repository 等
↓
再确认包扫描范围
```

------

## 九、一次完整的排查示范

现在模拟一个场景：

Postman：

```
GET /users/1
```

结果：

```
500
```

你应该怎么做？

### 1. 第一步：确认项目确实启动

控制台没有：

```
Application failed to start
```

而且：

```
Started XxxApplication
```

说明项目启动了。

排除：

```
端口占用
Bean 创建失败
数据库启动阶段错误
```

------

### 2. 第二步：看接口是否进入 Controller

Controller 打断点：

```
@GetMapping("/users/{id}")
public User get(@PathVariable Long id) {
```

重新请求。

断点停住。

说明：

```
URL 正确
HTTP 方法正确
Controller 映射正常
```

404 方向基本可以排除。

------

### 3. 第三步：检查参数

Variables：

```
id = 1
```

正常。

------

### 4. 第四步：进入 Service

Step Into：

```
User user = userMapper.selectById(id);
```

Step Over。

结果：

```
user = null
```

问题范围已经缩小到：

```
Mapper / 数据库
```

------

### 5. 第五步：查数据库

执行：

```
SELECT * FROM user WHERE id = 1;
```

结果：

```
没有数据
```

最终原因：

```
请求 id=1
↓
数据库不存在这个用户
↓
Mapper 返回 null
↓
Service 调用 null 的方法
↓
NullPointerException
↓
500
```

注意这里有一个非常关键的点：

> 真正的问题不是“500”。

`500` 只是最终表现。

也不是：

> “NullPointerException”这几个字本身。

真正的业务根因是：

> **代码没有正确处理“用户不存在”的情况。**

这就是从“看到错误”到“找到根因”的区别。

------

## 十、碰到 Bug 时应该先看什么

你可以把常见情况先形成一个简单映射：

| 现象                       | 第一反应                           |
| -------------------------- | ---------------------------------- |
| 项目启动失败               | 看启动日志最后的关键异常           |
| `Port 8080 already in use` | 端口占用                           |
| Bean could not be found    | Spring Bean / 注解 / 扫描          |
| 数据库连接失败             | 数据库服务、URL、端口、账号密码    |
| 404                        | URL、HTTP 方法、Controller 映射    |
| 500                        | 马上看后端异常日志                 |
| NullPointerException       | 谁是 null，为什么是 null           |
| SQLSyntaxErrorException    | SQL、表名、字段名                  |
| 返回结果不对但没报错       | Debug 看参数、中间变量、数据库结果 |

这个表不需要死背。

重点是逐渐形成：

> **不同现象，优先检查不同区域。**

------

## 十一、学会读报错的“第一关键行”

实际 Spring Boot 报错可能有几十甚至几百行。

初学者经常看到：

```
一大片红色
```

就从第一行看到最后一行，越看越乱。

更有效的思路是找关键内容，例如：

```
Caused by:
```

以及：

```
NullPointerException
Access denied
Port 8080 was already in use
Unknown column
Bean ... could not be found
```

再找自己项目的类：

```
com.example.service.UserService.java:35
```

而不是花大量时间研究：

```
org.springframework...
java.base...
tomcat...
```

这些框架堆栈。

对于你当前的目标：

> **先找到“异常是什么”和“自己代码在哪里触发”，通常比读懂完整调用栈重要。**

------

## 十二、这一课真正要形成的是一套思维

以后看到 Bug，不要：

```
报错
↓
慌
↓
复制整段错误给 AI
↓
直接照着 AI 改
```

尤其你以后会经常使用 Codex，这一点反而更重要。

更好的流程是：

```
出现问题
↓
先看现象
↓
判断属于启动、HTTP、业务还是数据库
↓
看日志
↓
找到异常类型
↓
找到自己项目代码位置
↓
必要时 Debug
↓
检查变量
↓
逐层缩小范围
↓
确认根因
↓
再修改代码
```

然后你当然可以让 Codex协助修复，但你至少应该能够判断：

> 它修改的地方是否和真正的根因有关。

这正是你学习这整个第五阶段的价值。

到这里，第二模块“调试能力”就结束了。你已经完成了 **Debug、日志和常见 Bug 排查** 三部分。下一课进入第三模块——**第7课：参数校验**，开始学习为什么后端不能相信用户传来的数据，以及 `@NotNull`、`@NotBlank`、`@Size`、`@Valid` 等参数校验机制。



# 第7课：参数校验

这一课开始进入第三模块——**项目规范**。

按照你的教学计划，这一课要解决的核心问题是：

> 后端不能默认相信用户传来的数据。

重点学习 `@NotNull`、`@NotBlank`、`@Size`、`@Valid`，并认识 `BindingResult`，最后理解一个注册接口为什么应该做自动参数校验。

------

## 一、为什么必须做参数校验

### 1. 前端已经校验了，后端还需要吗

假设注册页面要求：

```
用户名不能为空
密码至少 6 位
昵称最多 20 个字符
```

前端 Vue 完全可以先检查。

例如用户没填用户名，就不让点击提交。

但后端仍然必须校验。

原因很简单：

> 用户并不一定通过你的前端页面调用接口。

上一课你刚学过 Postman。

任何人都可以绕过页面，直接发送：

```
POST /users
```

然后提交：

```
{
  "username": "",
  "password": "1"
}
```

甚至：

```
{
  "username": null,
  "password": null
}
```

如果后端完全相信前端：

```
前端负责检查
↓
后端直接保存
```

那么只要绕过前端，校验就彻底失效。

所以要形成一个很重要的后端思维：

> **前端校验主要改善用户体验，后端校验才是真正的数据防线。**

------

### 2. 参数不合法会造成什么

假设接口：

```
@PostMapping("/users")
public void register(@RequestBody User user) {
    userService.save(user);
}
```

而 `User`：

```
public class User {
    private String username;
    private String password;
}
```

用户发送：

```
{
  "username": "",
  "password": ""
}
```

如果什么都不检查，数据可能直接进入：

```
Controller
↓
Service
↓
Mapper
↓
数据库
```

最终数据库里出现：

```
username = ""
password = ""
```

这种数据一旦进入系统，后面的业务逻辑会越来越麻烦。

所以更合理的是：

```
请求进入 Controller
↓
先校验
↓
合法
    → 执行业务
不合法
    → 直接拒绝
```

------

## 二、Bean Validation：把校验规则写在参数对象上

Spring Boot 项目里常见的做法，是使用校验注解。

例如：

```
public class RegisterRequest {

    @NotBlank
    private String username;

    @NotBlank
    private String password;
}
```

这里表达的思想非常直接：

> `username` 和 `password` 不允许是空白内容。

相比在 Controller 里写：

```
if (username == null || username.isBlank()) {
    ...
}

if (password == null || password.isBlank()) {
    ...
}
```

注解方式更加集中、清晰。

以后你看到一个 DTO：

```
public class RegisterRequest {

    @NotBlank
    private String username;

    @Size(min = 6, max = 20)
    private String password;
}
```

基本一眼就能知道：

```
username
不能为空白

password
长度 6～20
```

------

## 三、几个最常见的校验注解

### 1. `@NotNull`

```
@NotNull
private Long userId;
```

表示：

> 这个值不能是 `null`。

例如：

```
{
  "userId": null
}
```

不合法。

但这里有个细节：

`@NotNull` 只检查：

```
是不是 null
```

它并不关心字符串是不是空的。

例如：

```
@NotNull
private String username;
```

那么：

```
{
  "username": ""
}
```

其实不是 `null`。

所以单纯使用 `@NotNull`：

> 不能阻止空字符串。

------

### 2. `@NotBlank`

字符串字段更常见：

```
@NotBlank
private String username;
```

它不仅不允许：

```
null
```

也不允许：

```
""
```

以及只包含空格：

```
"   "
```

所以如果是：

```
用户名
密码
标题
昵称
```

这类要求“必须真正填写内容”的字符串，通常更适合：

```
@NotBlank
```

可以简单区分：

```
@NotNull
=
不能为 null

@NotBlank
=
字符串不能为 null、空字符串或全是空白
```

------

### 3. `@Size`

例如：

```
@Size(min = 6, max = 20)
private String password;
```

表示字符串长度必须：

```
>= 6
并且
<= 20
```

所以：

```
123
```

不合法。

```
123456
```

合法。

它也可以用于一些集合类型，不过你现阶段主要把它理解成：

> 控制字符串长度。

------

### 4. 校验注解通常组合使用

比如密码：

```
@NotBlank
@Size(min = 6, max = 20)
private String password;
```

这里有两个规则：

```
不能为空
+
长度 6～20
```

这种组合非常常见。

例如完整一点：

```
public class RegisterRequest {

    @NotBlank
    @Size(min = 3, max = 20)
    private String username;

    @NotBlank
    @Size(min = 6, max = 30)
    private String password;
}
```

可以直接读成：

```
username
必须填写
长度 3～20

password
必须填写
长度 6～30
```

------

## 四、只写注解还不够，还需要 `@Valid`

这是这一课最容易漏掉的地方。

### 1. DTO 上有规则

例如：

```
public class RegisterRequest {

    @NotBlank
    private String username;

    @Size(min = 6, max = 20)
    private String password;
}
```

然后 Controller：

```
@PostMapping("/register")
public void register(
        @RequestBody RegisterRequest request) {

    ...
}
```

你可能以为：

> 已经写了 `@NotBlank`，Spring 应该自动校验了。

但通常还需要在 Controller 参数上明确告诉 Spring：

```
@Valid
```

也就是：

```
@PostMapping("/register")
public void register(
        @Valid @RequestBody RegisterRequest request) {

    ...
}
```

这里：

```
@RequestBody
```

负责：

> 把 JSON 转成 Java 对象。

而：

```
@Valid
```

负责：

> 对这个对象执行校验规则。

这两个职责不要混淆。

------

### 2. 整个过程

Postman 发送：

```
{
  "username": "",
  "password": "123"
}
```

Spring 收到以后：

```
JSON
↓
@RequestBody
↓
RegisterRequest 对象
```

然后：

```
@Valid
↓
检查字段上的校验注解
```

发现：

```
username
违反 @NotBlank

password
违反 @Size(min = 6)
```

于是：

> 不再继续执行正常业务代码。

也就是说，这些非法参数会在进入 Service 之前被拦下来。

这正是我们想要的效果。

------

## 五、校验失败以后会发生什么

### 1. 默认行为

如果：

```
@Valid @RequestBody RegisterRequest request
```

校验失败，Spring 通常会抛出相应的参数校验异常。

最终客户端很可能收到：

```
400 Bad Request
```

这也很符合语义：

> 请求本身的数据有问题。

而不是：

```
500 Internal Server Error
```

因为：

```
用户名为空
```

不是服务器坏了。

而是：

> 用户提交了非法请求。

------

### 2. 可以给校验规则添加提示信息

例如：

```
@NotBlank(message = "用户名不能为空")
private String username;

@Size(
    min = 6,
    max = 20,
    message = "密码长度必须为6到20位"
)
private String password;
```

这样校验失败以后，我们就能获得更友好的错误信息：

```
用户名不能为空
```

而不是只得到一个非常生硬的系统异常。

真实项目通常会进一步配合：

```
全局异常处理
统一响应格式
```

把这些校验错误转换成统一 JSON。

比如：

```
{
  "code": 400,
  "message": "用户名不能为空"
}
```

不过这一课不需要把全局异常处理展开。

你只需要知道：

> 校验注解不仅定义规则，还可以定义失败提示。

------

## 六、为什么注册接口特别需要校验

假设一个注册接口需要：

```
username
password
nickname
```

我们可以先设计：

```
public class RegisterRequest {

    @NotBlank(message = "用户名不能为空")
    @Size(min = 3, max = 20,
          message = "用户名长度必须为3到20位")
    private String username;

    @NotBlank(message = "密码不能为空")
    @Size(min = 6, max = 30,
          message = "密码长度必须为6到30位")
    private String password;

    @Size(max = 20,
          message = "昵称不能超过20位")
    private String nickname;
}
```

然后：

```
@PostMapping("/register")
public void register(
        @Valid @RequestBody RegisterRequest request) {

    userService.register(request);
}
```

整个流程就是：

```
用户请求
↓
JSON 转 RegisterRequest
↓
自动参数校验
↓
不合法
    → 直接拒绝
↓
合法
    → UserService.register()
```

这里非常重要的一点是：

> Service 可以开始假设“基础格式校验已经通过”。

这样 Service 就不用反复写：

```
if (username == null) ...
if (password.length() < 6) ...
```

代码职责会更清楚。

------

## 七、参数校验和业务校验不是同一回事

这是这一课非常重要的边界。

### 1. 参数校验

例如：

```
username 是否为空
password 长度是否够
年龄是否符合数字范围
```

这些属于：

> 数据本身的格式是否合理。

适合使用：

```
@NotBlank
@Size
```

之类的注解。

------

### 2. 业务校验

例如注册时：

> 用户名是否已经被别人注册？

这个问题不能靠：

```
@NotBlank
```

解决。

因为：

```
"zhanghao"
```

格式完全合法。

但数据库里可能已经存在：

```
username = zhanghao
```

这就属于：

> 业务规则冲突。

需要 Service 查询数据库：

```
User existingUser =
        userMapper.selectByUsername(username);

if (existingUser != null) {
    // 用户名已经存在
}
```

所以要区分：

```
参数校验
=
这个输入本身长得合不合法

业务校验
=
这个输入放进当前业务环境中允不允许
```

例如：

```
password = "123"
```

属于参数校验失败。

而：

```
username = "tom"
但 tom 已经被注册
```

属于业务校验失败。

这两个概念以后非常常见。

------

## 八、为什么更推荐使用 DTO，而不是直接校验实体类

你以后可能看到这种代码：

```
public class User {

    @NotBlank
    private String username;

    @NotBlank
    private String password;
}
```

然后：

```
public void register(
        @Valid @RequestBody User user)
```

简单项目里确实可以这样做。

但项目稍微正规一点，经常会专门定义：

```
RegisterRequest
```

或者：

```
UserCreateDTO
```

例如：

```
public class RegisterRequest {

    @NotBlank
    private String username;

    @NotBlank
    @Size(min = 6, max = 20)
    private String password;
}
```

为什么？

因为：

```
数据库里的 User
```

和：

```
注册接口需要用户提交的数据
```

未必完全一样。

数据库 User 可能还有：

```
id
createTime
status
role
```

但注册时根本不应该让用户传：

```
role = ADMIN
```

所以实际项目更常见：

```
HTTP 请求
↓
RegisterRequest / DTO
↓
Service
↓
User 实体
↓
数据库
```

你现在只需要理解这个思想，不需要展开复杂 DTO 设计。

------

## 九、认识 `BindingResult`

你的教学计划对它要求只是“认识”，所以这一部分不深入。第五阶段：项目能力教学计划.mdMD

### 1. 它是干什么的

有一种写法：

```
@PostMapping("/register")
public void register(
        @Valid @RequestBody RegisterRequest request,
        BindingResult bindingResult) {

    if (bindingResult.hasErrors()) {
        ...
    }
}
```

`BindingResult` 可以让你：

> 在 Controller 里拿到参数校验的结果。

例如：

```
username 为空
password 太短
```

这些错误都能从里面获取。

------

### 2. 为什么现在不需要重点掌握

因为现代 Spring Boot 项目里，更常见的做法通常是：

```
@Valid 校验失败
↓
抛异常
↓
统一异常处理器
↓
转换成统一错误响应
```

而不是每个 Controller 都手动：

```
if (bindingResult.hasErrors()) {
    ...
}
```

否则几十个接口都会写重复代码。

所以你现阶段知道：

> `BindingResult` 可以手动获取校验结果。

就够了。

------

## 十、用 Postman 真正测试一次参数校验

这和第3课的 Postman 可以联系起来。

假设：

```
@PostMapping("/register")
public void register(
        @Valid @RequestBody RegisterRequest request) {

    ...
}
```

### 1. 正常数据

发送：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

校验：

```
通过
```

进入 Service。

------

### 2. username 为空

```
{
  "username": "",
  "password": "123456"
}
```

违反：

```
@NotBlank
```

应该被拒绝。

------

### 3. password 太短

```
{
  "username": "zhanghao",
  "password": "123"
}
```

违反：

```
@Size(min = 6)
```

应该被拒绝。

------

### 4. 字段直接不传

例如：

```
{
  "password": "123456"
}
```

转换成 Java 对象以后：

```
username = null
```

于是：

```
@NotBlank
```

仍然不能通过。

通过这种测试，你才能真正确认：

> 参数校验不是“代码看起来写了”，而是真的生效了。

------

## 十一、看到项目代码时应该怎么读

以后 Codex 生成这种代码：

```
@Data
public class LoginRequest {

    @NotBlank(message = "用户名不能为空")
    private String username;

    @NotBlank(message = "密码不能为空")
    private String password;
}
```

Controller：

```
@PostMapping("/login")
public Result login(
        @Valid @RequestBody LoginRequest request) {

    return userService.login(request);
}
```

你应该能直接读懂：

```
@Data
Lombok 帮助生成 getter/setter 等
@NotBlank
两个字段不能为空白
@RequestBody
请求 JSON → LoginRequest
@Valid
执行字段上的校验规则
校验通过
↓
才进入 userService.login()
```

这就是这一课最需要达到的阅读能力。

------

## 十二、这一课真正要掌握的内容

先记住最核心的四个：

```
@NotNull
不能是 null
@NotBlank
字符串不能为空白
@Size
限制长度
@Valid
触发对象上的参数校验
```

然后建立这套完整思路：

```
HTTP 请求
↓
@RequestBody
↓
DTO
↓
@Valid
↓
校验注解
↓
非法
    → 直接拒绝
合法
    → 进入 Service
```

以及一个非常重要的区别：

```
参数校验
=
格式是否合法

业务校验
=
业务规则是否允许
```

最后记住后端开发的一条基本原则：

> **不要因为“正常用户一般不会这样输入”，就认为不需要检查。**

真正可靠的后端应该默认：

```
任何来自外部的输入
↓
都需要验证
```

到这里，第7课的核心目标就完成了。

下一课是 **第8课：接口测试**。重点不再是“怎么用 Postman 发请求”，而是进一步建立测试思维：为什么**代码写完 ≠ 功能完成**，以及正常数据、异常数据、边界数据、重复数据应该怎么系统地测试。



# 第8课：接口测试

上一课我们讲的是**参数校验**：让后端能够主动拒绝不合法输入。

这一课继续往前一步。按照你的教学计划，重点不是再学 Postman 的按钮，而是建立一种开发习惯：

> **代码写完，不代表功能完成；只有经过验证，才能认为这个接口真的可用。**

这一课主要围绕正常数据、异常数据、边界数据、重复数据四类测试展开，并建立“开发 → 测试 → 修复”的思维。第五阶段：项目能力教学计划.mdMD

## 一、为什么“能跑”不等于“功能正确”

### 1. 最容易犯的错误：只测试一次成功情况

假设你写了一个注册接口：

```
@PostMapping("/register")
public Result register(
        @Valid @RequestBody RegisterRequest request) {

    return userService.register(request);
}
```

你用 Postman 发送：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

返回：

```
{
  "code": 200,
  "message": "注册成功"
}
```

数据库里也确实多了一条记录。

这时候很容易产生一种感觉：

> 接口写完了。

但实际上你只证明了一件事：

> **在这一组正常输入下，它能工作。**

你还不知道：

```
用户名为空会怎样？
密码只有5位会怎样？
用户名刚好3位呢？
重复注册同一个用户名呢？
传不存在的字段呢？
数据库异常时呢？
```

所以接口测试的核心不是：

> “点一下 Send，看看报不报错。”

而是：

> **主动设计不同输入，验证程序是否在各种情况下都表现正确。**

------

### 2. 测试既要验证“成功”，也要验证“失败得对”

一个接口可能本来就应该拒绝某些请求。

比如注册时：

```
{
  "username": "",
  "password": "123"
}
```

如果后端返回：

```
400
用户名不能为空
```

这不是接口坏了。

反而说明：

> 参数校验正确工作。

所以测试不能简单分成：

```
成功 = 好
失败 = Bug
```

更准确的是：

```
输入符合规则
→ 应该成功

输入不符合规则
→ 应该按照预期失败
```

我们验证的是：

> **实际结果是否符合设计预期。**

------

## 二、接口测试最重要的四类数据

### 1. 正常数据：先证明主流程能走通

这是最基本的测试。

假设注册要求：

```
用户名：3～20位
密码：6～30位
用户名不能重复
```

正常数据：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

预期：

```
注册成功
↓
数据库新增用户
↓
返回正确结果
```

这里不要只看：

```
HTTP 200
```

最好同时验证：

```
响应内容正确吗？
数据库真的新增了吗？
新增的数据正确吗？
```

例如数据库最终出现：

```
id = 10
username = zhanghao
password = ...
```

那主流程基本才算验证完成。

------

### 2. 异常数据：故意输入明显不合法的内容

例如：

```
{
  "username": "",
  "password": "123456"
}
```

预期：

```
注册失败
用户名不能为空
```

再比如：

```
{
  "username": "zhanghao",
  "password": "123"
}
```

预期：

```
密码长度不合法
```

这里其实正好验证了上一课的：

```
@NotBlank
@Size
@Valid
```

到底有没有真正生效。

如果你写了：

```
@Size(min = 6)
```

但：

```
password = "123"
```

居然还能注册成功，那说明：

> 代码虽然“写了校验”，实际功能却没有达到要求。

这就是测试的价值。

------

### 3. 边界数据：最容易暴露 Bug

边界测试是这一课非常重要的概念。

假设规则：

```
密码长度 6～20 位
```

很多人只会测试：

```
12345678
```

八位，正常。

但真正值得测试的是边界附近：

```
5位
6位
20位
21位
```

也就是：

```
边界值 - 1
边界值
边界值 + 1
```

例如：

```
5位 → 应该失败
6位 → 应该成功

20位 → 应该成功
21位 → 应该失败
```

为什么？

因为程序员特别容易写出这种错误：

本来要求：

```
长度 >= 6
```

结果误写成：

```
长度 > 6
```

那么：

```
7、8、10位
```

全部正常。

只有：

```
刚好6位
```

会暴露问题。

所以：

> **边界附近往往是 Bug 最容易藏身的地方。**

------

### 4. 重复数据：很多业务问题不是格式错误

假设第一次发送：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

注册成功。

然后你完全相同地再发一次：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

会发生什么？

这就是：

> 重复数据测试。

第二次请求的格式完全合法：

```
username 不为空
长度正确
password 长度正确
```

所以：

```
@NotBlank
@Size
```

都拦不住。

但业务规则可能要求：

> 用户名唯一。

因此应该进入 Service：

```
查询 username 是否存在
↓
已经存在
↓
拒绝注册
```

这也再次体现上一课的区别：

```
参数校验
≠
业务校验
```

接口测试需要把两者都覆盖。

------

## 三、用一个注册接口完整测试

假设现在接口要求：

```
username：3～20位，不能为空，不能重复
password：6～20位，不能为空
```

我们可以设计如下测试。

### 1. 正常情况

请求：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

预期：

```
成功
```

并检查数据库：

```
新增了一条 zhanghao
```

------

### 2. username 为空

```
{
  "username": "",
  "password": "123456"
}
```

预期：

```
失败
```

并且：

```
数据库不能新增数据
```

这一点很重要。

不能只看错误提示。

还要确保失败请求**没有产生副作用**。

------

### 3. password 太短

```
{
  "username": "testuser",
  "password": "12345"
}
```

五位。

预期：

```
失败
```

------

### 4. password 刚好6位

```
{
  "username": "testuser",
  "password": "123456"
}
```

预期：

```
成功
```

这就是边界测试。

------

### 5. password 刚好20位

预期：

```
成功
```

21位：

```
失败
```

------

### 6. 重复用户名

先注册：

```
testuser
```

成功。

再注册：

```
testuser
```

预期：

```
失败
用户名已存在
```

这时候你已经不再是：

> 随便用 Postman 点几个请求。

而是在有意识地设计：

```
正常
异常
边界
重复
```

四类测试。

------

## 四、CRUD 接口应该怎么测试

接口测试不只适用于注册。

对于典型 CRUD，也可以形成固定思路。

### 1. 查询接口

例如：

```
GET /users/{id}
```

正常数据：

```
查询一个确实存在的 id
```

例如：

```
/users/10
```

确认：

```
返回的确实是 id=10 的用户
```

异常数据：

```
/users/999999
```

如果用户不存在：

> 系统是否按照设计返回“用户不存在”？

而不是：

```
NullPointerException
500
```

------

### 2. 新增接口

例如：

```
POST /users
```

需要考虑：

```
正常新增
字段为空
长度超限
重复数据
```

并确认：

```
数据库实际新增结果
```

------

### 3. 修改接口

例如：

```
PUT /users/10
```

至少考虑：

```
修改存在的用户
修改不存在的用户
字段非法
修改后是否真的保存
```

例如发送：

```
{
  "nickname": "NewName"
}
```

接口返回成功以后，再：

```
GET /users/10
```

确认：

```
nickname 真正变成 NewName
```

------

### 4. 删除接口

例如：

```
DELETE /users/10
```

成功以后：

```
再 GET /users/10
```

应该查不到。

还可以测试：

```
再次 DELETE /users/10
```

系统怎么办？

是：

```
返回用户不存在
```

还是：

```
仍然返回成功
```

这取决于接口设计。

测试本身不替你决定业务规则。

测试要做的是：

> **确认实际行为符合约定。**

------

## 五、接口之间也要进行“连续测试”

有些 Bug 单独测试一个接口很难发现。

例如：

```
POST /users
```

新增成功。

单看它：

```
没问题
```

但接下来：

```
GET /users/{id}
```

却查不到刚刚新增的用户。

这时候说明：

> 两个接口之间没有形成正确闭环。

所以 CRUD 很适合按照业务流程测试：

```
新增
↓
查询
↓
修改
↓
再次查询
↓
删除
↓
再次查询
```

例如：

```
POST /users
↓
生成 id=10

GET /users/10
↓
确认新增成功

PUT /users/10
↓
修改 nickname

GET /users/10
↓
确认修改成功

DELETE /users/10
↓
删除

GET /users/10
↓
确认不存在
```

这种测试比：

```
今天随便测 POST
明天随便测 DELETE
```

可靠得多。

因为你是在验证一个完整业务过程。

------

## 六、测试发现问题以后怎么办

这一点正好连接前面的 Debug 和日志课程。

假设：

```
POST /users
```

正常。

但是：

```
GET /users/999
```

返回：

```
500
```

而你预期：

```
用户不存在
```

那么：

```
测试发现问题
```

只是第一步。

接下来：

```
Postman 看见 500
↓
IDEA 看异常日志
↓
发现 NullPointerException
↓
Debug
↓
发现 userMapper 查询结果为 null
↓
Service 没处理用户不存在
↓
修改
↓
重新测试
```

这就是你的教学计划真正想建立的：

```
开发
↓
测试
↓
发现问题
↓
调试
↓
修复
↓
再次测试
```

不是：

```
写完代码
↓
结束
```

第五阶段：项目能力教学计划.mdMD

------

## 七、修完 Bug 为什么必须重新测

假设你修复：

```
查询不存在用户导致500
```

添加：

```
if (user == null) {
    throw new UserNotFoundException();
}
```

现在 `/users/999` 正常了。

能不能结束？

还不够。

因为你的修改可能意外影响：

```
正常用户查询
```

所以至少重新测试：

```
不存在用户
→ 确认 Bug 修复

存在用户
→ 确认原功能没有被破坏
```

这种思想以后会发展成一个很重要的概念：

> **回归测试（Regression Testing）**

即：

> 修改以后重新验证原本正常的功能，避免“修好 A，又弄坏 B”。

这节课不深入自动化回归测试，但要先有这个意识。

------

## 八、不要只验证响应，还要验证最终状态

假设删除：

```
DELETE /users/10
```

Postman 返回：

```
{
  "code": 200,
  "message": "删除成功"
}
```

很多初学者到这里就认为测试结束。

但有一种很糟糕的 Bug：

```
接口永远返回“删除成功”
```

实际上数据库根本没删。

所以测试时需要区分：

```
接口说成功
```

和：

```
事情真的成功
```

对于数据库业务尤其重要。

例如：

```
新增
→ 查询数据库

修改
→ 再查询验证

删除
→ 再查询确认不存在
```

也就是：

> **不要只相信返回值，要验证系统最终状态。**

------

## 九、现阶段不用追求“测试所有可能情况”

这里需要控制学习深度。

真正的软件测试可以非常复杂：

```
单元测试
集成测试
接口自动化测试
性能测试
压力测试
安全测试
覆盖率
Mock
测试框架
```

这些都是真实存在的。

但你这一阶段的目标并不是成为测试工程师。

你现在最重要的是：

> **写完一个接口以后，具备最基础但可靠的验证意识。**

所以暂时抓住四类：

```
正常数据
异常数据
边界数据
重复数据
```

已经非常够用了。

------

## 十、你以后可以采用的简单测试模板

每写完一个接口，可以快速问自己：

```
1. 正常输入能成功吗？

2. 明显错误的输入会被拒绝吗？

3. 边界值正确吗？

4. 重复操作会怎样？

5. 不存在的数据会怎样？

6. 返回成功以后，数据库状态真的正确吗？
```

不是每个接口都需要六项全部测试。

比如查询接口没有“重复数据”问题。

要根据业务选择。

核心是：

> 不再只测试一条“最顺利的路线”。

------

## 十一、这一课真正要掌握的内容

第3课学 Postman 时，你掌握的是：

```
我怎么发请求？
```

这一课进一步解决：

```
我应该发哪些请求？
```

这是两个完全不同的层次。

现在你应该形成：

```
写完接口
↓
先测试正常数据
↓
再测试异常数据
↓
测试关键边界
↓
测试重复 / 不存在等业务场景
↓
检查响应
↓
验证数据库最终状态
↓
发现 Bug
↓
日志 / Debug 定位
↓
修复
↓
重新测试
```

当你开始自觉这样做时，就已经从：

> “代码能运行”

向：

> **“我能确认这个功能是可靠的”**

迈进了一步。

到这里，第五阶段的第三模块——**项目规范**——结束。

下一课进入第四模块，也是整个后端项目里非常核心的一部分：**第9课：登录认证**。我们会正式讲清楚一个看似简单、实际上非常关键的问题：

> 用户第一次登录以后，第二次请求 `/users`、`/orders` 时，服务器到底怎么知道“这个请求还是刚才那个人发来的”？



# 第9课：登录认证

这一课开始进入第四模块——**登录与权限**。按照你的教学计划，这一模块是整个后端里非常重要的业务知识，而第9课只解决一个核心问题：

> **用户登录成功之后，后续请求到来时，服务器怎么知道“你是谁”？**

这一课重点学习登录流程、Session、Cookie、JWT，以及它们分别怎样解决“第二次请求时识别用户身份”的问题。第五阶段：项目能力教学计划.mdMD

------

## 一、为什么“登录成功”还不够

### 1. HTTP 请求本身不会自动记住你是谁

先看一个最简单的登录接口：

```
@PostMapping("/login")
public Result login(@RequestBody LoginRequest request) {
    return userService.login(request);
}
```

用户发送：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

服务器查询数据库：

```
用户名存在
↓
密码正确
↓
登录成功
```

到这里似乎没问题。

但用户接下来又请求：

```
GET /orders
```

服务器会遇到一个关键问题：

> **这次请求是谁发来的？**

因为从服务器视角看，这是两次独立的 HTTP 请求：

```
请求1：
POST /login

请求结束
```

然后：

```
请求2：
GET /orders
```

HTTP 本身并不会天然告诉服务器：

```
请求2
=
刚才登录成功的那个用户
```

所以登录系统真正困难的地方并不是：

> 用户名密码对不对。

而是：

> **登录成功以后，怎样在后续请求中持续证明自己的身份。**

这就是“认证”系统要解决的问题。

------

### 2. Authentication 是“你是谁”

这一课主要讲的是：

> **认证（Authentication）**

它回答：

```
你是谁？
```

例如：

```
你是不是已经登录？

你是 userId=10 吗？
```

下一课讲的：

> **授权（Authorization）**

解决的则是：

```
你能做什么？
```

比如：

```
你虽然登录了，
但你是不是管理员？

你有没有删除用户的权限？
```

所以先把这两个词分开：

```
认证
=
确认身份

授权
=
确认权限
```

今天只重点解决前者。

------

## 二、一个完整的登录流程

### 1. 第一次请求：验证用户名和密码

假设用户登录：

```
POST /login
```

Body：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

服务器大致经历：

```
Controller 接收请求
↓
Service 查询用户
↓
数据库找到 zhanghao
↓
校验密码
↓
密码正确
↓
登录成功
```

如果用户名或者密码错误：

```
登录失败
```

这一部分很好理解。

真正关键的是：

> 登录成功以后服务器要做什么？

通常有两大类思路：

```
Session
```

或者：

```
Token / JWT
```

------

### 2. 两种核心思路

可以先把它们非常粗略地区分成：

```
Session：

服务器记住你
```

和：

```
JWT：

客户端拿着身份证明
```

这是整节课最重要的直觉。

后面我们分别展开。

------

## 三、Session + Cookie：服务器记住你

### 1. Session 的基本思想

假设张浩登录成功。

服务器可以创建一份记录：

```
Session ID:
abc123

对应用户:
userId = 10
```

也就是说服务器内部记住：

```
abc123
→
张浩
```

这份服务器端保存的用户状态，就是 Session 思路的核心。

但还有一个问题：

> 下一次请求时，服务器怎么知道该找 `abc123` 这个 Session？

这时候就轮到 Cookie 出场。

------

### 2. Cookie 是浏览器帮你保存的小数据

登录成功以后，服务器可能返回：

```
Set-Cookie: JSESSIONID=abc123
```

浏览器收到以后，就把：

```
JSESSIONID=abc123
```

保存起来。

以后请求：

```
GET /orders
```

浏览器会自动附带：

```
Cookie: JSESSIONID=abc123
```

服务器收到：

```
abc123
```

然后去 Session 中查：

```
abc123
↓
userId = 10
↓
原来是张浩
```

于是服务器就知道：

> 这是刚才已经登录的用户。

------

### 3. 整个 Session 登录流程

第一次登录：

```
浏览器
↓
POST /login
username + password
↓
服务器
↓
验证成功
↓
创建 Session

abc123 → userId=10
↓
返回 Cookie

JSESSIONID=abc123
```

后续请求：

```
浏览器
↓
GET /orders
Cookie: JSESSIONID=abc123
↓
服务器
↓
根据 abc123 查 Session
↓
找到 userId=10
↓
识别用户
```

可以把它浓缩成：

> **Cookie 带编号，Session 保存身份。**

这句话很适合记忆。

------

## 四、Cookie 和 Session 不要混为一谈

这两个词经常一起出现，所以初学者很容易觉得它们就是一回事。

不是。

### 1. Cookie 在客户端

Cookie 通常保存在：

```
浏览器
```

例如：

```
JSESSIONID=abc123
```

它只是客户端携带的一小段数据。

------

### 2. Session 在服务器

真正的用户信息可能保存在服务器：

```
abc123
↓
{
  userId: 10,
  username: "zhanghao"
}
```

所以：

```
Cookie
=
客户端保存
Session
=
服务器保存
```

它们通常这样配合：

```
客户端 Cookie
保存 Session ID
↓
请求发送 Session ID
↓
服务器查 Session
↓
找到用户身份
```

------

### 3. Cookie 里一般不是直接塞密码

不要想成：

```
Cookie:
username=zhanghao
password=123456
```

然后每次请求都重新登录。

这既不合理也不安全。

通常 Cookie 只需要携带：

```
一个能够关联身份的信息
```

比如：

```
Session ID
```

服务器再利用它找到真正的登录状态。

------

## 五、Session 有什么优点和问题

### 1. 优点：理解起来直观

Session 很符合人的直觉：

```
用户登录
↓
服务器建立档案
↓
以后通过编号找到档案
```

而且服务器掌握完整控制权。

例如用户退出：

```
删除 Session
```

之后即使客户端还有：

```
abc123
```

服务器查不到：

```
abc123
```

就认为已经退出。

------

### 2. 问题：服务器需要保存登录状态

假设有：

```
100个用户
```

服务器维护100份 Session。

如果：

```
100万用户
```

就需要维护大量 Session 状态。

更麻烦的是，真实系统可能不是一台服务器：

```
服务器 A
服务器 B
服务器 C
```

用户登录时：

```
服务器 A
创建 Session
```

下一次请求却被负载均衡送到：

```
服务器 B
```

如果 B 没有 A 的 Session：

> B 就不知道你是谁。

当然这有解决方案，比如共享 Session、Redis 等，但这已经属于更复杂架构。

所以后来很多前后端分离系统会采用另一种方案：

> Token / JWT。

------

## 六、JWT：客户端带着自己的身份证明

### 1. JWT 的基本思想

JWT 全称：

> JSON Web Token

但你现在不需要研究 JWT 标准的底层细节。

它解决身份问题的思路和 Session 很不一样。

登录成功后：

```
服务器
↓
生成一个 Token
↓
返回给客户端
```

例如：

```
eyJhbGciOiJIUzI1NiJ9....
```

这个字符串看起来很乱。

你不用背格式。

只要知道：

> **它代表一次经过服务器签发的身份凭证。**

------

### 2. 登录成功以后客户端保存 Token

登录：

```
POST /login
```

服务器验证：

```
username 正确
password 正确
```

然后返回：

```
{
  "token": "eyJhbGciOiJIUzI1NiJ9..."
}
```

客户端保存它。

以后访问：

```
GET /orders
```

就携带：

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

服务器收到以后：

```
读取 Token
↓
验证 Token 是否有效
↓
解析用户身份
↓
知道 userId=10
```

所以完整流程：

```
登录
↓
服务器发 Token
↓
客户端保存 Token
↓
以后每次请求携带 Token
↓
服务器验证 Token
↓
识别用户
```

------

## 七、JWT 与 Session 的根本区别

这个区别一定要理解。

### 1. Session

服务器保存：

```
abc123 → userId=10
```

客户端只带：

```
abc123
```

所以：

> **主要身份状态保存在服务器。**

------

### 2. JWT

客户端携带：

```
Token
```

Token 本身就可以包含类似：

```
userId = 10
username = zhanghao
```

等身份信息，并带有防篡改机制。

服务器收到以后验证 Token：

```
是真的服务器签发的吗？
有没有被修改？
有没有过期？
```

验证通过，就可以知道身份。

所以可以粗略理解为：

> **JWT 把更多身份凭证放到了客户端携带的 Token 中。**

------

### 3. 一个非常直观的类比

Session 像：

> 餐厅给你一个取餐号 38。

餐厅内部保存：

```
38号
→
张浩的订单
```

你只需要拿：

```
38
```

回来。

这就是：

```
Cookie：38

服务器 Session：
38 → 张浩
```

------

JWT 更像：

> 给你一张经过官方盖章的证件。

证件上写着：

```
姓名：张浩
用户ID：10
有效期：...
```

你每次都出示这张证件。

服务器检查：

> 章是真的、证件没有被篡改、还没有过期。

于是相信你的身份。

这个区别基本就是：

```
Session：
服务器查档案

JWT：
服务器验证凭证
```

------

## 八、JWT 为什么不能随便伪造

这时候你可能自然会想到：

> 如果 Token 里写着 `userId=10`，那我自己改成 `userId=1`，岂不是变成管理员？

这正是 JWT 必须：

```
签名
```

的原因。

我们不深入密码学，只理解原理。

服务器生成 JWT 时，会利用：

```
Token 内容
+
服务器掌握的秘密
```

生成一段签名。

粗略看：

```
用户信息
+
签名
=
JWT
```

攻击者虽然可能看到 Token 中的一些信息，但如果自行修改：

```
userId=10
↓
userId=1
```

原来的签名就对不上了。

服务器验证时会发现：

```
内容被改过
↓
拒绝
```

所以 JWT 的关键不是：

> 内容别人完全看不到。

而是：

> **别人不能在不知道服务器秘密的情况下随意修改，并继续让服务器相信。**

这个理解非常重要。

因此也不要把敏感秘密直接放进 JWT，例如：

```
用户密码
银行卡密码
```

JWT 并不等同于“绝对加密保险箱”。

------

## 九、JWT 一般放在哪里

最常见的是 HTTP Header：

```
Authorization: Bearer <token>
```

例如：

```
Authorization
↓
Bearer eyJhbGciOi...
```

你在第5课日志里已经提前见过这种形式。

以后 Postman 测试登录接口时，基本流程就是：

### 1. 登录

```
POST /login
```

获得：

```
token = abc...
```

### 2. 请求受保护接口

例如：

```
GET /users/me
```

加 Header：

```
Authorization: Bearer abc...
```

### 3. 后端认证

```
读取 Authorization
↓
取出 Token
↓
验证
↓
解析 userId
↓
认为当前用户已经登录
```

------

## 十、服务器一般会在哪里做 Token 校验

假设系统有50个接口：

```
/users
/orders
/products
/profile
...
```

显然不应该每个 Controller 都写：

```
String token = ...
validateToken(token);
```

否则重复代码很多。

所以实际 Spring Boot 项目往往会把身份认证放在统一的位置，例如：

```
Filter
```

或者：

```
Interceptor
```

你前面 Web / Spring Boot 阶段应该已经对 Filter、Interceptor 有过基础认识。

于是流程可能变成：

```
HTTP 请求
↓
Filter / Interceptor
↓
读取 Token
↓
验证 Token
↓
认证成功
↓
Controller
↓
Service
```

如果 Token 无效：

```
请求
↓
认证失败
↓
直接拒绝
```

甚至根本不进入 Controller。

这就是统一认证机制。

------

## 十一、哪些接口不应该要求登录

当然不是所有请求都要 Token。

例如：

```
POST /login
```

如果这个接口本身也要求：

```
你必须先登录
```

就死循环了。

所以通常会有：

```
公开接口
```

比如：

```
/login
/register
```

不需要认证。

而：

```
/users/me
/orders
/profile
```

要求认证。

因此认证系统通常还要配置：

```
哪些路径放行
哪些路径必须登录
```

以后你看真实项目时经常会看到类似：

```
/login
/register
```

被排除在拦截器之外。

现在理解为什么即可。

------

## 十二、登录、认证和退出的完整闭环

### 1. Session 模式

登录：

```
用户名密码
↓
验证成功
↓
创建 Session
↓
客户端保存 Cookie
```

认证：

```
后续请求带 Cookie
↓
服务器查 Session
↓
识别用户
```

退出：

```
删除 / 失效 Session
```

------

### 2. JWT 模式

登录：

```
用户名密码
↓
验证成功
↓
生成 JWT
↓
返回 Token
```

认证：

```
后续请求带 Token
↓
服务器验证 JWT
↓
识别用户
```

退出的概念相对复杂一点。

最简单的客户端做法是：

```
删除自己保存的 Token
```

以后不再携带：

```
Authorization
```

自然就相当于退出。

不过 JWT 如果已经签发，在服务器端如何主动让它立即失效，会涉及黑名单、短有效期、刷新 Token 等进一步设计，这一课不深入。

------

## 十三、Session 和 JWT 应该怎么理解，不必急着选“谁更好”

初学阶段很容易陷入：

> Session 和 JWT 哪个更先进？

其实没必要。

它们是两种不同的认证方案，各有适用场景。

你现在最需要的是能看懂项目。

看到：

```
JSESSIONID
HttpSession
```

马上意识到：

> 这是 Session 登录方案。

看到：

```
JwtUtils
Authorization
Bearer
Token
```

马上意识到：

> 这是 Token / JWT 认证方案。

对于现代前后端分离 Spring Boot 项目，你确实很容易遇到 JWT，所以必须重点理解。

但不要把 Session 理解成“落后技术”。

------

## 十四、用一次完整请求把所有知识串起来

假设项目采用 JWT。

用户登录：

```
POST /login
```

JSON：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

服务器：

```
查询数据库
↓
确认用户存在
↓
校验密码
↓
生成 JWT
↓
返回 Token
```

客户端得到：

```
eyJhbGciOi...
```

接下来请求：

```
GET /orders
```

Header：

```
Authorization: Bearer eyJhbGciOi...
```

后端：

```
请求进入
↓
认证拦截器
↓
读取 Authorization
↓
解析 JWT
↓
验证签名
↓
检查是否过期
↓
获得 userId=10
↓
确认已经登录
↓
放行 Controller
↓
查询 userId=10 的订单
```

如果请求没有 Token：

```
GET /orders
↓
无 Authorization
↓
认证失败
↓
拒绝访问
```

如果 Token 被篡改：

```
签名验证失败
↓
拒绝访问
```

如果 Token 过期：

```
认证失败
↓
要求重新登录
```

这就是一个完整认证流程。

------

## 十五、这一课真正需要掌握什么

首先理解问题本身：

```
登录成功
≠
服务器以后自动知道你是谁
```

必须建立一种持续身份机制。

然后掌握两种思路。

Session：

```
登录成功
↓
服务器保存 Session
↓
浏览器保存 Session ID Cookie
↓
以后带 Cookie
↓
服务器查 Session
```

JWT：

```
登录成功
↓
服务器签发 JWT
↓
客户端保存 Token
↓
以后 Header 携带 Token
↓
服务器验证 Token
```

再记住几个关键关系：

```
Cookie
=
客户端保存的一小段数据
Session
=
服务器保存的登录状态
JWT
=
服务器签发、客户端携带的身份凭证
```

以及今天最核心的一句话：

> **认证要解决的不是“第一次登录有没有成功”，而是“后续每一次请求到来时，服务器如何可靠地知道你是谁”。**

做到这里，第9课的目标就完成了。

下一课是 **第10课：权限控制**。我们会在“服务器已经知道你是谁”的基础上继续解决第二个问题：

> **知道你是张浩以后，为什么还不能默认允许你删除用户？**

也就是正式区分 **Authentication（认证）** 和 **Authorization（授权）**，并学习角色、权限与 RBAC。



# 第10课：权限控制

上一课解决的是**认证**：

> 服务器怎么知道“你是谁”。

这一课继续解决第二个问题：

> **服务器知道你是谁之后，怎么判断“你能做什么”。**

按照你的教学计划，这一课重点包括 `Authentication`、`Authorization`、角色 `Role`、权限 `Permission`、RBAC，以及为什么不能让所有登录用户都拥有相同能力。第五阶段：项目能力教学计划.mdMD

## 一、认证和授权必须分开理解

### 1. Authentication：确认身份

例如你请求：

```
GET /profile
```

Header：

```
Authorization: Bearer xxxxx
```

服务器解析 Token 后知道：

```
userId = 10
username = zhanghao
```

这一步叫：

> **Authentication，认证。**

也就是：

```
你是谁？
```

上一课已经解决。

------

### 2. Authorization：判断你有没有资格

现在用户已经登录。

他请求：

```
DELETE /users/20
```

服务器虽然知道：

```
这是 userId=10
```

但还有一个问题：

> userId=10 能不能删除其他用户？

这就是：

> **Authorization，授权。**

它回答的是：

```
你能不能做这件事？
```

所以要牢牢记住：

```
认证 Authentication
=
确认身份
授权 Authorization
=
判断权限
```

一个用户可能：

```
已经认证
```

但：

```
没有权限
```

这完全正常。

------

## 二、为什么“登录了”不能等于“什么都能做”

假设后台系统里有两种用户：

```
管理员
普通用户
```

普通用户登录后应该可以：

```
查看自己的资料
修改自己的密码
查看自己的订单
```

管理员可能还可以：

```
查看所有用户
禁用用户
删除用户
修改用户角色
```

如果系统只判断：

```
if (user != null) {
    // 已登录，放行
}
```

那么普通用户也可以请求：

```
DELETE /users/100
```

这显然不合理。

因此认证只能解决第一层：

```
匿名用户
↓
已登录用户
```

还要进一步区分：

```
已登录用户
├── 普通用户
├── 管理员
└── 其他角色
```

再判断：

> 当前这个角色是否允许执行当前操作。

------

## 三、Role：角色是什么

### 1. 角色就是“用户类别”

例如最简单的系统：

```
USER
ADMIN
```

数据库中的用户可能：

```
id = 1
username = alice
role = USER
```

另一个：

```
id = 2
username = bob
role = ADMIN
```

这时候角色实际上是在表达：

> 这个用户属于哪一类人员。

------

### 2. 为什么不直接针对每个用户写判断

假设你有1000个普通用户。

显然不能这样：

```
if (userId == 1 ||
    userId == 3 ||
    userId == 5 ||
    userId == 8 ||
    ...) {
}
```

因为太难维护。

更合理的是：

```
这些用户都属于 USER
```

然后规则写成：

```
USER
可以查看自己的信息
ADMIN
可以管理所有用户
```

于是：

```
用户
↓
角色
↓
角色决定一组能力
```

这就是 Role 的意义。

------

## 四、Permission：比角色更细的“具体能力”

角色解决了分类问题，但有时还不够细。

例如管理员能够：

```
查看用户
新增用户
修改用户
删除用户
```

这些具体行为可以拆成：

```
user:view
user:create
user:update
user:delete
```

这些就是：

> **Permission，权限。**

可以简单理解为：

```
Role
=
你是什么身份类别
Permission
=
你具体能做哪些操作
```

例如：

```
ADMIN
```

拥有：

```
user:view
user:create
user:update
user:delete
```

而：

```
USER
```

可能只有：

```
user:view-self
user:update-self
```

------

## 五、RBAC：通过角色管理权限

RBAC 全称：

> **Role-Based Access Control**

中文通常叫：

> **基于角色的访问控制。**

这是实际后台系统里非常常见的一种权限设计思想。

核心关系可以画成：

```
用户
↓
角色
↓
权限
```

例如：

```
张浩
↓
ADMIN
↓
user:view
user:create
user:update
user:delete
```

另一个用户：

```
Tom
↓
USER
↓
user:view-self
user:update-self
```

这样以后如果想让所有管理员增加一个：

```
order:refund
```

权限，不需要修改每个管理员。

只要：

```
给 ADMIN 角色增加 order:refund
```

即可。

------

## 六、为什么 Role 和 Permission 有时会同时存在

假设系统很简单，只有：

```
ADMIN
USER
```

那么直接根据 Role 判断就够了。

例如：

```
if (!"ADMIN".equals(currentUser.getRole())) {
    throw new ForbiddenException();
}
```

这已经可以工作。

但系统变复杂后，角色可能很多：

```
超级管理员
用户管理员
订单管理员
客服
财务
普通用户
```

这时候直接写：

```
if (role == ADMIN
    || role == USER_ADMIN
    || role == SUPER_ADMIN) {
}
```

会越来越乱。

于是可以改成判断：

```
有没有 user:delete 权限
```

至于这个权限来自哪个角色，由 RBAC 配置决定。

于是代码关心的是：

```
你有没有这个能力
```

而不是：

```
你具体叫什么角色
```

这会更灵活。

不过你当前阶段不需要设计复杂权限系统。

只需要理解：

> 小项目可以直接按角色判断；复杂项目通常会进一步拆到权限级别。

------

## 七、一次完整的权限判断流程

假设有接口：

```
DELETE /users/20
```

当前用户携带 JWT：

```
Authorization: Bearer ...
```

整个流程可能是：

```
请求进入
↓
认证
↓
解析 JWT
↓
得到当前用户 userId=10
↓
查询 / 解析角色
↓
role = USER
↓
检查 DELETE /users/* 是否要求 ADMIN
↓
权限不足
↓
拒绝
```

如果当前用户：

```
role = ADMIN
```

则：

```
认证成功
↓
授权成功
↓
进入 Controller
↓
执行删除
```

所以完整访问控制其实是两道门：

```
第一道门：
你登录了吗？
```

然后：

```
第二道门：
你有权限吗？
```

------

## 八、未登录和没权限不是同一种错误

这是很值得区分的一点。

### 1. 用户根本没登录

例如：

```
GET /admin/users
```

没有 Token。

服务器连你是谁都不知道。

这种情况本质是：

> **认证失败。**

------

### 2. 用户已经登录，但权限不足

例如：

```
userId = 10
role = USER
```

去请求：

```
DELETE /users/20
```

服务器知道你是谁。

但是判断：

```
USER
没有 user:delete
```

于是拒绝。

这属于：

> **授权失败。**

所以：

```
没登录
≠
没权限
```

这两个以后排查接口问题时也要分清。

------

## 九、权限控制通常放在哪里

### 1. 最直接的写法

可以在 Controller 里：

```
@DeleteMapping("/users/{id}")
public Result deleteUser(@PathVariable Long id) {

    if (!currentUser.isAdmin()) {
        throw new ForbiddenException();
    }

    userService.delete(id);
    return Result.success();
}
```

能工作。

但如果几十个接口都这样：

```
if (!currentUser.isAdmin()) {
    ...
}
```

就会很重复。

------

### 2. 更常见的是统一处理

实际项目可能通过：

```
Interceptor
Filter
AOP
Spring Security
```

统一做权限判断。

例如概念上：

```
@RequireRole("ADMIN")
@DeleteMapping("/users/{id}")
public Result deleteUser(...) {
}
```

或者以后使用 Spring Security 时可能看到类似：

```
@PreAuthorize("hasRole('ADMIN')")
```

你现在不需要掌握这些具体框架 API。

重要的是理解：

> **权限判断属于横切逻辑，通常不会把大量判断散落在每个业务方法里。**

这和上一课的认证很类似。

认证通常统一验证 Token；

授权通常统一检查角色或权限。

------

## 十、一个容易忽略的问题：自己的数据和别人的数据

权限并不永远只是：

```
ADMIN
vs
USER
```

还有一种非常常见的业务规则：

> 普通用户只能操作自己的数据。

比如：

```
GET /users/10
```

当前登录用户：

```
userId = 10
```

可以查看自己。

但如果请求：

```
GET /users/20
```

即使他是：

```
USER
```

也不应该随便查看别人的隐私数据。

所以权限判断可能变成：

```
当前用户是不是管理员？
或者
资源是不是属于当前用户？
```

例如：

```
if (!currentUser.isAdmin()
        && !currentUser.getId().equals(targetUserId)) {
    throw new ForbiddenException();
}
```

这说明权限控制不只是：

```
角色判断
```

还可能结合：

```
当前用户
目标资源
业务关系
```

一起决定。

这是非常典型的实际项目逻辑。

------

## 十一、不要相信前端“把按钮藏起来”就是权限控制

这一点和前面的参数校验非常类似。

假设普通用户登录以后，前端不显示：

```
删除用户
```

按钮。

这能改善界面。

但不能作为真正的安全措施。

因为用户完全可以绕过前端，用 Postman：

```
DELETE /users/20
```

如果后端没有权限检查：

> 删除照样成功。

所以：

```
前端隐藏按钮
```

只能表示：

> 用户体验层面不让他看到。

真正的权限控制必须发生在：

```
后端
```

这和第7课非常像：

```
前端参数校验
不能替代
后端参数校验
```

同样：

```
前端按钮控制
不能替代
后端权限控制
```

------

## 十二、Role 和 JWT 是什么关系

你可能会想到：

> 既然 JWT 能包含用户信息，那角色是不是也可以放进去？

可以。

例如 Token 中可能包含：

```
userId = 10
role = ADMIN
```

后端解析 JWT 后得到：

```
当前用户 = 10
当前角色 = ADMIN
```

然后进行权限判断。

但也有项目只在 Token 中保存：

```
userId
```

然后每次根据 userId 查询数据库获取最新角色。

两种都有可能。

区别涉及：

```
性能
角色更新后的实时性
Token设计
缓存
```

现在不需要深入。

你只需要知道：

> JWT 负责携带/证明身份信息；Role 和 Permission 用来决定授权结果。

不要把它们混成一个概念。

------

## 十三、用一个后台管理系统串起来

假设用户表：

```
user
```

包含：

```
id
username
password
role
```

有两条记录：

```
1  admin  ...  ADMIN
10 zhanghao ... USER
```

### 1. 张浩登录

```
POST /login
```

服务器确认身份：

```
userId=10
role=USER
```

生成 JWT。

------

### 2. 张浩查看自己

```
GET /profile
```

认证：

```
Token有效
```

授权：

```
USER 可以查看自己的资料
```

成功。

------

### 3. 张浩删除别人

```
DELETE /users/20
```

认证：

```
成功
```

授权：

```
当前角色 = USER
要求角色 = ADMIN
```

失败。

------

### 4. Admin 登录后再请求

当前：

```
role = ADMIN
```

请求：

```
DELETE /users/20
```

认证：

```
成功
```

授权：

```
ADMIN 拥有删除用户权限
```

然后才真正进入：

```
UserService.delete(20)
```

这一整套逻辑就是：

```
登录
↓
认证
↓
身份确定
↓
授权
↓
是否允许执行
↓
业务代码
```

------

## 十四、权限测试也要用 Postman 验证

学了第8课以后，你应该自然想到：

> 权限代码写出来以后不能只看代码，必须测试。

例如管理员接口：

```
DELETE /users/10
```

至少测试三种情况。

### 1. 不带 Token

预期：

```
拒绝
```

因为：

```
未认证
```

------

### 2. 带普通用户 Token

预期：

```
拒绝
```

原因变成：

```
认证成功
但授权失败
```

------

### 3. 带管理员 Token

预期：

```
允许
```

这三组测试非常重要。

因为它们分别验证：

```
认证机制
+
授权机制
```

都工作正常。

------

## 十五、这一课真正需要掌握什么

首先把两个核心词彻底分开：

```
Authentication
认证
=
你是谁
Authorization
授权
=
你能做什么
```

然后理解：

```
User
↓
Role
↓
Permission
```

其中：

```
Role
=
用户类别
Permission
=
具体操作能力
```

而：

```
RBAC
```

就是通过角色来组织和分配权限：

```
用户
↓
角色
↓
权限
```

最后形成完整访问控制思路：

```
请求
↓
检查登录状态
↓
未登录 → 拒绝
↓
认证成功
↓
获取当前用户 / 角色 / 权限
↓
检查当前操作是否允许
↓
没权限 → 拒绝
↓
有权限
↓
进入真正业务逻辑
```

这一课最核心的一句话就是：

> **登录只证明“你是你”，并不证明“你什么都能做”。**

到这里，第10课的目标完成。

下一课是 **第11课：登录项目实践**，也是这一模块的收尾。按照教学计划，我们会把前面的用户管理项目升级起来，把**登录、退出、身份认证和权限判断**真正串成一个完整后台系统。



# 第11课：登录项目实践

这一课是“登录与权限”模块的收尾。

前两课已经分别解决了两个问题：

- 第9课：**你是谁**——认证
- 第10课：**你能做什么**——授权

这一课不再引入太多新概念，而是把这些内容串成一个真正像后台系统的完整流程：**登录、退出、身份识别、权限判断**。这也正是你的教学计划对第11课的要求。第五阶段：项目能力教学计划.mdMD

## 一、先看一个完整后台系统应该具备什么

### 1. 用户管理系统升级前

假设我们原本只有：

```
GET    /users
GET    /users/{id}
POST   /users
PUT    /users/{id}
DELETE /users/{id}
```

这些接口可以完成 CRUD。

但问题是：

> 谁都可以调用。

只要知道接口地址，就可以：

```
查看用户
新增用户
修改用户
删除用户
```

这只能算：

> “有用户管理功能”。

还不能算真正的后台管理系统。

------

### 2. 加入登录和权限以后

升级后的系统应该是：

```
/login
↓
登录成功
↓
获得身份凭证
↓
访问后台接口
↓
认证
↓
权限判断
↓
执行真正业务
```

例如：

```
普通用户
→ 查看自己的资料

管理员
→ 查看所有用户
→ 新增用户
→ 修改用户
→ 删除用户
```

于是整个系统开始具有：

```
用户
身份
角色
权限
```

这时候才真正像一个后台系统。

------

## 二、登录功能应该怎么组织

我们继续采用前面讲过的 JWT 思路，因为它比较适合你以后常见的前后端分离 Spring Boot 项目。

### 1. 登录请求 DTO

先定义：

```
public class LoginRequest {

    @NotBlank
    private String username;

    @NotBlank
    private String password;
}
```

这里顺便用到了第7课的参数校验。

它表达：

```
username
必须有内容

password
必须有内容
```

------

### 2. Controller 接收登录请求

例如：

```
@PostMapping("/login")
public Result login(
        @Valid @RequestBody LoginRequest request) {

    return userService.login(request);
}
```

Controller 的职责依然很简单：

```
接收请求
↓
参数校验
↓
调用 Service
```

真正的登录业务放到 Service。

------

### 3. Service 处理登录

逻辑大致应该是：

```
根据 username 查询用户
↓
用户不存在
→ 登录失败
↓
用户存在
↓
校验密码
↓
密码错误
→ 登录失败
↓
密码正确
↓
生成 JWT
↓
返回 Token
```

代码概念上可能像：

```
public String login(LoginRequest request) {

    User user =
        userMapper.selectByUsername(request.getUsername());

    if (user == null) {
        throw new LoginException("用户名或密码错误");
    }

    if (!passwordMatches(
            request.getPassword(),
            user.getPassword())) {

        throw new LoginException("用户名或密码错误");
    }

    return jwtUtils.generateToken(
            user.getId(),
            user.getRole());
}
```

这里你应该能读懂业务结构，而不用纠结 JWT 工具类具体每一行怎么实现。

------

## 三、密码为什么不能直接这样比较

### 1. 最简单但不正规的写法

你可能想到：

```
if (!request.getPassword()
        .equals(user.getPassword())) {
    ...
}
```

如果数据库：

```
password = 123456
```

确实能比较。

但真实项目通常不会把用户密码直接明文保存。

否则数据库一旦泄露：

```
zhanghao  123456
tom       abc123
alice     666666
```

所有密码直接暴露。

------

### 2. 更合理的基本思路

注册时：

```
用户输入密码
↓
密码哈希
↓
保存哈希结果
```

数据库可能保存：

```
$2a$10$......
```

而不是：

```
123456
```

登录时：

```
用户输入 123456
↓
和数据库中的哈希结果进行验证
↓
判断是否匹配
```

以后你可能看到：

```
passwordEncoder.matches(
    rawPassword,
    encodedPassword
)
```

例如 Spring Security 常见的 `PasswordEncoder`。

这一课不深入密码哈希算法。

你只要记住：

> **正规登录系统一般不应该明文保存密码。**

------

## 四、登录成功后返回什么

### 1. 最核心的是 Token

例如：

```
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOi..."
  }
}
```

客户端拿到 Token 以后保存起来。

以后访问：

```
GET /users
```

携带：

```
Authorization: Bearer eyJhbGciOi...
```

------

### 2. 也可能同时返回一些用户信息

例如：

```
{
  "token": "xxxxx",
  "user": {
    "id": 10,
    "username": "zhanghao",
    "role": "USER"
  }
}
```

这样前端可以直接显示：

```
欢迎你，zhanghao
```

而不必马上再请求一次用户信息。

不同项目设计可能不同。

核心不变：

> 登录成功以后，客户端必须拿到后续能够证明身份的东西。

------

## 五、后续请求怎么知道当前用户是谁

这是整个项目实践最关键的一步。

### 1. 请求进入认证层

客户端：

```
GET /users/me

Authorization: Bearer xxxxx
```

后端首先经过：

```
Filter / Interceptor
```

概念流程：

```
读取 Authorization
↓
没有 Token
→ 拒绝
↓
有 Token
↓
验证 Token
↓
无效 / 过期
→ 拒绝
↓
有效
↓
解析 userId / role
```

到这里系统已经知道：

```
当前用户：
userId = 10
role = USER
```

------

### 2. 当前用户信息要传给后面的业务

问题来了：

> Interceptor 知道 userId=10，Controller 和 Service 怎么获得？

真实项目有很多方案。

比如：

```
请求属性
ThreadLocal
Spring Security Context
```

你目前不需要深入这些机制。

只要理解最终目标：

```
认证层解析身份
↓
保存“当前用户”信息
↓
Controller / Service 可以读取当前用户
```

概念上可能有：

```
Long userId = CurrentUser.getId();
```

于是：

```
@GetMapping("/me")
public User getMe() {

    Long userId = CurrentUser.getId();

    return userService.getById(userId);
}
```

注意这里的 `CurrentUser` 只是帮助理解的抽象写法，不代表所有项目都会这么命名。

------

## 六、为什么“当前用户”很重要

假设接口是：

```
GET /users/me
```

它不需要客户端传：

```
?id=10
```

因为：

> 当前登录用户是谁，后端已经从 Token 里知道了。

流程：

```
Token
↓
解析 userId=10
↓
GET /users/me
↓
查询 id=10
```

这种设计比：

```
GET /users/10
```

更适合“查看自己的信息”。

因为如果让客户端自己传：

```
id=10
```

攻击者完全可以改成：

```
id=20
```

然后尝试看别人数据。

所以涉及“我自己”的业务时，经常应该从：

```
当前认证身份
```

获取 userId，而不是盲目信任请求参数。

------

## 七、权限判断怎么接进去

### 1. 普通用户接口

例如：

```
GET /users/me
```

只要求：

```
已经登录
```

那么流程：

```
Token有效
↓
认证成功
↓
允许访问
```

------

### 2. 管理员接口

例如：

```
DELETE /users/{id}
```

要求：

```
ADMIN
```

流程变成：

```
Token
↓
认证
↓
当前角色 = ?
↓
ADMIN
→ 放行

USER
→ 拒绝
```

所以 Controller 甚至可以保持很干净：

```
@DeleteMapping("/users/{id}")
public Result delete(@PathVariable Long id) {

    userService.delete(id);

    return Result.success();
}
```

权限检查在进入 Controller 前已经完成。

这是比较理想的职责划分。

------

## 八、一个简单的接口权限设计

假设项目只有：

```
USER
ADMIN
```

可以设计成：

| 接口                 | USER     | ADMIN    |
| -------------------- | -------- | -------- |
| `POST /login`        | 无需登录 | 无需登录 |
| `POST /logout`       | √        | √        |
| `GET /users/me`      | √        | √        |
| `PUT /users/me`      | √        | √        |
| `GET /users`         | ×        | √        |
| `POST /users`        | ×        | √        |
| `DELETE /users/{id}` | ×        | √        |

你应该注意：

```
/login
```

必须公开。

否则就会出现：

> 没登录不能访问登录接口。

明显不合理。

------

## 九、退出登录到底怎么实现

这取决于你使用 Session 还是 JWT。

### 1. Session 模式

Session 的退出很好理解：

```
找到当前 Session
↓
使其失效
```

例如概念上：

```
session.invalidate();
```

以后浏览器再带原来的 Session ID：

```
服务器已经找不到对应登录状态
```

于是认为未登录。

------

### 2. JWT 模式

最简单的 JWT 模式下：

```
客户端删除 Token
```

例如前端：

```
localStorage 中删除 token
```

以后：

```
请求不再携带 Authorization
```

自然就变成未登录。

------

### 3. JWT 的一个现实问题

假设 Token 有效期：

```
2小时
```

用户点击退出以后，客户端把 Token 删除了。

但如果这个 Token 之前被别人复制走：

```
它理论上仍然可能在2小时内有效
```

因为服务器并没有像 Session 那样删除一条状态。

所以更严格的系统可能实现：

```
Token 黑名单
Redis
短 Token + Refresh Token
```

等机制。

这一课只认识这个问题，不展开。

------

## 十、登录失败时不要暴露太多信息

假设用户名不存在。

你可以返回：

```
用户名不存在
```

密码错误时：

```
密码错误
```

看起来对用户很友好。

但安全角度上，这也告诉了攻击者：

```
这个用户名到底有没有注册
```

因此真实系统经常统一返回：

```
用户名或密码错误
```

无论是：

```
用户名不存在
```

还是：

```
密码错误
```

都给同样提示。

这样外部更难通过登录接口批量判断：

> 哪些用户名真实存在。

你现在知道这种设计思想即可。

------

## 十一、登录接口应该怎么测试

结合第8课的接口测试，这一课应该真正形成测试闭环。

### 1. 正常登录

请求：

```
{
  "username": "zhanghao",
  "password": "123456"
}
```

预期：

```
登录成功
获得 Token
```

------

### 2. 密码错误

```
{
  "username": "zhanghao",
  "password": "wrong"
}
```

预期：

```
登录失败
不给 Token
```

------

### 3. 用户不存在

```
{
  "username": "nobody",
  "password": "123456"
}
```

预期：

```
登录失败
```

------

### 4. 参数为空

```
{
  "username": "",
  "password": ""
}
```

预期：

```
参数校验失败
```

这里验证：

```
@NotBlank
@Valid
```

------

## 十二、认证功能怎么测试

拿到正确 Token 后：

```
GET /users/me
```

Header：

```
Authorization: Bearer 正确Token
```

预期：

```
成功
```

然后分别测试：

```
不带 Token
```

预期：

```
拒绝
```

再测试：

```
伪造 Token
```

预期：

```
拒绝
```

以及：

```
过期 Token
```

预期：

```
拒绝
```

这样才说明认证机制基本可靠。

------

## 十三、权限怎么测试

假设：

```
DELETE /users/20
```

管理员才能使用。

至少测试：

```
不带 Token
→ 拒绝
USER Token
→ 拒绝
ADMIN Token
→ 成功
```

注意这里分别验证：

```
认证
```

和：

```
授权
```

不能只测试管理员成功一次。

------

## 十四、把整个项目流程串起来

现在一个完整后台系统已经可以这样运行：

### 1. 登录

```
POST /login
↓
参数校验
↓
查询用户
↓
校验密码
↓
生成 JWT
↓
返回 Token
```

### 2. 后续请求

```
GET /users/me
↓
携带 Token
↓
认证层验证 JWT
↓
得到当前用户
↓
权限检查
↓
Controller
↓
Service
↓
Mapper
↓
数据库
```

### 3. 管理员接口

```
DELETE /users/20
↓
Token有效？
↓
否 → 拒绝
↓
是
↓
角色是 ADMIN？
↓
否 → 拒绝
↓
是
↓
执行删除
```

### 4. 退出

```
POST /logout
↓
Session 失效
或
客户端删除 JWT
```

到这里，我们已经真正把前面的知识连起来了。

------

## 十五、你以后看到登录项目时应该怎么读

假设 Codex 生成项目里有：

```
LoginController
UserService
JwtUtils
AuthInterceptor
CurrentUser
Role
```

你应该大致能判断：

```
LoginController
→ 接收登录请求
UserService
→ 查询用户、校验密码等业务
JwtUtils
→ 生成和解析 Token
AuthInterceptor
→ 拦截请求并验证身份
CurrentUser
→ 保存 / 获取当前登录用户
Role
→ 用于权限控制
```

即使其中几十行代码不是你亲手写的，你也已经能根据职责把整个系统拼起来。

这正符合你这套课程最初的目标。

------

## 十六、这一课真正要掌握什么

不要把登录系统理解成只有：

```
if (用户名密码正确)
    登录成功
```

真正完整的是：

```
登录
↓
验证账号密码
↓
建立身份凭证
↓
后续请求携带凭证
↓
服务器认证
↓
获取当前用户
↓
权限判断
↓
执行业务
↓
退出 / 身份失效
```

而在代码结构上，你应该能够识别：

```
DTO
→ 接收并校验登录参数

Service
→ 登录业务

JWT / Session
→ 保存或证明登录状态

Filter / Interceptor
→ 统一认证

Role / Permission
→ 授权

Current User
→ 让业务代码知道当前是谁
```

做到这里，第11课就完成了，也意味着整个**登录与权限模块结束**。

接下来只剩第五阶段最后一个模块——**事务**。第12课会先不急着讲 `@Transactional`，而是先解决一个更基础的问题：

> 为什么有些数据库操作，不能只保证“每条 SQL 单独成功”，而必须保证“一组操作要么全部成功，要么全部失败”。



# 第12课：为什么需要事务

这一课进入第五阶段最后一个模块——**事务**。

按照你的教学计划，这一课先不急着讲 `@Transactional`，而是先把最核心的问题讲清楚：

> **为什么有些数据库操作不能只要求“每条 SQL 各自成功”，而必须保证“一组操作要么全部成功，要么全部失败”。**

这一课重点理解什么叫事务、为什么需要事务，以及 ACID 的基本含义；ACID 只要求理解，不深入到底层实现。

------

## 一、事务解决的到底是什么问题

### 1. 单条 SQL 成功，不代表整个业务成功

假设我们现在做一个最典型的业务：

> 张三给李四转账 100 元。

数据库里：

```
张三余额：1000
李四余额：500
```

这个转账至少需要两步：

```
UPDATE account
SET balance = balance - 100
WHERE id = 1;
```

张三扣 100。

然后：

```
UPDATE account
SET balance = balance + 100
WHERE id = 2;
```

李四加 100。

正常情况下：

```
张三：1000 → 900
李四：500  → 600
```

没有问题。

但现在假设第一条 SQL 成功以后：

```
张三：900
```

突然发生异常。

例如：

```
数据库连接中断
程序抛异常
服务器宕机
第二条 SQL 写错
```

导致：

```
李四加钱失败
```

最终数据库变成：

```
张三：900
李四：500
```

100 元凭空消失了。

问题并不是：

> 第一条 SQL 执行失败。

恰恰相反，第一条执行得非常成功。

真正的问题是：

> **整个“转账”业务只完成了一半。**

------

### 2. 数据库看到的是两条 SQL，业务看到的是一件事

从数据库操作来看：

```
SQL 1：扣款

SQL 2：加款
```

是两条独立 SQL。

但是从业务来看：

```
张三 → 李四 转账100元
```

是一个完整动作。

我们真正想要的是：

```
扣款成功
+
加款成功
```

才叫：

```
转账成功
```

只完成其中任何一步都不可以。

所以我们需要一种机制，把这几条数据库操作捆绑成一个整体：

```
开始

↓
张三扣100
↓
李四加100
↓

全部成功
→ 保存所有修改

中途任何一步失败
→ 前面的修改也撤销
```

这个整体就是：

> **事务（Transaction）。**

------

## 二、事务最核心的规则：要么全做，要么全不做

### 1. Commit：整个事务成功

还是刚才的转账。

开始事务：

```
张三：1000
李四：500
```

执行：

```
张三 -100
```

此时：

```
张三：900
```

再执行：

```
李四 +100
```

变成：

```
李四：600
```

所有操作都成功。

于是：

```
Commit
```

也就是：

> **提交事务。**

最终：

```
张三：900
李四：600
```

这些修改正式生效。

这里的 `commit` 和前面学 Git 的 `git commit` **不是同一个东西**。

只是英文都叫：

```
commit
```

Git Commit 是：

> 创建代码版本。

数据库 Commit 是：

> 确认事务中的数据库修改。

不要混淆。

------

### 2. Rollback：整个事务失败

还是：

```
张三：1000
李四：500
```

先执行：

```
张三 -100
```

然后第二步出现异常。

如果有事务：

```
异常
↓
Rollback
```

也就是：

> **回滚。**

那么第一步扣掉的100也撤销。

最终重新变成：

```
张三：1000
李四：500
```

所以：

```
事务成功
→ Commit
事务失败
→ Rollback
```

这两个词以后会非常常见。

------

### 3. 事务真正保护的不是 SQL，而是业务完整性

这一点很重要。

很多初学者会把事务理解成：

> “防止 SQL 报错。”

并不准确。

SQL 该报错还是会报错。

事务解决的是：

> **某一步出错以后，不让前面已经成功的操作留下一个不完整的业务状态。**

也就是：

```
失败可以
```

但：

```
失败一半
```

不可以。

------

## 三、什么时候需要事务

不是所有数据库操作都需要你特别关注事务。

### 1. 单纯查询通常不容易理解出事务价值

例如：

```
SELECT * FROM user WHERE id = 1;
```

它只是在读数据。

即使失败了：

```
查询失败
```

也没有发生：

```
数据库被改了一半
```

这种问题。

所以初学阶段理解事务，最好从：

> **一次业务中有多个写操作**

开始。

------

### 2. 多个修改必须共同成功时

典型场景之一就是转账：

```
账户A扣钱
+
账户B加钱
```

必须一起成功。

------

### 3. 创建订单

例如用户购买商品：

```
创建订单
↓
扣减库存
↓
记录支付信息
```

假设：

```
订单创建成功
```

但是：

```
库存扣减失败
```

那可能出现：

> 用户买到了实际上没有库存的商品。

反过来：

```
库存扣了
```

但：

```
订单创建失败
```

那更奇怪：

> 商品库存少了一件，但系统里根本没有对应订单。

所以这组操作也应该尽量保持完整：

```
创建订单
+
扣库存
```

要么一起成功，要么一起失败。

------

### 4. 用户注册

再比如注册时可能不仅仅：

```
INSERT user
```

还需要：

```
创建用户
↓
创建用户资料
↓
分配默认角色
```

假设：

```
user 表插入成功
```

但是：

```
角色分配失败
```

那么系统里可能出现：

> 一个存在账号，但没有正常角色配置的“半成品用户”。

如果业务要求这几步必须形成完整用户，那么就适合使用事务。

------

## 四、不是“代码连续写在一起”就自动成为事务

这是一个很容易产生的误解。

假设 Java：

```
userMapper.insert(user);

roleMapper.insertUserRole(
    user.getId(),
    roleId
);
```

因为这两行代码：

```
紧挨着
```

并不意味着数据库就自动认为：

> 这是一件不可分割的事情。

如果没有事务控制，完全可能：

```
第一条 SQL 成功
↓
数据库保存
↓
第二条 SQL 失败
```

然后第一条已经生效。

所以：

```
写在同一个方法里
```

和：

```
处于同一个事务里
```

不是一回事。

下一课的 `@Transactional`，就是 Spring 帮我们把一个方法中的相关数据库操作放进事务的一种重要方式。

------

## 五、事务和 Java 异常是怎么联系起来的

假设 Service：

```
public void transfer() {

    accountMapper.subtract(1, 100);

    accountMapper.add(2, 100);
}
```

正常：

```
subtract 成功
↓
add 成功
↓
结束
```

如果：

```
accountMapper.add(...)
```

抛出异常：

```
subtract 已经执行过
↓
add 报错
```

事务机制会关注：

> 当前整个事务是否正常完成。

如果判断事务失败，就会：

```
Rollback
```

把前面的数据库修改撤销。

所以以后你会经常看到：

```
执行 SQL
↓
发生 Exception
↓
事务回滚
```

但这里先注意：

> **并不是“任何异常、任何写法”都会天然触发 Spring 事务回滚。**

具体哪些异常回滚、为什么事务有时会失效，这是下一课 `@Transactional` 会涉及的内容。

这一课先只理解整体逻辑。

------

## 六、ACID：事务应该具备的四个基本特性

你的教学计划要求 ACID **够理解即可**，所以我们只建立直觉，不研究数据库底层实现。第五阶段：项目能力教学计划.mdMD

ACID 分别是：

```
A：Atomicity
C：Consistency
I：Isolation
D：Durability
```

------

### 1. Atomicity：原子性

这是你这一课最需要理解的。

原子性就是：

> **一个事务中的操作，要么全部成功，要么全部失败。**

例如：

```
张三扣100
+
李四加100
```

不能出现：

```
只扣钱
```

或者：

```
只加钱
```

所以：

```
成功
→ 全部提交

失败
→ 全部回滚
```

这就是原子性。

你现在可以把：

> 事务的核心直觉

基本和原子性联系起来。

------

### 2. Consistency：一致性

一致性稍微抽象一点。

简单理解：

> **事务执行前后，数据库都应该处于符合业务规则的合法状态。**

例如转账前：

```
张三 1000
李四 500

总金额 1500
```

转账后：

```
张三 900
李四 600

总金额还是 1500
```

符合业务规则。

但如果：

```
张三 900
李四 500
```

总金额变成：

```
1400
```

显然破坏了业务一致性。

这里需要注意：

> 一致性并不完全是数据库自己“魔法保证”的。

业务代码、约束、事务等都会共同保证系统的一致状态。

你现在知道这个概念即可。

------

### 3. Isolation：隔离性

这个是多用户同时操作时的问题。

假设两个人几乎同时操作同一个账户：

```
请求A
↓
修改余额

请求B
↓
也修改余额
```

两个事务同时执行时，不能毫无规则地互相干扰，否则可能产生错误数据。

所以数据库需要提供一定程度的：

> **事务之间的隔离。**

可以粗略理解成：

```
事务A正在做自己的事
事务B也在做自己的事
```

数据库要尽可能控制：

> A 能看到 B 的哪些中间状态，以及它们同时修改数据时怎么办。

以后数据库还有：

```
隔离级别
脏读
不可重复读
幻读
```

这些更深入的知识。

你当前目标不是数据库专家，所以这一阶段只记：

> **隔离性主要解决多个事务同时执行时相互影响的问题。**

------

### 4. Durability：持久性

持久性很好理解。

一旦：

```
事务 Commit 成功
```

数据库就应该认为：

> 这个修改已经正式保存。

不能说：

```
刚告诉你转账成功
↓
服务器重启
↓
钱又变回去了
```

所以持久性强调：

> **成功提交的数据应该可靠地保存下来。**

------

### 5. 用一句话压缩 ACID

你不需要背非常学术的定义，可以先这样理解：

```
A 原子性
→ 要么全成，要么全败
C 一致性
→ 前后都要符合数据和业务规则
I 隔离性
→ 多个事务同时执行时不要乱互相干扰
D 持久性
→ 提交成功后结果要真正保存
```

这样已经达到这一阶段要求。

------

## 七、一个完整案例：下单为什么需要事务

假设用户购买一台显示器。

当前库存：

```
stock = 5
```

下单逻辑：

```
public void createOrder() {

    orderMapper.insert(...);

    productMapper.reduceStock(...);
}
```

### 1. 没有事务

先：

```
INSERT order
```

成功。

数据库：

```
订单表新增一条订单
```

然后：

```
UPDATE product
SET stock = stock - 1
```

失败。

最终：

```
订单存在
库存没扣
```

系统状态就有问题。

------

### 2. 有事务

整体：

```
开始事务
↓
创建订单
↓
扣库存
```

如果全部成功：

```
Commit
```

最终：

```
订单存在
库存 5 → 4
```

如果扣库存失败：

```
Rollback
```

那么创建订单也撤销：

```
订单不存在
库存还是5
```

整个业务失败，但数据库仍然干净。

这其实就是事务最重要的价值：

> **宁愿整个业务失败，也不要留下一个只完成一半的状态。**

------

## 八、事务不能解决所有业务问题

这一点也很重要。

假设代码：

```
扣库存
↓
发送短信
```

数据库事务能够比较自然地控制：

```
INSERT
UPDATE
DELETE
```

等数据库操作。

但是：

```
短信已经发送出去
```

通常不能因为数据库 Rollback，就 magically 把用户手机上的短信收回来。

同样：

```
调用第三方支付
发送邮件
调用其他服务器
```

也不是简单数据库事务就能完全撤销的。

这会涉及更复杂的：

```
分布式事务
消息队列
补偿机制
```

等等。

但这些明显超出你现在的学习目标。

现在只要建立边界：

> **我们这一阶段讲的主要是 Spring Boot 中的一般数据库事务。**

------

## 九、事务一般应该放在哪一层

结合你前面学过的分层结构：

```
Controller
↓
Service
↓
Mapper
↓
Database
```

假设转账业务：

```
扣钱
+
加钱
```

这是一个：

> 完整业务动作。

因此事务通常应该围绕：

```
Service 方法
```

组织。

例如：

```
public void transfer(...) {

    accountMapper.subtract(...);

    accountMapper.add(...);
}
```

而不是分别给两个 Mapper 操作各自搞一个互不相关的事务。

因为我们的目标是：

```
整个 transfer()
```

要么成功，要么失败。

这再次体现：

> **事务的边界通常应该跟业务边界一致。**

这个概念对下一课非常重要。

------

## 十、遇到事务问题时应该怎么思考

以后你看到一个 Service：

```
public void createOrder() {

    orderMapper.insert(order);

    stockMapper.reduceStock(productId);
}
```

你的第一反应应该开始变成：

> 这两个数据库修改是不是一个不可分割的业务？

如果：

```
是
```

那就继续问：

> 如果第二步失败，第一步允许保留吗？

如果答案：

```
不允许
```

那这通常就是一个明确的事务场景。

所以判断要不要事务，可以使用一个非常实用的问题：

> **“中间执行到一半失败时，我能接受数据库保持这个状态吗？”**

不能接受：

```
→ 需要考虑事务
```

能接受，或者操作本来就是互相独立的：

```
→ 未必需要把它们放进同一个事务
```

------

## 十一、这一课真正需要掌握什么

这一课最重要的不是背 ACID，而是建立事务直觉。

首先理解：

```
一项业务
可能由多条 SQL 构成
```

而事务负责让它们：

```
全部成功
→ Commit

任何一步失败
→ Rollback
```

核心目标：

> **避免数据库出现“业务只完成一半”的状态。**

然后知道四个基本特性：

```
Atomicity
原子性
→ 要么全成，要么全败

Consistency
一致性
→ 保持合法业务状态

Isolation
隔离性
→ 并发事务减少相互干扰

Durability
持久性
→ 提交后的数据可靠保存
```

以及一个很关键的项目设计意识：

```
事务边界
≈
业务边界
```

比如：

```
transfer()
createOrder()
registerUser()
```

这种完整业务方法，往往比单独某条 SQL 更适合作为事务范围。

到这里，第12课的核心目标就完成了。

下一课就是整个第五阶段、也是整套五阶段课程的**最后一课：第13课——Spring 事务**。届时会把今天的思想真正落实到 Spring Boot 代码里，重点学习 `@Transactional`，并认识事务传播和几种常见的“明明加了 `@Transactional`，事务却没生效”的情况。



# 第13课：Spring 事务

这是第五阶段的最后一课，也是整套五阶段课程的最后一课。

上一课已经理解了事务为什么存在：**一个完整业务中的多条数据库操作，要么全部成功，要么全部失败。**

这一课把这个思想真正落到 Spring Boot 代码中，重点学习：

- `@Transactional`
- 事务传播：认识即可
- 常见事务失效场景：认识即可

这正对应你的第五阶段教学计划最后一课的内容。

## 一、`@Transactional` 到底做了什么

### 1. 先看没有事务的代码

假设转账：

```
@Service
public class AccountService {

    public void transfer(
            Long fromId,
            Long toId,
            BigDecimal amount) {

        accountMapper.subtract(fromId, amount);

        accountMapper.add(toId, amount);
    }
}
```

业务上：

```
A账户扣钱
↓
B账户加钱
```

如果第二步：

```
accountMapper.add(...)
```

执行失败，而第一步已经写入数据库，就可能留下：

```
A的钱扣了
B的钱没收到
```

上一课已经知道，这种业务应该使用事务。

------

### 2. Spring 的做法非常简单

我们可以：

```
@Transactional
public void transfer(
        Long fromId,
        Long toId,
        BigDecimal amount) {

    accountMapper.subtract(fromId, amount);

    accountMapper.add(toId, amount);
}
```

这个：

```
@Transactional
```

就是：

> **声明这个方法应该在一个事务中执行。**

概念上，Spring 帮我们做了类似：

```
开始事务
↓
执行 transfer()
↓
subtract()
↓
add()
↓
方法正常结束
↓
Commit
```

如果执行过程中发生需要回滚的异常：

```
开始事务
↓
subtract() 成功
↓
add() 抛异常
↓
Rollback
```

第一步也会被撤销。

所以你可以把：

```
@Transactional
```

先理解成：

> **“把这个业务方法包进一个事务。”**

------

### 3. 为什么它通常写在 Service

上一课我们讲过：

> 事务边界应该尽量与业务边界一致。

比如：

```
transfer()
```

是一个完整转账业务。

而：

```
accountMapper.subtract()
```

只是其中一个数据库操作。

因此：

```
Controller
↓
Service ← 事务通常放这里
↓
Mapper
↓
Database
```

很合理。

例如：

```
@Service
public class OrderService {

    @Transactional
    public void createOrder(...) {

        orderMapper.insert(...);

        stockMapper.reduceStock(...);
    }
}
```

我们真正希望保证的是：

```
创建订单
+
扣库存
```

作为一个整体。

而不是让两条 SQL 各自独立完成。

------

## 二、事务什么时候提交，什么时候回滚

### 1. 正常结束：提交

例如：

```
@Transactional
public void createOrder() {

    orderMapper.insert(...);

    stockMapper.reduceStock(...);
}
```

两个操作全部成功：

```
insert 成功
↓
reduceStock 成功
↓
方法正常返回
↓
Commit
```

数据库正式保留这些修改。

------

### 2. 出现异常：回滚

比如：

```
@Transactional
public void createOrder() {

    orderMapper.insert(...);

    stockMapper.reduceStock(...);

    throw new RuntimeException("测试异常");
}
```

执行过程：

```
创建订单成功
↓
扣库存成功
↓
抛异常
↓
Rollback
```

于是：

```
订单插入撤销
库存修改撤销
```

最终数据库回到事务开始之前的状态。

这也是你测试事务是否真的生效时，非常经典的一种办法：

> 人为在中间或末尾抛异常，然后观察前面的数据库操作有没有被撤销。

------

### 3. 默认情况下，不是所有异常都一样

这里需要稍微深入一点，但不必钻到底层。

Spring 的声明式事务默认主要会对：

```
RuntimeException
```

和：

```
Error
```

进行回滚。

例如：

```
throw new RuntimeException();
```

通常会回滚。

但是某些受检异常：

```
Exception
```

的默认处理规则可能不同。

因此真实项目里你可能看到：

```
@Transactional(rollbackFor = Exception.class)
```

意思大致是：

> 遇到 `Exception` 及其子类，也进行回滚。

例如：

```
@Transactional(rollbackFor = Exception.class)
public void createOrder() throws Exception {
    ...
}
```

你目前不需要死背所有异常规则。

但需要形成意识：

> **“方法抛了异常”并不意味着任何情况下事务规则都完全一样。**

看到：

```
rollbackFor
```

时，你应该知道它是在指定：

> 哪些异常触发事务回滚。

------

## 三、一个完整案例：注册用户

假设注册不是只插入一张表。

业务需要：

```
1. 创建用户
2. 创建用户资料
3. 分配默认角色
```

Service：

```
@Transactional
public void register(RegisterRequest request) {

    userMapper.insertUser(...);

    profileMapper.insertProfile(...);

    roleMapper.insertUserRole(...);
}
```

正常情况：

```
user 插入成功
↓
profile 插入成功
↓
role 插入成功
↓
Commit
```

数据库形成一个完整用户。

如果第三步：

```
roleMapper.insertUserRole(...)
```

报错：

```
user 已插入
profile 已插入
role 失败
↓
事务回滚
```

最终前两条也撤销。

所以数据库不会留下：

```
有账号
有资料
没有角色
```

这种半成品数据。

这正是 `@Transactional` 在项目里的典型意义。

------

## 四、`@Transactional` 不是“加上就百分百生效”

这是这一课最值得认识的部分。

实际开发中非常常见：

> “我明明加了 `@Transactional`，为什么数据库还是没有回滚？”

原因通常不是：

```
Spring 事务坏了
```

而是：

> 事务使用方式破坏了 Spring 的事务机制。

你的教学计划只要求“认识事务失效”，所以我们挑最常见的几种讲。

------

## 五、常见事务失效场景

### 1. 异常被你自己吃掉了

看这个：

```
@Transactional
public void createOrder() {

    try {
        orderMapper.insert(...);
        stockMapper.reduceStock(...);
    } catch (Exception e) {
        log.error("创建订单失败", e);
    }
}
```

假设：

```
stockMapper.reduceStock(...)
```

抛异常。

但你：

```
catch (Exception e)
```

把异常捕获了。

然后方法：

```
正常结束
```

从事务机制视角看：

```
“这个方法虽然内部发生过异常，
但最后它正常返回了。”
```

于是可能：

```
Commit
```

而不是 Rollback。

这就非常危险。

------

### 2. 为什么会这样

事务机制通常需要通过：

> 异常从事务方法中抛出去

来知道：

```
这次业务失败了
```

但你把异常完全吞掉：

```
异常
↓
catch
↓
只打印日志
↓
不再抛出
```

事务可能认为：

```
业务正常完成
```

因此，一个很重要的意识是：

> **如果业务应该失败并回滚，不要把异常吃掉以后假装方法正常成功。**

如果确实需要捕获，可以在处理后重新抛出：

```
catch (Exception e) {
    log.error("创建订单失败", e);
    throw e;
}
```

这样异常继续传播，事务才能感知失败。

------

### 3. 同一个类内部直接调用事务方法

这是 Spring 事务里非常经典的坑。

假设：

```
@Service
public class UserService {

    public void register() {
        saveUser();
    }

    @Transactional
    public void saveUser() {
        ...
    }
}
```

你可能认为：

```
register()
↓
调用 saveUser()
↓
saveUser 有 @Transactional
↓
事务生效
```

但在某些典型 Spring 代理机制下：

> **同一个类内部通过 `this` 直接调用自己的事务方法，可能绕过 Spring 代理，导致事务没有按预期生效。**

为什么？

这里不深入 Spring AOP 底层，只建立直觉。

Spring 实际上经常不是直接让你调用原始：

```
UserService
```

而是：

```
外部代码
↓
Spring 代理对象
↓
开启事务
↓
真正的 UserService 方法
```

相当于：

```
代理
{
    开事务
    调方法
    提交 / 回滚
}
```

但是：

```
register()
```

在类内部直接调用：

```
saveUser()
```

可能变成：

```
UserService 内部
↓
直接调用自己
```

没有重新经过外面的 Spring 代理。

于是：

```
@Transactional
```

可能没有被正确拦截。

你不用掌握代理源码。

只需要以后看到这种情况时警觉：

> **事务方法被同类内部直接调用，是经典事务失效排查点之一。**

------

### 4. 对象不是 Spring 管理的

例如：

```
UserService userService = new UserService();
userService.createUser();
```

哪怕：

```
@Transactional
public void createUser() {
}
```

也可能没有 Spring 事务。

为什么？

因为你自己：

```
new UserService()
```

创建的对象并不是 Spring 容器管理的 Bean。

Spring 没有机会给它套事务代理。

所以：

```
@Autowired / 构造器注入得到的 Bean
```

和：

```
自己 new 出来的对象
```

在这里不是一回事。

这也和你以前学 Spring Bean 时的知识连起来了：

> Spring 的很多功能，都依赖对象确实由 Spring 容器管理。

------

### 5. 数据库本身不支持事务

这在现代常用数据库配置中没那么常见，但概念上需要知道：

> Spring 能发起事务，不代表数据库底层什么情况下都一定支持。

比如 MySQL 某些存储引擎历史上并不支持事务。

现在常见的：

```
InnoDB
```

支持事务，所以一般不用担心。

你现阶段只需要知道：

> `@Transactional` 最终还是建立在数据库事务能力之上。

------

## 六、事务传播是什么

教学计划对事务传播只要求“认识”，所以不要被这个名字吓到。

事务传播其实是在解决一个问题：

> **一个事务方法调用另一个事务方法时，第二个方法应该怎么办？**

例如：

```
@Transactional
public void createOrder() {

    saveOrder();

    reduceStock();
}
```

而：

```
@Transactional
public void reduceStock() {
    ...
}
```

这时候：

```
createOrder 已经有事务
```

然后调用：

```
reduceStock
```

问题来了：

> `reduceStock()` 是加入已有事务，还是另开一个事务？

这就是：

> **事务传播（Propagation）**

------

### 1. 最常见的 `REQUIRED`

Spring 默认：

```
Propagation.REQUIRED
```

它的基本思想是：

> **有事务就加入，没有事务就新建。**

例如：

```
createOrder()
已经有事务A
↓
调用 reduceStock()
↓
reduceStock 加入事务A
```

于是：

```
创建订单
+
扣库存
```

还是同一个事务。

如果扣库存失败：

```
事务A整体回滚
```

这正符合大多数普通业务的需要。

所以平时：

```
@Transactional
```

不写传播属性，默认通常就够用了。

------

### 2. `REQUIRES_NEW`：认识即可

还有一种你以后可能看到：

```
@Transactional(
    propagation = Propagation.REQUIRES_NEW
)
```

可以粗略理解为：

> **不管外面有没有事务，我都开启自己的新事务。**

例如：

```
外层事务A
↓
调用方法B
↓
B暂停/独立于A
↓
开启事务B
```

这样：

```
事务A
```

和：

```
事务B
```

可能具有不同的提交与回滚结果。

什么时候会这么做？

例如某些独立日志、独立记录等特殊业务可能需要。

但你当前不需要掌握具体应用。

只需看到：

```
REQUIRED
```

想到：

> 加入已有事务，没有则创建。

看到：

```
REQUIRES_NEW
```

想到：

> 强制创建一个新的独立事务。

足够。

------

## 七、事务应该加得越多越好吗

不是。

### 1. 不要看到 Service 就无脑加

比如：

```
public User getById(Long id) {
    return userMapper.selectById(id);
}
```

只是简单查询。

你没必要因为：

> “事务很安全”

就到处乱加复杂事务配置。

事务本身也有管理成本。

------

### 2. 先从业务完整性判断

例如：

```
public void updateUsername(...) {
    userMapper.updateUsername(...);
}
```

只有一次简单修改。

数据库单条 SQL 本身就是一个完整操作。

虽然实际框架和数据库也会涉及事务，但从业务设计角度：

> 这里没有明显的“多步业务只成功一半”问题。

而：

```
public void transfer() {
    subtract();
    add();
}
```

明显就需要重点考虑事务。

所以仍然使用上一课那个判断问题：

> **如果执行到中间失败，我能不能接受当前数据库状态？**

不能：

```
重点考虑事务
```

------

## 八、事务不要包得过大

假设：

```
@Transactional
public void processOrder() {

    // 查询很多数据

    // 调用第三方HTTP接口

    // 做复杂计算

    // 等待外部服务

    // 修改数据库
}
```

事务持续时间过长通常不是好事。

因为数据库事务可能持有：

```
连接
锁
其他资源
```

时间越长，并发影响可能越明显。

所以理想上：

> **事务范围应该足够覆盖完整业务，但又不要无意义地扩大。**

对你现在而言不用做性能优化。

只需要形成：

```
事务边界
≈
需要保持原子性的数据库业务范围
```

而不是：

```
整个 Controller 请求从头包到尾
```

------

## 九、怎么验证 `@Transactional` 真的生效

这正好结合第8课“接口测试”和第4～6课“排 Bug”。

假设：

```
@Transactional
public void createOrder() {

    orderMapper.insert(order);

    stockMapper.reduceStock(productId);

    throw new RuntimeException("测试事务");
}
```

数据库原来：

```
订单：0条
库存：10
```

执行接口。

因为最后人为抛异常：

```
接口失败
```

然后检查数据库。

如果事务正常：

```
订单：还是0条
库存：还是10
```

说明：

```
前面的数据库操作被回滚
```

如果：

```
订单变成1条
库存变成9
```

说明：

> 事务没有按照预期生效。

这时候就按照前面讲的排查：

```
@Transactional 加在哪？
↓
对象是不是 Spring Bean？
↓
调用有没有经过代理？
↓
异常有没有被 catch 吃掉？
↓
异常类型是否符合回滚规则？
```

这就是一个完整的测试和排错闭环。

------

## 十、一个典型 Spring Boot Service 应该怎么看

以后 Codex 可能生成：

```
@Service
@RequiredArgsConstructor
public class OrderService {

    private final OrderMapper orderMapper;
    private final ProductMapper productMapper;

    @Transactional(rollbackFor = Exception.class)
    public void createOrder(
            Long userId,
            Long productId) {

        Product product =
                productMapper.selectById(productId);

        if (product == null) {
            throw new BusinessException("商品不存在");
        }

        if (product.getStock() <= 0) {
            throw new BusinessException("库存不足");
        }

        Order order = new Order();
        order.setUserId(userId);
        order.setProductId(productId);

        orderMapper.insert(order);

        productMapper.reduceStock(productId);
    }
}
```

你现在应该能把整个方法拆成：

```
@Service
→ Spring 管理的业务 Bean
@Transactional
→ createOrder 是一个事务业务
rollbackFor = Exception.class
→ 指定更广的异常回滚规则
```

然后业务：

```
查询商品
↓
商品不存在 → 异常
↓
库存不足 → 异常
↓
创建订单
↓
扣库存
↓
全部成功 → Commit
```

如果：

```
创建订单成功
但扣库存失败
```

那么：

```
Rollback
```

把订单创建也撤销。

如果你已经能这样读代码，就达到了这一课真正需要的程度。

------

## 十一、把第五阶段最后串成一个完整项目

现在回顾的目的不是重新总结所有知识，而是看看这13课最终组成了什么。

一个真正的小型 Spring Boot 后台项目开发流程，可以是：

```
Git 创建项目版本
↓
branch 开发新功能
↓
Controller / Service / Mapper
↓
参数校验
↓
数据库操作
↓
@Transactional 保证关键业务完整性
↓
登录认证
↓
角色权限控制
↓
Postman 测试接口
↓
测试正常 / 异常 / 边界情况
↓
发现 Bug
↓
日志定位
↓
Debug 深入排查
↓
修复
↓
重新测试
↓
Git commit
↓
push
```

这时候你已经不再只是学习：

```
Java语法
Spring注解
SQL
```

而是在逐步形成：

> **完整的软件项目开发与维护能力。**

这也正是第五阶段最初设定的目标：独立开发小型 Spring Boot 项目、调试 Bug、阅读日志、使用 Git、测试接口、理解登录权限与事务，并具备继续学习 Redis、Spring Security、微服务等内容的基础。第五阶段：项目能力教学计划.mdMD

------

## 十二、这一课真正要掌握什么

先记住：

```
@Transactional
```

可以理解成：

> **让这个业务方法在一个事务里执行。**

正常结束：

```
Commit
```

发生符合回滚规则的异常：

```
Rollback
```

然后认识三类常见事务失效思路：

```
异常被自己 catch 掉
同类内部直接调用，绕过 Spring 代理
对象不是 Spring 容器管理的 Bean
```

再认识事务传播：

```
REQUIRED
→ 有事务就加入，没有就创建
REQUIRES_NEW
→ 创建新的事务
```

最后继续保持上一课的核心判断：

> **事务不是为了“让 SQL 不报错”，而是为了避免一个完整业务只执行成功一半。**

到这里，**第13课完成，第五阶段全部完成。**

你最初规划的五个阶段——**Java 基础 → Web 基础 → Spring Boot 基础 → 数据库基础 → 项目能力**——也至此全部走完。